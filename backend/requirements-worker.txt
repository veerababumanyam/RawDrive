# =============================================================================
# Face Detection Worker Dependencies
# Separate from main backend to isolate CPU-heavy workloads
# =============================================================================

# Core async/web
fastapi==0.115.5
uvicorn[standard]==0.32.0
asyncpg==0.30.0
pydantic==2.9.2
pydantic-settings==2.4.0
email-validator==2.2.0

# Database
redis[hiredis]==5.1.1
alembic==1.13.2
SQLAlchemy==2.0.36
pg8000==1.31.5

# Storage (Cloudflare R2 - S3-compatible)
boto3==1.35.0

# Image processing
Pillow==10.4.0

# AI Providers for face detection
google-cloud-vision>=3.5.0
google-generativeai>=0.8.0

# Local face detection (OpenCV / MediaPipe)
opencv-python-headless>=4.8.0
numpy>=1.24.0
mediapipe>=0.10.0

# Encryption
cryptography==43.0.3

# HTTP client
httpx==0.27.2

# Auth dependencies (transitively imported)
PyJWT[crypto]==2.9.0
argon2-cffi==23.1.0
