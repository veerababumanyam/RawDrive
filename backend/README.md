# RawDrive Backend

FastAPI-based backend for RawDrive authentication, RBAC, workspace management, and platform services. This directory uses a `src/` layout. Development is done via Docker containers. See `.kiro/specs/auth-infrastructure` for the implementation plan.
