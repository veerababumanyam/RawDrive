# RawDrive Backend

FastAPI-based backend for RawDrive authentication, RBAC, workspace management, and platform services. This directory uses a `src/` layout. Install dependencies with `pip install -e .[dev]` from this folder. See `.kiro/specs/auth-infrastructure` for the implementation plan.
