---
phase: 10-foundation-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - frontend/src/pages/public/PublicPersonalProfilePage.tsx
  - frontend/src/pages/public/PublicProfilePage.tsx
  - frontend/src/components/features/profile/ProfileThemeEngine.ts
  - frontend/src/utils/themeTransformer.ts
  - frontend/src/services/themeService.ts
  - frontend/src/tests/smoke/profile-pages.test.tsx
autonomous: false
requirements:
  - FNDTN-05

must_haves:
  truths:
    - "PublicPersonalProfilePage (/u/:slug) renders using PublicProfileRenderer"
    - "PublicProfilePage (/p/:slug) renders using PublicProfileRenderer"
    - "Legacy theme engine files are deleted or emptied with re-export stubs"
    - "Smoke tests verify both pages load, avatar displays, and themes render without errors"
  artifacts:
    - path: "frontend/src/pages/public/PublicPersonalProfilePage.tsx"
      provides: "Personal profile page wired to PublicProfileRenderer"
      contains: "PublicProfileRenderer"
    - path: "frontend/src/pages/public/PublicProfilePage.tsx"
      provides: "Company profile page wired to PublicProfileRenderer"
      contains: "PublicProfileRenderer"
    - path: "frontend/src/tests/smoke/profile-pages.test.tsx"
      provides: "Smoke tests for both profile pages"
      contains: "profile-smoke"
  key_links:
    - from: "frontend/src/pages/public/PublicPersonalProfilePage.tsx"
      to: "frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx"
      via: "import and render with profileType='personal'"
      pattern: "profileType.*personal"
    - from: "frontend/src/pages/public/PublicProfilePage.tsx"
      to: "frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx"
      via: "import and render with profileType='company'"
      pattern: "profileType.*company"
---

<objective>
Wire both public profile pages to the shared PublicProfileRenderer, delete legacy theme engine files, and add smoke tests verifying both pages work end-to-end.

Purpose: Plans 01 and 02 created the new components in isolation. This plan connects them to the actual route pages, removes dead code, and adds smoke tests as the final verification gate.

Output: Both /u/:slug and /p/:slug use PublicProfileRenderer, legacy theme files removed, smoke tests passing.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation-fixes/10-CONTEXT.md
@.planning/phases/10-foundation-fixes/10-RESEARCH.md
@.planning/phases/10-foundation-fixes/10-01-SUMMARY.md
@.planning/phases/10-foundation-fixes/10-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From frontend/src/components/features/profile/shared/AvatarDisplay.tsx:
```typescript
export const AvatarDisplay: React.FC<{
  avatarUrl?: string | null;
  displayName: string;
  size?: 'sm' | 'md' | 'lg';
  className?: string;
}>
```

<!-- From Plan 02 output -->
From frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx:
```typescript
export const PublicProfileRenderer: React.FC<{
  profileData: Record<string, unknown>;
  profileType: 'personal' | 'company';
  themeId?: string;
}>
```

From frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts:
```typescript
export function resolveThemeTokens(themeId: string, prefersDark: boolean): ThemeTokens;
export function applyThemeToRoot(tokens: ThemeTokens, element?: HTMLElement): void;
export function removeThemeFromRoot(element?: HTMLElement): void;
export function resolveThemeId(rawId: string | undefined | null): string;
export const LEGACY_TO_PREBUILT_MAP: Record<string, string>;
```

From frontend/src/components/features/profile/shared/SectionRegistry.ts:
```typescript
export type ProfileType = 'personal' | 'company';
export function getSectionsForProfile(type: ProfileType, data: Record<string, unknown>): SectionRegistryEntry[];
```

Legacy files to delete:
- frontend/src/components/features/profile/ProfileThemeEngine.ts
- frontend/src/utils/themeTransformer.ts
- frontend/src/services/themeService.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire profile pages to PublicProfileRenderer and remove legacy theme files</name>
  <files>
    frontend/src/pages/public/PublicPersonalProfilePage.tsx,
    frontend/src/pages/public/PublicProfilePage.tsx,
    frontend/src/components/features/profile/ProfileThemeEngine.ts,
    frontend/src/utils/themeTransformer.ts,
    frontend/src/services/themeService.ts
  </files>
  <read_first>
    frontend/src/pages/public/PublicPersonalProfilePage.tsx,
    frontend/src/pages/public/PublicProfilePage.tsx,
    frontend/src/components/features/profile/public/PublicProfileView.tsx,
    frontend/src/components/features/profile/ProfileThemeEngine.ts,
    frontend/src/utils/themeTransformer.ts,
    frontend/src/services/themeService.ts
  </read_first>
  <action>
    1. Refactor `PublicPersonalProfilePage.tsx`:
       - Keep existing data fetching logic (useParams, TanStack Query for profile data)
       - Replace the inline Bento Grid rendering with `<PublicProfileRenderer profileData={profileData} profileType="personal" themeId={profileData?.theme_id} />`
       - Remove imports of ProfileBentoGrid, ProfileHeader, ProfileBio, ProfileSocials, ProfileContactGrid (now handled by renderer)
       - Remove any direct theme application code (ProfileThemeEngine usage)
       - Keep react-helmet-async SEO meta tags
       - Keep loading/error states

    2. Refactor `PublicProfilePage.tsx`:
       - Keep existing data fetching logic for company profile
       - Replace PublicProfileView usage with `<PublicProfileRenderer profileData={profileData} profileType="company" themeId={profileData?.theme_id} />`
       - Remove import of PublicProfileView
       - Keep react-helmet-async SEO meta tags
       - Keep loading/error states

    3. Handle legacy theme file removal:
       - Search codebase for all imports of `ProfileThemeEngine`, `themeTransformer`, `themeService` (besides the new UnifiedThemeEngine)
       - If other files still import these (e.g., editor pages): create thin re-export stubs that delegate to UnifiedThemeEngine
       - If no other imports: delete the files entirely
       - The goal: no production code path uses legacy theme logic. Editor pages may still need a stub during transition.

    4. Verify no broken imports:
       - Run `cd frontend && pnpm build` to catch any broken imports from deleted files
       - Fix any remaining references
  </action>
  <verify>
    <automated>cd frontend && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `PublicPersonalProfilePage.tsx` contains `import.*PublicProfileRenderer` from shared path
    - `PublicPersonalProfilePage.tsx` contains `profileType="personal"` or `profileType={'personal'}`
    - `PublicPersonalProfilePage.tsx` does NOT contain `import.*ProfileBentoGrid` (direct usage removed)
    - `PublicProfilePage.tsx` contains `import.*PublicProfileRenderer` from shared path
    - `PublicProfilePage.tsx` contains `profileType="company"` or `profileType={'company'}`
    - `PublicProfilePage.tsx` does NOT contain `import.*PublicProfileView` (legacy view removed)
    - `ProfileThemeEngine.ts` is either deleted or contains only re-export stubs pointing to UnifiedThemeEngine
    - `themeTransformer.ts` is either deleted or contains only re-export stubs
    - `themeService.ts` is either deleted or contains only re-export stubs
    - `cd frontend && pnpm build` exits 0 (no broken imports)
  </acceptance_criteria>
  <done>Both profile pages render through PublicProfileRenderer. Legacy theme files deleted or stubbed. Frontend builds without errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Smoke tests for both profile pages</name>
  <files>
    frontend/src/tests/smoke/profile-pages.test.tsx
  </files>
  <read_first>
    frontend/src/pages/public/PublicPersonalProfilePage.tsx,
    frontend/src/pages/public/PublicProfilePage.tsx,
    frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx,
    frontend/src/components/features/profile/shared/AvatarDisplay.tsx,
    frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts
  </read_first>
  <behavior>
    - Test 1: Personal profile page renders display name when profile data is provided
    - Test 2: Company profile page renders company name when profile data is provided
    - Test 3: Personal profile page shows AvatarDisplay with avatar URL from profile data
    - Test 4: Personal profile page shows initials fallback when avatar URL is null
    - Test 5: Theme CSS variables are applied when themeId is provided
    - Test 6: Legacy theme ID (e.g., 'minimal') resolves to PREBUILT and renders without error
    - Test 7: Both pages render without console errors (no uncaught exceptions)
  </behavior>
  <action>
    1. Create `frontend/src/tests/smoke/` directory if not exists.

    2. Create `profile-pages.test.tsx`:
       - Mock TanStack Query's `useQuery` to return controlled profile data
       - Mock `useParams` to return a slug
       - Mock `window.matchMedia` for dark mode detection

       Personal profile smoke tests:
       - Render PublicPersonalProfilePage with mock data containing `display_name: "Jane Smith"`, `bio: "Photographer"`, `avatar_url: "https://example.com/avatar.jpg"`, `theme_id: "theme-clean-slate"`, `social_links: [...]`
       - Assert "Jane Smith" appears in document
       - Assert img element with avatar URL exists
       - Render with `avatar_url: null`, assert initials "JS" appear

       Company profile smoke tests:
       - Render PublicProfilePage with mock data containing `company_name: "Smith Studios"`, `bio: "Photography studio"`, `logo_url: "https://example.com/logo.jpg"`, `theme_id: "minimal"` (legacy ID)
       - Assert "Smith Studios" appears in document
       - Assert no console errors (spy on console.error)

       Theme smoke tests:
       - Render with a PREBUILT theme ID, verify CSS custom properties are set on the wrapper
       - Render with a legacy theme ID, verify it resolves without error

    3. Use `@testing-library/react` for rendering and assertions.
       Use `vi.spyOn(console, 'error')` to catch unexpected errors.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run src/tests/smoke/profile-pages.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/tests/smoke/profile-pages.test.tsx` exists
    - Test file contains `describe` blocks for both personal and company profiles
    - Test file contains at least 5 test cases (`it(` or `test(`)
    - Test file imports `PublicProfileRenderer` or the page components
    - Test file contains `console.error` spy for error detection
    - `cd frontend && pnpm test --run src/tests/smoke/profile-pages.test.tsx` exits 0
  </acceptance_criteria>
  <done>Smoke tests verify both profile pages load, render avatar (with fallback), apply themes (including legacy IDs), and produce no console errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of both profile pages</name>
  <files>None (verification only)</files>
  <action>
    Human verifies complete Phase 10 output:
    - Avatar upload pipeline migrated to R2 with lazy fallback from PostgreSQL
    - AvatarDisplay component with initials fallback when images fail
    - UnifiedThemeEngine replacing 3 legacy theme files, with legacy ID mapping
    - PublicProfileRenderer shared by both /u/:slug and /p/:slug pages
    - Section registry dynamically selecting sections by profile type
    - Smoke tests covering both pages, avatars, and themes

    Steps:
    1. Start dev server: `cd frontend && pnpm dev`
    2. Visit http://localhost:5173/u/test-user -- verify profile sections, avatar, theme colors
    3. Visit http://localhost:5173/p/test-company -- verify shared renderer, company logo
    4. Upload a new avatar -- verify it displays on public page after reload
    5. Check browser console -- no errors related to themes, avatars, or profile rendering
  </action>
  <verify>
    <automated>cd frontend && pnpm build</automated>
  </verify>
  <done>Human confirms both profile pages render correctly with avatars, themes, and shared renderer. No console errors.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm build` succeeds (no broken imports)
- `cd frontend && pnpm test --run src/tests/smoke/profile-pages.test.tsx` passes
- `cd frontend && pnpm test --run src/components/features/profile/shared/__tests__/` passes (all prior tests still green)
- Both /u/:slug and /p/:slug pages render through PublicProfileRenderer
- Legacy theme files deleted or reduced to re-export stubs
</verification>

<success_criteria>
- Both public profile pages use PublicProfileRenderer (verified by imports)
- Legacy ProfileThemeEngine.ts, themeTransformer.ts, themeService.ts are deleted or stubbed
- Smoke tests pass for both profile types with avatar display and theme rendering
- Frontend builds cleanly with no broken imports
- Human verification confirms visual correctness on both pages
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-fixes/10-03-SUMMARY.md`
</output>
