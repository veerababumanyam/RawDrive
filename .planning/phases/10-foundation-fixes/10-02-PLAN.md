---
phase: 10-foundation-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts
  - frontend/src/components/features/profile/shared/SectionRegistry.ts
  - frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx
  - frontend/src/components/features/profile/shared/sections/HeaderSection.tsx
  - frontend/src/components/features/profile/shared/sections/BioSection.tsx
  - frontend/src/components/features/profile/shared/sections/ContactSection.tsx
  - frontend/src/components/features/profile/shared/sections/SocialsSection.tsx
  - frontend/src/components/features/profile/shared/sections/index.ts
  - frontend/src/components/features/profile/shared/__tests__/UnifiedThemeEngine.test.ts
  - frontend/src/components/features/profile/shared/__tests__/PublicProfileRenderer.test.tsx
  - frontend/src/components/features/profile/shared/__tests__/SectionRegistry.test.ts
autonomous: true
requirements:
  - FNDTN-03
  - FNDTN-04

must_haves:
  truths:
    - "Theme selection applies consistently via CSS custom properties on both profile types"
    - "Legacy theme IDs (minimal, dark, pastel, bold, cinematic) resolve to nearest PREBUILT equivalent"
    - "Both /u/:slug and /p/:slug render using the same PublicProfileRenderer component"
    - "Section registry dynamically selects sections based on profile type and available data"
    - "No legacy theme engine code remains (ProfileThemeEngine.ts, themeTransformer.ts, themeService.ts replaced)"
  artifacts:
    - path: "frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts"
      provides: "Single theme engine with CSS custom property generation"
      exports: ["resolveThemeTokens", "applyThemeToRoot", "resolveThemeId", "LEGACY_TO_PREBUILT_MAP", "ThemeTokens"]
    - path: "frontend/src/components/features/profile/shared/SectionRegistry.ts"
      provides: "Section registry for dynamic section selection by profile type"
      exports: ["getSectionsForProfile", "SectionRegistryEntry", "ProfileType"]
    - path: "frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx"
      provides: "Shared renderer for both personal and company profiles"
      exports: ["PublicProfileRenderer"]
  key_links:
    - from: "frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx"
      to: "frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts"
      via: "resolveThemeTokens + applyThemeToRoot on mount"
      pattern: "resolveThemeTokens|applyThemeToRoot"
    - from: "frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx"
      to: "frontend/src/components/features/profile/shared/SectionRegistry.ts"
      via: "getSectionsForProfile to get applicable sections"
      pattern: "getSectionsForProfile"
    - from: "frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts"
      to: "frontend/src/constants/themes.ts"
      via: "getThemeById to look up PREBUILT themes"
      pattern: "getThemeById|getDefaultTheme"
---

<objective>
Consolidate the fragmented theme engine into a single UnifiedThemeEngine with CSS custom properties, and build a shared PublicProfileRenderer with section registry that both personal and company profiles use.

Purpose: Currently 3 separate theme files and 2 divergent profile pages make maintenance expensive and behavior inconsistent. This plan creates a single source of truth for themes and a unified rendering path.

Output: UnifiedThemeEngine.ts, SectionRegistry.ts, PublicProfileRenderer.tsx, 4 section wrapper components, all tests passing.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation-fixes/10-CONTEXT.md
@.planning/phases/10-foundation-fixes/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/constants/themes.ts:
```typescript
export const PREBUILT_THEMES: Theme[] = [...]  // 20 themes
export function getThemeById(themeId: string): Theme | undefined;
export function getDefaultTheme(): Theme;
export function getAllThemes(): Theme[];
// Theme type has: id, name, base_colors, variants (with colors.background, colors.surface, colors.text_primary, etc.), default_typography (heading_font, body_font)
```

From frontend/src/components/features/profile/ProfileBentoGrid.tsx:
```typescript
export const ProfileBentoGrid: React.FC<ProfileBentoGridProps>
// Props: children, className
```

From frontend/src/components/features/profile/ProfileHeader.tsx:
```typescript
export const ProfileHeader: React.FC<ProfileHeaderProps>
// Props: name, title, location, avatarUrl, isVerified, etc.
```

From frontend routing:
```typescript
// /p/:slug -> PublicProfilePage -> PublicProfileView (company)
// /u/:slug -> PublicPersonalProfilePage (personal, uses ProfileBentoGrid)
```

Legacy theme files to replace:
- frontend/src/components/features/profile/ProfileThemeEngine.ts
- frontend/src/utils/themeTransformer.ts
- frontend/src/services/themeService.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UnifiedThemeEngine with CSS custom properties and legacy mapping</name>
  <files>
    frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts,
    frontend/src/components/features/profile/shared/__tests__/UnifiedThemeEngine.test.ts
  </files>
  <read_first>
    frontend/src/constants/themes.ts,
    frontend/src/components/features/profile/ProfileThemeEngine.ts,
    frontend/src/utils/themeTransformer.ts,
    frontend/src/services/themeService.ts
  </read_first>
  <behavior>
    - Test 1: resolveThemeId returns PREBUILT ID for known legacy theme (e.g., 'minimal' -> 'theme-clean-slate')
    - Test 2: resolveThemeId returns input unchanged for valid PREBUILT theme ID
    - Test 3: resolveThemeId returns 'theme-clean-slate' for undefined/null input
    - Test 4: resolveThemeTokens returns ThemeTokens object with all required CSS variable keys
    - Test 5: resolveThemeTokens with prefersDark=true selects dark variant colors
    - Test 6: resolveThemeTokens with prefersDark=false selects light variant colors
    - Test 7: resolveThemeTokens with unknown theme ID falls back to default theme
    - Test 8: applyThemeToRoot sets CSS custom properties on provided element
    - Test 9: LEGACY_TO_PREBUILT_MAP contains entries for all 5 legacy themes (minimal, dark, pastel, bold, cinematic)
  </behavior>
  <action>
    1. Create `frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts`:

       Export `ThemeTokens` interface with these exact CSS variable keys:
       `--theme-bg`, `--theme-surface`, `--theme-text`, `--theme-text-secondary`, `--theme-accent`, `--theme-primary`, `--theme-border`, `--theme-font-heading`, `--theme-font-body`, `--theme-radius`, `--theme-shadow`, `--theme-gradient`

       Export `LEGACY_TO_PREBUILT_MAP: Record<string, string>` mapping:
       - `'minimal'` -> look up nearest clean/minimal PREBUILT in themes.ts
       - `'dark'` -> look up nearest dark PREBUILT
       - `'pastel'` -> look up nearest soft/pastel PREBUILT
       - `'bold'` -> look up nearest bold/vivid PREBUILT
       - `'cinematic'` -> look up nearest cinematic/film PREBUILT
       (Executor: read themes.ts PREBUILT_THEMES array and pick closest visual match for each)

       Export `resolveThemeId(rawId: string | undefined | null): string`:
       - If falsy, return `'theme-clean-slate'` (or whatever the first/default PREBUILT ID is)
       - If in LEGACY_TO_PREBUILT_MAP, return mapped value
       - Otherwise return rawId as-is

       Export `resolveThemeTokens(themeId: string, prefersDark: boolean): ThemeTokens`:
       - Call `resolveThemeId(themeId)` first
       - Use `getThemeById()` from constants/themes.ts, fallback to `getDefaultTheme()`
       - Select dark or light variant based on `prefersDark`
       - Map theme properties to CSS variable values
       - Include font family with fallback stack

       Export `applyThemeToRoot(tokens: ThemeTokens, element?: HTMLElement): void`:
       - Default element to `document.documentElement`
       - Iterate `Object.entries(tokens)` and call `element.style.setProperty(key, value)`

       Export `removeThemeFromRoot(element?: HTMLElement): void`:
       - Clean up CSS vars on unmount to prevent leaking into other pages
       - Remove all `--theme-*` properties from element

    2. Create test file at `shared/__tests__/UnifiedThemeEngine.test.ts`:
       - Import from `../UnifiedThemeEngine`
       - For applyThemeToRoot tests, create a mock HTMLElement with `style.setProperty` spy
       - Verify all 5 legacy themes exist in LEGACY_TO_PREBUILT_MAP
       - Verify token object has all 12 CSS variable keys
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run src/components/features/profile/shared/__tests__/UnifiedThemeEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `UnifiedThemeEngine.ts` contains `export interface ThemeTokens` with `--theme-bg` through `--theme-gradient`
    - `UnifiedThemeEngine.ts` contains `export const LEGACY_TO_PREBUILT_MAP` with keys `minimal`, `dark`, `pastel`, `bold`, `cinematic`
    - `UnifiedThemeEngine.ts` contains `export function resolveThemeId`
    - `UnifiedThemeEngine.ts` contains `export function resolveThemeTokens`
    - `UnifiedThemeEngine.ts` contains `export function applyThemeToRoot`
    - `UnifiedThemeEngine.ts` contains `export function removeThemeFromRoot`
    - `UnifiedThemeEngine.ts` imports `getThemeById` from `constants/themes`
    - `UnifiedThemeEngine.test.ts` contains at least 7 test cases
    - `cd frontend && pnpm test --run src/components/features/profile/shared/__tests__/UnifiedThemeEngine.test.ts` exits 0
  </acceptance_criteria>
  <done>UnifiedThemeEngine resolves themes (including legacy mapping), generates CSS tokens, applies/removes them from DOM elements. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SectionRegistry + PublicProfileRenderer + section wrappers</name>
  <files>
    frontend/src/components/features/profile/shared/SectionRegistry.ts,
    frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx,
    frontend/src/components/features/profile/shared/sections/HeaderSection.tsx,
    frontend/src/components/features/profile/shared/sections/BioSection.tsx,
    frontend/src/components/features/profile/shared/sections/ContactSection.tsx,
    frontend/src/components/features/profile/shared/sections/SocialsSection.tsx,
    frontend/src/components/features/profile/shared/sections/index.ts,
    frontend/src/components/features/profile/shared/__tests__/SectionRegistry.test.ts,
    frontend/src/components/features/profile/shared/__tests__/PublicProfileRenderer.test.tsx
  </files>
  <read_first>
    frontend/src/components/features/profile/ProfileBentoGrid.tsx,
    frontend/src/components/features/profile/ProfileGridItem.tsx,
    frontend/src/components/features/profile/ProfileHeader.tsx,
    frontend/src/components/features/profile/ProfileBio.tsx,
    frontend/src/components/features/profile/ProfileSocials.tsx,
    frontend/src/components/features/profile/ProfileContactGrid.tsx,
    frontend/src/pages/public/PublicPersonalProfilePage.tsx,
    frontend/src/pages/public/PublicProfilePage.tsx,
    frontend/src/components/features/profile/public/PublicProfileView.tsx
  </read_first>
  <behavior>
    - Test 1 (Registry): getSectionsForProfile('personal', fullData) returns header, bio, contact, socials sections
    - Test 2 (Registry): getSectionsForProfile('company', fullData) returns header, bio, contact, socials sections
    - Test 3 (Registry): getSectionsForProfile filters out sections when requiredData is missing (e.g., no bio key -> no bio section)
    - Test 4 (Registry): Sections are returned sorted by order field
    - Test 5 (Renderer): PublicProfileRenderer renders with profileType='personal' and shows all sections
    - Test 6 (Renderer): PublicProfileRenderer renders with profileType='company' and shows all sections
    - Test 7 (Renderer): PublicProfileRenderer applies theme via UnifiedThemeEngine on mount
    - Test 8 (Renderer): PublicProfileRenderer cleans up theme CSS vars on unmount
  </behavior>
  <action>
    1. Create `SectionRegistry.ts`:
       ```typescript
       export type ProfileType = 'personal' | 'company';

       export interface SectionProps {
         profileData: Record<string, unknown>;
         profileType: ProfileType;
       }

       export interface SectionRegistryEntry {
         id: string;
         component: React.ComponentType<SectionProps>;
         supportedTypes: ProfileType[];
         requiredData: string[];
         gridSpan?: { cols: number; rows: number };
         order: number;
       }
       ```
       - Export `SECTION_REGISTRY` array with entries for header (order 0, both types, requiredData: ['display_name']), bio (order 1, both, requiredData: ['bio']), contact (order 2, both, requiredData: []), socials (order 3, both, requiredData: ['social_links'])
       - Export `getSectionsForProfile(type: ProfileType, profileData: Record<string, unknown>): SectionRegistryEntry[]`
       - Filters by supportedTypes, filters by requiredData presence, sorts by order

    2. Create section wrapper components in `shared/sections/`:

       Each section wrapper imports the existing component and adapts SectionProps to the original component's props:

       - `HeaderSection.tsx`: Wraps ProfileHeader -- maps `profileData.display_name` to `name`, `profileData.title` to `title`, `profileData.location` to `location`, normalizes avatar URL (uses `profileData.avatar_url || profileData.logo_url` for company). Import AvatarDisplay from plan 01's output (use relative import `../AvatarDisplay`).
       - `BioSection.tsx`: Wraps ProfileBio -- maps `profileData.bio` to bio prop
       - `ContactSection.tsx`: Wraps ProfileContactGrid -- maps contact fields
       - `SocialsSection.tsx`: Wraps ProfileSocials -- maps `profileData.social_links`
       - `index.ts`: Re-exports all sections + creates the registry entries array

    3. Create `PublicProfileRenderer.tsx`:
       ```typescript
       interface PublicProfileRendererProps {
         profileData: Record<string, unknown>;
         profileType: ProfileType;
         themeId?: string;
       }
       ```
       - On mount: detect `prefers-color-scheme` via `window.matchMedia('(prefers-color-scheme: dark)')`, call `resolveThemeTokens(themeId, prefersDark)`, apply tokens to a scoped wrapper div (NOT document.documentElement -- prevents CSS leaking per Pitfall 4)
       - On unmount: call `removeThemeFromRoot` on the wrapper element
       - Call `getSectionsForProfile(profileType, profileData)` to get applicable sections
       - Render sections inside `ProfileBentoGrid` using `ProfileGridItem` with gridSpan from registry
       - Normalize company profile field names: map `logo_url` to `avatar_url`, `company_name` to `display_name` if profileType is 'company' (per Pitfall 5)
       - Handle font loading: extract font loading pattern from existing PublicProfileView (fontsLoaded state, show skeleton until fonts ready)

    4. Create test files:
       - `SectionRegistry.test.ts`: Test filtering, sorting, requiredData logic
       - `PublicProfileRenderer.test.tsx`: Test rendering with personal and company data, mock UnifiedThemeEngine functions, verify sections appear
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run src/components/features/profile/shared/__tests__/SectionRegistry.test.ts src/components/features/profile/shared/__tests__/PublicProfileRenderer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `SectionRegistry.ts` contains `export type ProfileType = 'personal' | 'company'`
    - `SectionRegistry.ts` contains `export function getSectionsForProfile`
    - `SectionRegistry.ts` contains `export interface SectionRegistryEntry` with `supportedTypes` field
    - `PublicProfileRenderer.tsx` contains `export const PublicProfileRenderer` or `export function PublicProfileRenderer`
    - `PublicProfileRenderer.tsx` imports `getSectionsForProfile` from `SectionRegistry`
    - `PublicProfileRenderer.tsx` imports `resolveThemeTokens` from `UnifiedThemeEngine`
    - `PublicProfileRenderer.tsx` contains `prefers-color-scheme` for dark mode detection
    - `PublicProfileRenderer.tsx` contains `removeThemeFromRoot` in cleanup/unmount
    - `HeaderSection.tsx` exists and imports `ProfileHeader`
    - `BioSection.tsx` exists and imports `ProfileBio`
    - `sections/index.ts` exists and re-exports all sections
    - Both test files exist and `cd frontend && pnpm test --run src/components/features/profile/shared/__tests__/SectionRegistry.test.ts` exits 0
    - `cd frontend && pnpm test --run src/components/features/profile/shared/__tests__/PublicProfileRenderer.test.tsx` exits 0
  </acceptance_criteria>
  <done>SectionRegistry dynamically selects sections by profile type. PublicProfileRenderer renders both personal and company profiles through shared path with scoped theme application. All tests pass.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm test --run src/components/features/profile/shared/__tests__/UnifiedThemeEngine.test.ts` passes
- `cd frontend && pnpm test --run src/components/features/profile/shared/__tests__/SectionRegistry.test.ts` passes
- `cd frontend && pnpm test --run src/components/features/profile/shared/__tests__/PublicProfileRenderer.test.tsx` passes
- UnifiedThemeEngine replaces ProfileThemeEngine.ts, themeTransformer.ts, themeService.ts
- Legacy theme IDs map to PREBUILT equivalents
- PublicProfileRenderer handles both personal and company profile types
</verification>

<success_criteria>
- Single UnifiedThemeEngine generates CSS custom properties from any theme ID (including legacy)
- SectionRegistry filters sections by profile type and available data
- PublicProfileRenderer renders both profile types using shared sections inside BentoGrid
- Theme CSS variables are scoped to profile container (no leaking)
- Dark mode detected via prefers-color-scheme and applies correct variant
- All automated tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-fixes/10-02-SUMMARY.md`
</output>
