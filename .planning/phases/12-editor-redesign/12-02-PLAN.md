---
phase: 12-editor-redesign
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - frontend/src/components/features/settings/DndSectionList.tsx
  - frontend/src/components/features/settings/DndSectionList.test.tsx
  - frontend/src/components/features/settings/DeviceFramePreview.tsx
  - frontend/src/components/features/settings/DeviceFramePreview.test.tsx
  - frontend/src/components/features/settings/GradientColorPicker.tsx
  - frontend/src/components/features/settings/GradientColorPicker.test.tsx
autonomous: true
requirements:
  - EDITR-02
  - EDITR-03
  - EDITR-04

must_haves:
  truths:
    - "User can drag a section handle and drop it to reorder; the new order dispatches SET_SECTION_ORDER"
    - "User can pick a solid color or gradient via the visual picker; it dispatches SET_FIELD for theme colors"
    - "User can toggle between mobile (375px), tablet (768px), and desktop (1280px) frames that scale the preview"
  artifacts:
    - path: "frontend/src/components/features/settings/DndSectionList.tsx"
      provides: "Drag-and-drop sortable list of profile sections"
      exports: ["DndSectionList"]
    - path: "frontend/src/components/features/settings/DeviceFramePreview.tsx"
      provides: "Scaled preview container with device chrome"
      exports: ["DeviceFramePreview"]
    - path: "frontend/src/components/features/settings/GradientColorPicker.tsx"
      provides: "Gradient + solid color picker wrapper"
      exports: ["GradientColorPicker"]
  key_links:
    - from: "DndSectionList.tsx"
      to: "ProfileEditorContext.tsx"
      via: "useProfileEditorDispatch SET_SECTION_ORDER"
      pattern: "dispatch.*SET_SECTION_ORDER"
    - from: "DeviceFramePreview.tsx"
      to: "ProfileEditorContext.tsx"
      via: "useProfileEditor reads deviceMode"
      pattern: "deviceMode"
    - from: "GradientColorPicker.tsx"
      to: "ProfileEditorContext.tsx"
      via: "useProfileEditorDispatch SET_FIELD"
      pattern: "dispatch.*SET_FIELD"
---

<objective>
Build the three core editor UI components: drag-and-drop section reordering, device frame
preview with responsive scaling, and visual gradient/solid color picker.

Purpose: These are the interactive editor features users will use to customize their profiles.
Output: Three tested, self-contained components ready to wire into editors in Plan 03.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-editor-redesign/12-CONTEXT.md
@.planning/phases/12-editor-redesign/12-01-SUMMARY.md

@frontend/src/contexts/ProfileEditorContext.tsx
@frontend/src/types/profileEditor.ts
@frontend/src/components/features/profile/shared/SectionRegistry.ts
@frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx
</context>

<interfaces>
<!-- From Plan 01 outputs -->

From frontend/src/contexts/ProfileEditorContext.tsx (created in Plan 01):
```typescript
export function ProfileEditorProvider(props: {
  initialProfile: Record<string, unknown>;
  profileType: ProfileType;
  initialThemeId?: string;
  initialSectionOrder?: string[];
  children: React.ReactNode;
}): JSX.Element;

export function useProfileEditor(): EditorState & {
  profileType: ProfileType;
  sectionOrder: string[];
};

export function useProfileEditorDispatch(): React.Dispatch<EditorAction>;

// Action types: SET_FIELD, SET_THEME, SET_DEVICE_MODE, SET_SECTION_ORDER, RESET, MARK_SAVED, SET_SAVING
```

From frontend/src/types/profileEditor.ts:
```typescript
export type DeviceMode = 'phone' | 'tablet' | 'desktop';
export const DEVICE_BREAKPOINTS: Record<DeviceMode, { width: number; height: number }> = {
  phone: { width: 375, height: 812 },
  tablet: { width: 768, height: 1024 },
  desktop: { width: 1440, height: 900 },
};
```

From frontend/src/components/features/profile/shared/SectionRegistry.ts:
```typescript
export const SECTION_REGISTRY: SectionRegistryEntry[];
// IDs: 'header', 'bio', 'contact', 'socials'
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DnD section reordering with @dnd-kit</name>
  <files>
    frontend/src/components/features/settings/DndSectionList.tsx,
    frontend/src/components/features/settings/DndSectionList.test.tsx
  </files>
  <read_first>
    frontend/src/contexts/ProfileEditorContext.tsx,
    frontend/src/components/features/profile/shared/SectionRegistry.ts,
    frontend/src/types/profileEditor.ts
  </read_first>
  <behavior>
    - Test 1: Renders all section items from sectionOrder with drag handles
    - Test 2: Calls dispatch(SET_SECTION_ORDER) when items are reordered via onDragEnd
    - Test 3: Each item shows section name (capitalized ID) and a GripVertical icon
    - Test 4: Applies Framer Motion layoutId to each item for smooth animation
  </behavior>
  <action>
1. Create `DndSectionList.tsx`:
   - Import from @dnd-kit/core: DndContext, closestCenter, KeyboardSensor, PointerSensor, useSensor, useSensors
   - Import from @dnd-kit/sortable: SortableContext, verticalListSortingStrategy, useSortable, arrayMove
   - Import from @dnd-kit/utilities: CSS (for transform)
   - Read sectionOrder from useProfileEditor()
   - Read dispatch from useProfileEditorDispatch()
   - Each sortable item renders:
     - GripVertical icon (from lucide-react) as drag handle via {...attributes, ...listeners}
     - Section name: capitalize the section ID (e.g., 'header' -> 'Header')
     - Framer Motion `motion.div` with `layoutId={sectionId}` for smooth reorder animation
     - Tailwind classes: `flex items-center gap-3 p-3 bg-white dark:bg-zinc-800 rounded-lg border border-zinc-200 dark:border-zinc-700 cursor-grab active:cursor-grabbing`
   - onDragEnd handler: compute new order via arrayMove, dispatch SET_SECTION_ORDER with new array
   - PointerSensor with activationConstraint distance: 8 (prevents accidental drags)
   - KeyboardSensor for accessibility

2. Create `DndSectionList.test.tsx`:
   - Mock ProfileEditorContext
   - Verify renders all section items
   - Verify drag handle icon exists for each item
   - Verify onDragEnd dispatches correct action
  </action>
  <verify>
    <automated>cd frontend && pnpm test src/components/features/settings/DndSectionList.test.tsx --run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "DndContext" frontend/src/components/features/settings/DndSectionList.tsx
    - grep -q "SortableContext" frontend/src/components/features/settings/DndSectionList.tsx
    - grep -q "useSortable" frontend/src/components/features/settings/DndSectionList.tsx
    - grep -q "SET_SECTION_ORDER" frontend/src/components/features/settings/DndSectionList.tsx
    - grep -q "arrayMove" frontend/src/components/features/settings/DndSectionList.tsx
    - grep -q "GripVertical" frontend/src/components/features/settings/DndSectionList.tsx
    - grep -q "layoutId" frontend/src/components/features/settings/DndSectionList.tsx
  </acceptance_criteria>
  <done>
    - DndSectionList renders sortable section items with drag handles
    - Reordering dispatches SET_SECTION_ORDER with new array via arrayMove
    - Framer Motion layoutId on each item for smooth animation
    - PointerSensor (distance 8) + KeyboardSensor configured
    - Tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Device frame preview and gradient color picker</name>
  <files>
    frontend/src/components/features/settings/DeviceFramePreview.tsx,
    frontend/src/components/features/settings/DeviceFramePreview.test.tsx,
    frontend/src/components/features/settings/GradientColorPicker.tsx,
    frontend/src/components/features/settings/GradientColorPicker.test.tsx
  </files>
  <read_first>
    frontend/src/contexts/ProfileEditorContext.tsx,
    frontend/src/types/profileEditor.ts,
    frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx,
    frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts
  </read_first>
  <behavior>
    DeviceFramePreview:
    - Test 1: Renders 3 device toggle buttons (phone, tablet, desktop) with Smartphone/Tablet/Monitor icons
    - Test 2: Active device button has highlighted style (bg-blue-600 text-white)
    - Test 3: Clicking a device button dispatches SET_DEVICE_MODE
    - Test 4: Preview container scales using CSS transform based on DEVICE_BREAKPOINTS

    GradientColorPicker:
    - Test 1: Renders ColorPicker from react-best-gradient-color-picker
    - Test 2: onChange calls dispatch(SET_FIELD) with the color/gradient value
    - Test 3: Shows current color as a preview swatch
  </behavior>
  <action>
1. Create `DeviceFramePreview.tsx`:
   - Props: `children: React.ReactNode` (the PublicProfileRenderer will be passed as children)
   - Read deviceMode from useProfileEditor(), dispatch from useProfileEditorDispatch()
   - Toggle bar at top: 3 buttons with icons from lucide-react (Smartphone, Tablet, Monitor)
   - Active button: `bg-blue-600 text-white`, inactive: `bg-zinc-100 dark:bg-zinc-800 text-zinc-600`
   - Button click dispatches SET_DEVICE_MODE with the mode
   - Preview container:
     - Outer wrapper: `overflow-hidden bg-zinc-100 dark:bg-zinc-900 rounded-xl border border-zinc-200 dark:border-zinc-700 flex items-start justify-center p-4`
     - Inner frame: fixed width/height from DEVICE_BREAKPOINTS[deviceMode], CSS transform scale to fit container
     - Scale factor: calculate from container width / device width using useRef + ResizeObserver
     - Frame chrome: rounded corners (rounded-[2rem] for phone, rounded-xl for tablet/desktop), shadow-2xl
     - Phone: show a top notch bar (small dark pill centered at top, 30% width, 24px height, rounded-full)
   - Children rendered inside the scaled frame

2. Create `GradientColorPicker.tsx`:
   - Props: `value: string` (CSS color or gradient string), `onChange: (value: string) => void`, `label?: string`
   - Import ColorPicker from 'react-best-gradient-color-picker'
   - Render:
     - Label (if provided) with Tailwind `text-sm font-medium text-zinc-700 dark:text-zinc-300`
     - Current color preview swatch: 40px circle with the value as background
     - ColorPicker component with width=280, height=160
     - onChange calls props.onChange with the new color string
   - Wrapper: `space-y-3`

3. Create tests for both components:
   - DeviceFramePreview: mock context, verify buttons render, verify dispatch on click, verify container dimensions
   - GradientColorPicker: verify renders color picker, verify onChange callback fires
  </action>
  <verify>
    <automated>cd frontend && pnpm test src/components/features/settings/DeviceFramePreview.test.tsx src/components/features/settings/GradientColorPicker.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "DeviceFramePreview" frontend/src/components/features/settings/DeviceFramePreview.tsx
    - grep -q "Smartphone" frontend/src/components/features/settings/DeviceFramePreview.tsx
    - grep -q "SET_DEVICE_MODE" frontend/src/components/features/settings/DeviceFramePreview.tsx
    - grep -q "DEVICE_BREAKPOINTS" frontend/src/components/features/settings/DeviceFramePreview.tsx
    - grep -q "transform.*scale" frontend/src/components/features/settings/DeviceFramePreview.tsx
    - grep -q "GradientColorPicker" frontend/src/components/features/settings/GradientColorPicker.tsx
    - grep -q "react-best-gradient-color-picker" frontend/src/components/features/settings/GradientColorPicker.tsx
    - grep -q "ColorPicker" frontend/src/components/features/settings/GradientColorPicker.tsx
  </acceptance_criteria>
  <done>
    - DeviceFramePreview renders 3 device toggles, scales preview container via CSS transform, shows device chrome
    - GradientColorPicker wraps react-best-gradient-color-picker with swatch preview
    - Both components read/write via ProfileEditorContext
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm test src/components/features/settings/DndSectionList.test.tsx --run` passes
- `cd frontend && pnpm test src/components/features/settings/DeviceFramePreview.test.tsx --run` passes
- `cd frontend && pnpm test src/components/features/settings/GradientColorPicker.test.tsx --run` passes
- All three components export correctly and use ProfileEditorContext
</verification>

<success_criteria>
- DndSectionList provides drag-and-drop reordering with @dnd-kit, dispatches to context
- DeviceFramePreview scales preview in phone/tablet/desktop frames with device chrome
- GradientColorPicker wraps react-best-gradient-color-picker with swatch
- All components use ProfileEditorContext for state
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-editor-redesign/12-02-SUMMARY.md`
</output>
