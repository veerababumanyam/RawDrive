---
phase: 12-editor-redesign
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - frontend/src/components/settings/PersonalProfileTabContent.tsx
  - frontend/src/components/features/settings/CompanyProfileForm.tsx
  - frontend/src/components/settings/PersonalProfilePreview.tsx
  - frontend/src/components/features/profile/shared/SectionRegistry.ts
  - frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx
autonomous: false
requirements:
  - EDITR-01
  - EDITR-02
  - EDITR-03
  - EDITR-04
  - EDITR-05
  - EDITR-06

must_haves:
  truths:
    - "User edits a field in personal editor and preview updates instantly without save"
    - "User edits a field in company editor and preview updates instantly without save"
    - "User drags sections in either editor and reorder persists after reload"
    - "User picks a gradient color and preview updates immediately"
    - "User toggles device frame and preview rescales to match device"
    - "User stops typing and changes auto-save after 2 seconds"
    - "Editors look native to RawDrive (SettingsLayout shell, Tailwind, breadcrumbs)"
  artifacts:
    - path: "frontend/src/components/settings/PersonalProfileTabContent.tsx"
      provides: "Enhanced personal editor with live preview, DnD, color picker, device frames, auto-save"
      contains: "ProfileEditorProvider"
    - path: "frontend/src/components/features/settings/CompanyProfileForm.tsx"
      provides: "Enhanced company editor with live preview, DnD, color picker, device frames, auto-save"
      contains: "ProfileEditorProvider"
    - path: "frontend/src/components/features/profile/shared/SectionRegistry.ts"
      provides: "Updated getSectionsForProfile that respects sectionOrder parameter"
      contains: "sectionOrder"
  key_links:
    - from: "PersonalProfileTabContent.tsx"
      to: "ProfileEditorContext.tsx"
      via: "wraps content in ProfileEditorProvider"
      pattern: "ProfileEditorProvider"
    - from: "PersonalProfileTabContent.tsx"
      to: "PublicProfileRenderer.tsx"
      via: "renders in DeviceFramePreview for live preview"
      pattern: "PublicProfileRenderer"
    - from: "PersonalProfileTabContent.tsx"
      to: "useProfileAutoSave.ts"
      via: "auto-save on dirty state"
      pattern: "useProfileAutoSave"
    - from: "SectionRegistry.ts"
      to: "getSectionsForProfile"
      via: "accepts sectionOrder param to override default ordering"
      pattern: "sectionOrder"
---

<objective>
Wire all Plan 01 + 02 components into both profile editors: PersonalProfileTabContent and
CompanyProfileForm. Replace current preview with live preview via PublicProfileRenderer inside
DeviceFramePreview, add DnD sidebar, color picker, and auto-save. Update SectionRegistry to
respect custom section ordering.

Purpose: This is the integration plan that delivers the complete editor experience to users.
Output: Both editors fully functional with all EDITR requirements met.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-editor-redesign/12-CONTEXT.md
@.planning/phases/12-editor-redesign/12-01-SUMMARY.md
@.planning/phases/12-editor-redesign/12-02-SUMMARY.md

@frontend/src/contexts/ProfileEditorContext.tsx
@frontend/src/hooks/useProfileAutoSave.ts
@frontend/src/components/features/settings/DndSectionList.tsx
@frontend/src/components/features/settings/DeviceFramePreview.tsx
@frontend/src/components/features/settings/GradientColorPicker.tsx
@frontend/src/components/settings/PersonalProfileTabContent.tsx
@frontend/src/components/features/settings/CompanyProfileForm.tsx
@frontend/src/components/settings/PersonalProfilePreview.tsx
@frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx
@frontend/src/components/features/profile/shared/SectionRegistry.ts
</context>

<interfaces>
<!-- From Plan 01 -->
```typescript
// ProfileEditorContext exports
export function ProfileEditorProvider(props): JSX.Element;
export function useProfileEditor(): ExtendedEditorState;
export function useProfileEditorDispatch(): Dispatch;
// Actions: SET_FIELD, SET_THEME, SET_DEVICE_MODE, SET_SECTION_ORDER, RESET, MARK_SAVED

// useProfileAutoSave
export function useProfileAutoSave(opts): { isSaving, lastSavedAt, saveNow };
```

<!-- From Plan 02 -->
```typescript
export function DndSectionList(): JSX.Element; // reads from context
export function DeviceFramePreview(props: { children: ReactNode }): JSX.Element; // reads deviceMode from context
export function GradientColorPicker(props: { value, onChange, label? }): JSX.Element;
```

<!-- Existing -->
```typescript
// PublicProfileRenderer
export const PublicProfileRenderer: React.FC<{
  profileData: Record<string, unknown>;
  profileType: ProfileType;
  themeId?: string;
}>;

// SectionRegistry
export function getSectionsForProfile(type, profileData): SectionRegistryEntry[];
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update SectionRegistry and wire both editors</name>
  <files>
    frontend/src/components/features/profile/shared/SectionRegistry.ts,
    frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx,
    frontend/src/components/settings/PersonalProfileTabContent.tsx,
    frontend/src/components/features/settings/CompanyProfileForm.tsx,
    frontend/src/components/settings/PersonalProfilePreview.tsx
  </files>
  <read_first>
    frontend/src/components/features/profile/shared/SectionRegistry.ts,
    frontend/src/components/settings/PersonalProfileTabContent.tsx,
    frontend/src/components/features/settings/CompanyProfileForm.tsx,
    frontend/src/components/settings/PersonalProfilePreview.tsx,
    frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx,
    frontend/src/contexts/ProfileEditorContext.tsx,
    frontend/src/hooks/useProfileAutoSave.ts,
    frontend/src/components/features/settings/DndSectionList.tsx,
    frontend/src/components/features/settings/DeviceFramePreview.tsx,
    frontend/src/components/features/settings/GradientColorPicker.tsx
  </read_first>
  <action>
1. Update `SectionRegistry.ts`:
   - Modify `getSectionsForProfile` to accept optional `sectionOrder?: string[]` parameter
   - If sectionOrder provided, sort filtered sections by their index in sectionOrder array
   - Sections not in sectionOrder go to the end
   - Signature: `getSectionsForProfile(type, profileData, sectionOrder?)`

2. Update `PublicProfileRenderer.tsx`:
   - Add optional `sectionOrder?: string[]` prop
   - Pass sectionOrder to getSectionsForProfile call

3. Rewrite `PersonalProfileTabContent.tsx`:
   - Wrap entire component in `<ProfileEditorProvider>` with initialProfile from fetched data, profileType='personal', initialSectionOrder from API response
   - Split layout: left panel (form controls, ~60%), right panel (live preview, ~40%)
   - Left panel contains:
     - Existing form fields (keep all current fields: display_name, profile_title, slug, bio, location, phone, email, website, social_links, custom_links, secondary_contacts, embedded_media, seo_metadata)
     - Replace manual save button with auto-save status indicator showing "Saved", "Saving...", or "Unsaved changes" using useProfileAutoSave
     - Add `<DndSectionList />` in a collapsible "Section Order" panel (use existing collapsible pattern or Disclosure)
     - Add `<GradientColorPicker />` in the theme section, wired to dispatch SET_FIELD for theme accent/background colors
     - Each form field onChange dispatches SET_FIELD to context (replaces useState per field)
   - Right panel contains:
     - `<DeviceFramePreview>` wrapping `<PublicProfileRenderer profileData={editorState.profile} profileType="personal" themeId={editorState.theme?.theme_id} sectionOrder={editorState.sectionOrder} />`
   - Keep existing AvatarUploader, VisibilityToggle, ThemePreviewCard, ProfileStatusBar
   - Use Tailwind responsive: on mobile, preview stacks below form (lg:flex-row)
   - Auto-save integration: useProfileAutoSave reads isDirty from context, dispatches MARK_SAVED on success

4. Rewrite `CompanyProfileForm.tsx`:
   - Same pattern as PersonalProfileTabContent but for company profiles
   - Wrap in `<ProfileEditorProvider>` with profileType='company'
   - Keep all existing company-specific fields (company_name, tagline, logo, address_structured, etc.)
   - Add DndSectionList, GradientColorPicker, DeviceFramePreview with PublicProfileRenderer
   - Auto-save via useProfileAutoSave pointing to companyProfileService
   - Form fields dispatch to context instead of react-hook-form (or keep react-hook-form and sync to context via useEffect)

5. Delete or repurpose `PersonalProfilePreview.tsx`:
   - This file is no longer needed since preview is now PublicProfileRenderer inside DeviceFramePreview
   - Keep the file but mark it as deprecated with a comment, or replace its content to re-export the new preview setup as a thin wrapper if anything imports it
  </action>
  <verify>
    <automated>cd frontend && pnpm tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "sectionOrder" frontend/src/components/features/profile/shared/SectionRegistry.ts
    - grep -q "ProfileEditorProvider" frontend/src/components/settings/PersonalProfileTabContent.tsx
    - grep -q "useProfileAutoSave" frontend/src/components/settings/PersonalProfileTabContent.tsx
    - grep -q "DndSectionList" frontend/src/components/settings/PersonalProfileTabContent.tsx
    - grep -q "DeviceFramePreview" frontend/src/components/settings/PersonalProfileTabContent.tsx
    - grep -q "GradientColorPicker" frontend/src/components/settings/PersonalProfileTabContent.tsx
    - grep -q "PublicProfileRenderer" frontend/src/components/settings/PersonalProfileTabContent.tsx
    - grep -q "ProfileEditorProvider" frontend/src/components/features/settings/CompanyProfileForm.tsx
    - grep -q "useProfileAutoSave" frontend/src/components/features/settings/CompanyProfileForm.tsx
    - grep -q "DndSectionList" frontend/src/components/features/settings/CompanyProfileForm.tsx
    - grep -q "DeviceFramePreview" frontend/src/components/features/settings/CompanyProfileForm.tsx
  </acceptance_criteria>
  <done>
    - SectionRegistry.getSectionsForProfile accepts sectionOrder for custom ordering
    - PersonalProfileTabContent wraps in ProfileEditorProvider, has DnD sidebar, gradient picker, device frame preview with PublicProfileRenderer, auto-save
    - CompanyProfileForm same: wrapped, DnD, picker, frames, auto-save
    - TypeScript compiles without errors
    - Preview updates in real-time as fields change (same PublicProfileRenderer = pixel-perfect match)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of both editors</name>
  <files>
    frontend/src/components/settings/PersonalProfileTabContent.tsx,
    frontend/src/components/features/settings/CompanyProfileForm.tsx
  </files>
  <action>
    No code changes. Human visually verifies both editors work end-to-end:
    - Live preview updates in real-time
    - DnD section reordering persists
    - Color picker updates preview
    - Device frame toggle works
    - Auto-save fires after 2s idle
    - Editors match RawDrive design patterns
  </action>
  <verify>
    <automated>cd frontend && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <what-built>
    Complete editor redesign for both personal and company profiles with:
    - Live preview panel (right side) using PublicProfileRenderer
    - Device frame toggle (mobile/tablet/desktop)
    - Drag-and-drop section reordering with smooth animation
    - Visual gradient/solid color picker
    - Auto-save with status indicator (no manual save button)
    - Consistent RawDrive UI (SettingsLayout shell, Tailwind, breadcrumbs)
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:5173/workspace/profile (personal editor)
    2. Verify split layout: form left, preview right
    3. Edit display_name field -- preview should update INSTANTLY
    4. Toggle device frames (phone/tablet/desktop) -- preview should rescale with device chrome
    5. Open "Section Order" panel, drag a section to reorder -- preview should reflect new order
    6. Open color picker, select a gradient -- preview background should update immediately
    7. Stop typing, wait 2+ seconds -- status should show "Saving..." then "Saved"
    8. Reload page -- all changes (including section order) should persist
    9. Visit http://localhost:5173/workspace/branding (company editor)
    10. Repeat steps 2-8 for company profile
    11. Verify both editors feel consistent with RawDrive app (same shell, same Tailwind styling)
  </how-to-verify>
  <done>
    - Human confirms both personal and company editors work with live preview, DnD, color picker, device frames, auto-save
    - All EDITR-01 through EDITR-06 requirements visually verified
  </done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && pnpm tsc --noEmit` passes (no type errors)
- `cd frontend && pnpm test --run` passes (all existing + new tests)
- Both editors render with live preview, DnD, color picker, device frames, auto-save
- Section order persists to database and survives page reload
- Preview matches public page exactly (same PublicProfileRenderer component)
</verification>

<success_criteria>
- Personal editor: live preview, DnD, color picker, device frames, auto-save
- Company editor: live preview, DnD, color picker, device frames, auto-save
- SectionRegistry respects custom ordering
- All EDITR-01 through EDITR-06 requirements satisfied
- Editors consistent with RawDrive design patterns
- Human verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-editor-redesign/12-03-SUMMARY.md`
</output>
