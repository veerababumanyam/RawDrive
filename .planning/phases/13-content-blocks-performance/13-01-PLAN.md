---
phase: 13-content-blocks-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/app/api/personal_profile_schemas.py
  - backend/src/app/repositories/personal_profile_repository.py
  - backend/src/app/services/personal_profile_service.py
  - frontend/src/types/personalProfile.ts
  - frontend/src/components/features/profile/ProfileGalleryPreview.tsx
  - frontend/src/components/features/profile/ProfileSocials.tsx
  - frontend/src/components/features/profile/shared/sections/GalleryPreviewSection.tsx
  - frontend/src/components/features/profile/shared/sections/BookingCTASection.tsx
  - frontend/src/components/features/profile/shared/sections/TestimonialsSection.tsx
  - frontend/src/components/features/profile/shared/sections/SocialsSection.tsx
  - frontend/src/components/features/profile/shared/SectionRegistry.ts
  - frontend/src/components/features/profile/shared/__tests__/SectionRegistry.test.ts
autonomous: true
requirements:
  - CNTNT-01
  - CNTNT-02
  - CNTNT-03
  - CNTNT-04

must_haves:
  truths:
    - "Gallery preview section renders a 2x2 grid of cover images with View Gallery link"
    - "Booking CTA renders as full-width accent button with calendar icon linking to booking_calendar_url"
    - "Testimonials section renders quote cards with client name and star rating (max 3 visible)"
    - "Social links render with platform-specific icons including Threads and Bluesky with hover scale animation and platform colors"
    - "All 4 new sections are registered in SectionRegistry and appear on public profiles when data exists"
  artifacts:
    - path: "frontend/src/components/features/profile/shared/sections/GalleryPreviewSection.tsx"
      provides: "Gallery preview section wrapper"
      min_lines: 20
    - path: "frontend/src/components/features/profile/shared/sections/BookingCTASection.tsx"
      provides: "Booking CTA section wrapper"
      min_lines: 15
    - path: "frontend/src/components/features/profile/shared/sections/TestimonialsSection.tsx"
      provides: "Testimonials section wrapper"
      min_lines: 25
    - path: "frontend/src/components/features/profile/shared/SectionRegistry.ts"
      provides: "Registry with all 8 sections"
      contains: "gallery-preview"
  key_links:
    - from: "frontend/src/components/features/profile/shared/SectionRegistry.ts"
      to: "frontend/src/components/features/profile/shared/sections/GalleryPreviewSection.tsx"
      via: "import and registry entry"
      pattern: "GalleryPreviewSection"
    - from: "frontend/src/components/features/profile/shared/SectionRegistry.ts"
      to: "frontend/src/components/features/profile/shared/sections/BookingCTASection.tsx"
      via: "import and registry entry"
      pattern: "BookingCTASection"
    - from: "frontend/src/components/features/profile/shared/SectionRegistry.ts"
      to: "frontend/src/components/features/profile/shared/sections/TestimonialsSection.tsx"
      via: "import and registry entry"
      pattern: "TestimonialsSection"
    - from: "backend/src/app/api/personal_profile_schemas.py"
      to: "backend/src/app/repositories/personal_profile_repository.py"
      via: "testimonials JSONB field"
      pattern: "testimonials"
---

<objective>
Create 4 content block sections (gallery preview, booking CTA, testimonials, social links modernization) and register them in SectionRegistry so they render on public profiles.

Purpose: Adds rich, photography-specific content blocks that differentiate RawDrive from generic link-in-bio tools. Gallery preview is the moat feature; booking CTA drives conversions.
Output: 4 new section components registered in SectionRegistry, backend support for testimonials data, modernized social links with platform icons.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/components/features/profile/shared/SectionRegistry.ts:
```typescript
export type ProfileType = 'personal' | 'company';

export interface SectionProps {
  profileData: Record<string, unknown>;
  profileType: ProfileType;
}

export interface SectionRegistryEntry {
  id: string;
  component: ComponentType<SectionProps>;
  supportedTypes: ProfileType[];
  requiredData: string[];
  gridSpan?: { cols: number; rows: number };
  order: number;
}

export const SECTION_REGISTRY: SectionRegistryEntry[] = [
  { id: 'header', component: HeaderSection, supportedTypes: ['personal', 'company'], requiredData: ['display_name'], gridSpan: { cols: 4, rows: 1 }, order: 0 },
  { id: 'bio', component: BioSection, supportedTypes: ['personal', 'company'], requiredData: ['bio'], gridSpan: { cols: 4, rows: 1 }, order: 1 },
  { id: 'contact', component: ContactSection, supportedTypes: ['personal', 'company'], requiredData: [], gridSpan: { cols: 4, rows: 1 }, order: 2 },
  { id: 'socials', component: SocialsSection, supportedTypes: ['personal', 'company'], requiredData: ['social_links'], gridSpan: { cols: 4, rows: 1 }, order: 3 },
];

export function getSectionsForProfile(type: ProfileType, profileData: Record<string, unknown>, sectionOrder?: string[]): SectionRegistryEntry[]
```

From frontend/src/types/personalProfile.ts:
```typescript
export interface FeaturedGalleryInfo {
  gallery_id: string;
  name: string;
  cover_image_url?: string;
  asset_count?: number;
}
```

Existing section wrapper pattern (from SocialsSection.tsx):
```typescript
import { ProfileSocials } from '../../ProfileSocials';
import { getTheme } from '../../ProfileThemeEngine';
import type { SectionProps } from '../SectionRegistry';

export const SocialsSection: React.FC<SectionProps> = ({ profileData }) => {
  const socialLinks = (profileData.social_links as Record<string, string>) || {};
  if (Object.keys(socialLinks).length === 0) return null;
  const theme = getTheme();
  return <ProfileSocials theme={theme} socials={socialLinks} />;
};
```

Backend fields already exist:
- `featured_gallery_id: Optional[UUID]` on PersonalProfile
- `booking_calendar_url: Optional[str]` on PersonalProfile
- `featured_gallery: Optional[dict]` on public response schema
- No testimonials field exists yet (needs adding as JSONB)

From backend/src/app/api/personal_profile_schemas.py:
- PersonalProfileCreateRequest has `featured_gallery_id`, `booking_calendar_url`
- PersonalProfilePublicResponse has `featured_gallery: Optional[dict]`, `booking_calendar_url: Optional[str]`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend testimonials support + frontend types</name>
  <files>
    backend/src/app/api/personal_profile_schemas.py
    backend/src/app/repositories/personal_profile_repository.py
    backend/src/app/services/personal_profile_service.py
    frontend/src/types/personalProfile.ts
  </files>
  <read_first>
    backend/src/app/api/personal_profile_schemas.py
    backend/src/app/repositories/personal_profile_repository.py
    backend/src/app/services/personal_profile_service.py
    backend/src/app/services/visibility_service.py
    frontend/src/types/personalProfile.ts
  </read_first>
  <behavior>
    - Test 1: PersonalProfileCreateRequest accepts testimonials as list of {client_name: str, text: str, rating: int (1-5)}
    - Test 2: PersonalProfilePublicResponse includes testimonials field when visibility allows
    - Test 3: Testimonial type exported from personalProfile.ts with client_name, text, rating fields
  </behavior>
  <action>
    1. Add `testimonials` JSONB column to personal_profiles table via Alembic migration:
       - `docker exec rawdrive-backend alembic revision -m "add_testimonials_jsonb_to_personal_profiles"`
       - Column: `testimonials JSONB DEFAULT '[]'::jsonb`
       - Migration should ALTER TABLE personal_profiles ADD COLUMN IF NOT EXISTS

    2. Update backend/src/app/api/personal_profile_schemas.py:
       - Add Testimonial pydantic model: `class Testimonial(BaseModel): client_name: str; text: str; rating: int = Field(ge=1, le=5)`
       - Add `testimonials: Optional[list[Testimonial]] = None` to PersonalProfileCreateRequest
       - Add `testimonials: Optional[list[Testimonial]] = None` to PersonalProfileUpdateRequest
       - Add `testimonials: Optional[list[dict]] = None` to PersonalProfilePublicResponse
       - Add field_validator for testimonials: max 10 items, each text max 500 chars, client_name max 100 chars

    3. Update backend/src/app/repositories/personal_profile_repository.py:
       - Add `testimonials` to the list of fields in create/update methods (same pattern as booking_calendar_url)
       - Add to SELECT field lists and INSERT/UPDATE statements
       - Serialize as JSON for storage, deserialize on read

    4. Update backend/src/app/services/personal_profile_service.py:
       - Add `testimonials` to the field mapping in create/update flows
       - Add to the public response builder

    5. Update frontend/src/types/personalProfile.ts:
       - Add `export interface Testimonial { client_name: string; text: string; rating: number; }`
       - Add `testimonials?: Testimonial[]` to the public profile response interface
       - Add `testimonials` to PublicProfileData interface
  </action>
  <verify>
    <automated>docker exec rawdrive-backend python -c "from app.api.personal_profile_schemas import Testimonial; t = Testimonial(client_name='Test', text='Great work', rating=5); print('OK:', t.model_dump())"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class Testimonial" backend/src/app/api/personal_profile_schemas.py
    - grep -q "testimonials" backend/src/app/repositories/personal_profile_repository.py
    - grep -q "interface Testimonial" frontend/src/types/personalProfile.ts
    - Alembic migration file exists for testimonials column
  </acceptance_criteria>
  <done>Backend accepts and returns testimonials data. Frontend types defined. Migration ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Content block section components + SectionRegistry registration</name>
  <files>
    frontend/src/components/features/profile/ProfileGalleryPreview.tsx
    frontend/src/components/features/profile/ProfileSocials.tsx
    frontend/src/components/features/profile/shared/sections/GalleryPreviewSection.tsx
    frontend/src/components/features/profile/shared/sections/BookingCTASection.tsx
    frontend/src/components/features/profile/shared/sections/TestimonialsSection.tsx
    frontend/src/components/features/profile/shared/sections/SocialsSection.tsx
    frontend/src/components/features/profile/shared/SectionRegistry.ts
    frontend/src/components/features/profile/shared/__tests__/SectionRegistry.test.ts
  </files>
  <read_first>
    frontend/src/components/features/profile/ProfileGalleryPreview.tsx
    frontend/src/components/features/profile/ProfileSocials.tsx
    frontend/src/components/features/profile/ProfileContactGrid.tsx
    frontend/src/components/features/profile/shared/sections/SocialsSection.tsx
    frontend/src/components/features/profile/shared/sections/ContactSection.tsx
    frontend/src/components/features/profile/shared/SectionRegistry.ts
    frontend/src/components/features/profile/shared/__tests__/SectionRegistry.test.ts
  </read_first>
  <behavior>
    - Test 1: SectionRegistry contains entries for 'gallery-preview', 'booking-cta', 'testimonials' IDs
    - Test 2: getSectionsForProfile('personal', dataWithGallery) includes gallery-preview section
    - Test 3: getSectionsForProfile('personal', dataWithBookingUrl) includes booking-cta section
    - Test 4: getSectionsForProfile('personal', dataWithTestimonials) includes testimonials section
    - Test 5: Social icons map includes 'threads' and 'bluesky' entries
  </behavior>
  <action>
    1. **Modernize ProfileGalleryPreview.tsx** — Replace single cover image with 2x2 grid layout:
       - Accept `galleries: FeaturedGalleryInfo[]` (array, not single gallery)
       - Render up to 4 gallery cover images in a 2-column grid (`grid grid-cols-2 gap-2`)
       - Each cell shows cover_image_url with rounded corners and hover:scale-105 transition
       - If cover_image_url missing, show ImageIcon placeholder
       - Below grid: "View Gallery" link styled as subtle text link with ArrowRight icon
       - Use `motion.div` for fade-in animation (keep framer-motion for now, Plan 02 optimizes)
       - Remove old single-image layout entirely

    2. **Modernize ProfileSocials.tsx** — Add platform icons for Threads and Bluesky, add platform colors on hover:
       - Add to SOCIAL_ICONS map: `threads` (use AtSign icon from lucide), `bluesky` (use Cloud icon from lucide)
       - Add SOCIAL_COLORS map: `{ instagram: '#E4405F', facebook: '#1877F2', twitter: '#1DA1F2', linkedin: '#0A66C2', youtube: '#FF0000', tiktok: '#000000', pinterest: '#E60023', threads: '#000000', bluesky: '#0085FF', spotify: '#1DB954', whatsapp: '#25D366' }`
       - On hover, apply platform color as background with white icon (use inline style + onMouseEnter/Leave state, or CSS hover with custom property)
       - Add scale(1.15) on hover animation (already has scale(1.1), bump to 1.15)
       - Keep icon-only layout (no labels)

    3. **Create GalleryPreviewSection.tsx** — Section wrapper following existing pattern:
       - Import ProfileGalleryPreview from '../../ProfileGalleryPreview'
       - Extract `featured_gallery` from profileData (cast as FeaturedGalleryInfo or null)
       - If featured_gallery exists, wrap in array and pass to ProfileGalleryPreview
       - Also check for `galleries` array in profileData for multiple gallery preview
       - Use getTheme() for theme prop

    4. **Create BookingCTASection.tsx** — New standalone section (NOT inside ContactSection):
       - Extract `booking_calendar_url` (or `booking_url`) from profileData
       - Render full-width accent-colored button: `w-full py-4 px-6 text-center font-semibold text-lg`
       - Calendar icon (from lucide-react) on left side of button text "Book a Session"
       - Background: use brand_color from profileData if available, else theme primary button color
       - Link opens booking_calendar_url in new tab
       - Use motion.a with whileHover={{ scale: 1.02 }} and whileTap={{ scale: 0.98 }}
       - Apply theme radius and shadow classes

    5. **Create TestimonialsSection.tsx** — New section component:
       - Extract `testimonials` from profileData (cast as Testimonial[])
       - Show max 3 testimonials (slice array)
       - Each testimonial is a quote card:
         - Opening quote mark (text-4xl opacity-20 font-serif)
         - Text in italic (text-sm md:text-base, max 3 lines with line-clamp-3)
         - Client name in bold (text-sm font-semibold)
         - Star rating: render filled stars using Star icon from lucide-react (yellow-400 fill)
       - Card styling: theme surface bg, border, radius, shadow, padding p-5
       - Layout: flex flex-col gap-4 (stack on mobile), md:grid md:grid-cols-3 md:gap-4 for 3 cards side by side on desktop
       - Motion: stagger fade-in with 0.1s delay per card

    6. **Update SocialsSection.tsx** — Already exists, just verify it still works with the modernized ProfileSocials

    7. **Update SectionRegistry.ts** — Add 3 new entries after existing 4:
       - Import GalleryPreviewSection, BookingCTASection, TestimonialsSection
       - Add entry: `{ id: 'gallery-preview', component: GalleryPreviewSection, supportedTypes: ['personal'], requiredData: ['featured_gallery'], gridSpan: { cols: 4, rows: 1 }, order: 4 }`
       - Add entry: `{ id: 'booking-cta', component: BookingCTASection, supportedTypes: ['personal'], requiredData: ['booking_calendar_url'], gridSpan: { cols: 4, rows: 1 }, order: 5 }`
       - Add entry: `{ id: 'testimonials', component: TestimonialsSection, supportedTypes: ['personal'], requiredData: ['testimonials'], gridSpan: { cols: 4, rows: 1 }, order: 6 }`
       - NOTE: booking_calendar_url might be stored as booking_url in profileData — check ContactSection.tsx which uses `booking_url`. Use the same key.

    8. **Update SectionRegistry.test.ts** — Add tests:
       - Add gallery-preview, booking-cta, testimonials to fullPersonalData with appropriate mock values
       - Test that getSectionsForProfile returns new sections when data present
       - Test that sections are omitted when data absent
  </action>
  <verify>
    <automated>cd frontend && pnpm test src/components/features/profile/shared/__tests__/SectionRegistry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "gallery-preview" frontend/src/components/features/profile/shared/SectionRegistry.ts
    - grep -q "booking-cta" frontend/src/components/features/profile/shared/SectionRegistry.ts
    - grep -q "testimonials" frontend/src/components/features/profile/shared/SectionRegistry.ts
    - grep -q "threads" frontend/src/components/features/profile/ProfileSocials.tsx
    - grep -q "bluesky" frontend/src/components/features/profile/ProfileSocials.tsx
    - grep -q "grid-cols-2" frontend/src/components/features/profile/ProfileGalleryPreview.tsx
    - test -f frontend/src/components/features/profile/shared/sections/GalleryPreviewSection.tsx
    - test -f frontend/src/components/features/profile/shared/sections/BookingCTASection.tsx
    - test -f frontend/src/components/features/profile/shared/sections/TestimonialsSection.tsx
    - SectionRegistry.test.ts passes with all new sections tested
  </acceptance_criteria>
  <done>4 content block sections created and registered. Gallery preview shows 2x2 grid. Booking CTA is prominent full-width button. Testimonials show quote cards with stars. Social links have Threads/Bluesky icons with platform colors on hover. All registered in SectionRegistry and tested.</done>
</task>

</tasks>

<verification>
- SectionRegistry contains 7 entries (4 original + 3 new)
- Each new section renders when appropriate data is present in profileData
- Social links show platform-colored hover states
- Backend accepts and returns testimonials via API
- All tests pass: `cd frontend && pnpm test src/components/features/profile/shared/__tests__/SectionRegistry.test.ts`
</verification>

<success_criteria>
- Gallery preview renders 2x2 image grid with "View Gallery" link (CNTNT-01)
- Booking CTA is full-width accent button with calendar icon (CNTNT-02)
- Testimonials show as quote cards with name + star rating, max 3 (CNTNT-03)
- Social links have platform icons for Threads/Bluesky + hover colors + scale animation (CNTNT-04)
- All sections registered in SectionRegistry and conditionally shown based on data
</success_criteria>

<output>
After completion, create `.planning/phases/13-content-blocks-performance/13-01-SUMMARY.md`
</output>
