---
phase: 14-faceid-deep-dive-and-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/app/api/face_schemas.py
  - backend/src/app/api/v1/faces.py
  - backend/src/app/api/v1/face_groups.py
  - backend/src/app/services/face_detection_service.py
  - backend/src/app/services/ai/face_embedder.py
  - backend/src/app/models/face_group.py
  - backend/reproduce_500.py
  - backend/tests/test_face_critical_fixes.py
autonomous: true
requirements:
  - FACE-01
  - FACE-04

must_haves:
  truths:
    - "GET /api/v1/face-groups returns valid JSON without 500 errors"
    - "Face embedding vectors are never exposed in API responses"
    - "Biometric consent cannot be bypassed in production"
    - "ONNX model integrity is validated on load via SHA-256 hash"
    - "All face_groups have a representative_face_id with valid thumbnail_url"
  artifacts:
    - path: "backend/src/app/api/face_schemas.py"
      provides: "FaceGroupResponse without duplicate id, FaceResponse without embedding"
      contains: "exclude.*embedding"
    - path: "backend/src/app/services/face_detection_service.py"
      provides: "Consent bypass removed or dev-only gated"
      contains: "RAWDRIVE_ENV"
    - path: "backend/src/app/services/ai/face_embedder.py"
      provides: "Actual SHA-256 hash for model validation"
      contains: "EXPECTED_MODEL_HASH ="
    - path: "backend/tests/test_face_critical_fixes.py"
      provides: "Regression tests for all critical fixes"
      exports: ["test_face_group_response_no_500", "test_embedding_not_in_response", "test_consent_bypass_blocked"]
  key_links:
    - from: "backend/src/app/api/face_schemas.py"
      to: "backend/src/app/api/v1/face_groups.py"
      via: "FaceGroupResponse used in endpoint return"
      pattern: "FaceGroupResponse"
    - from: "backend/src/app/services/face_detection_service.py"
      to: "backend/src/app/api/v1/faces.py"
      via: "consent check in detection trigger"
      pattern: "check_consent|consent"
---

<objective>
Fix all critical backend bugs and security vulnerabilities in the face identification system.

Purpose: The face system has 500 errors on FaceGroupResponse (Pydantic duplicate id), exposes raw embedding vectors in API responses (privacy risk), allows bypassing GDPR biometric consent via env var, and has no ONNX model integrity validation. These must be fixed before any other work.

Output: Zero 500 errors on face group endpoints, embeddings stripped from API responses, consent bypass removed, model hash validated, representative faces fixed, regression tests covering all fixes.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-faceid-deep-dive-and-enhancement/14-CONTEXT.md
@.planning/phases/14-faceid-deep-dive-and-enhancement/BACKEND-ANALYSIS.md
@.planning/phases/14-faceid-deep-dive-and-enhancement/AI-SERVICE-ANALYSIS.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From backend/src/app/api/face_schemas.py (lines ~332, ~399):
```python
class FaceResponse(FaceBase):
    embedding: Optional[list[float]] = Field(None, description="Face embedding vector (512 dimensions)")
    # BUG: embedding exposed to API consumers

class FaceGroupResponse(FaceGroupBase):
    # BUG: duplicate `id` parameter causes Pydantic validation 500
    # Has centroid embedding field exposed
```

From backend/src/app/services/face_detection_service.py (line 67):
```python
BYPASS_CONSENT_CHECKS = os.getenv("RAWDRIVE_BYPASS_BIOMETRIC_CONSENT", "false").lower() == "true"
# SECURITY: Must be removed or restricted to dev-only
```

From backend/src/app/services/ai/face_embedder.py (line 36):
```python
EXPECTED_MODEL_HASH = None  # Set to actual hash if available
# SECURITY: Model integrity not validated
```

From backend/src/app/models/face_group.py (line 65):
```python
representative_face_id: Optional[UUID] = Field(...)
# Some groups have None here, causing missing avatars
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix FaceGroupResponse 500 errors and strip embeddings from API responses</name>
  <files>backend/src/app/api/face_schemas.py, backend/src/app/api/v1/face_groups.py, backend/src/app/api/v1/faces.py, backend/tests/test_face_critical_fixes.py</files>
  <read_first>
    - backend/src/app/api/face_schemas.py (current schema with duplicate id bug and exposed embeddings)
    - backend/src/app/api/v1/face_groups.py (endpoint using FaceGroupResponse)
    - backend/src/app/api/v1/faces.py (endpoints returning FaceResponse with embeddings)
    - backend/reproduce_500.py (reproduction script showing exact error)
    - backend/src/app/models/face_group.py (FaceGroup model with representative_face_id)
  </read_first>
  <behavior>
    - Test 1: FaceGroupResponse.model_validate(face_group_dict) succeeds without Pydantic ValidationError (no duplicate id)
    - Test 2: FaceResponse serialization output does NOT contain "embedding" key
    - Test 3: FaceGroupResponse serialization output does NOT contain "centroid" key
    - Test 4: GET /face-groups/{id} response body has no "embedding" or "centroid" fields
    - Test 5: FaceResponse still works for internal similarity search (embedding accessible via separate internal schema)
  </behavior>
  <action>
    1. In `backend/src/app/api/face_schemas.py`:
       - Fix FaceGroupResponse: remove duplicate `id` field that conflicts with FaceGroupBase.id inherited field. The Pydantic error is caused by `id` being passed both as a positional arg and in **dict unpacking. Remove the redundant `id` field declaration in FaceGroupResponse.
       - Create `FaceResponsePublic` schema that excludes `embedding` field using `model_config = ConfigDict(json_schema_extra=...)` or by simply not including the embedding field. This is the schema used in API responses.
       - Create `FaceGroupResponsePublic` schema that excludes `centroid` field. This is the schema used in API responses.
       - Keep `FaceResponse` and `FaceGroupResponse` with embeddings for internal service-to-service use only.

    2. In `backend/src/app/api/v1/face_groups.py`:
       - Change all endpoint response_model annotations from `FaceGroupResponse` to `FaceGroupResponsePublic`
       - Ensure list endpoints also use `list[FaceGroupResponsePublic]` or paginated wrapper with FaceGroupResponsePublic

    3. In `backend/src/app/api/v1/faces.py`:
       - Change all endpoint response_model annotations from `FaceResponse` to `FaceResponsePublic`
       - Keep internal endpoints (if any, like similarity search) using full FaceResponse

    4. Create `backend/tests/test_face_critical_fixes.py`:
       - Test FaceGroupResponse validates without error (regression for 500)
       - Test FaceResponsePublic excludes embedding
       - Test FaceGroupResponsePublic excludes centroid
       - Test endpoint integration returns correct schema
  </action>
  <verify>
    <automated>cd /c/Users/admin/Desktop/RawDrive2 && docker exec rawdrive-backend pytest tests/test_face_critical_fixes.py -x -v 2>/dev/null || python -m pytest backend/tests/test_face_critical_fixes.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class FaceResponsePublic" backend/src/app/api/face_schemas.py returns 0
    - grep -q "class FaceGroupResponsePublic" backend/src/app/api/face_schemas.py returns 0
    - grep -c "embedding" on FaceResponsePublic class body returns 0 (no embedding field)
    - grep -q "FaceGroupResponsePublic" backend/src/app/api/v1/face_groups.py returns 0
    - grep -q "FaceResponsePublic" backend/src/app/api/v1/faces.py returns 0
    - backend/tests/test_face_critical_fixes.py exists with at least 4 test functions
  </acceptance_criteria>
  <done>FaceGroupResponse no longer causes 500 errors. Embedding vectors never appear in API responses. Internal schemas still available for similarity search.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove consent bypass, set model hash, fix missing representative faces</name>
  <files>backend/src/app/services/face_detection_service.py, backend/src/app/services/ai/face_embedder.py, backend/src/app/services/face_cluster_service.py, backend/tests/test_face_critical_fixes.py</files>
  <read_first>
    - backend/src/app/services/face_detection_service.py (BYPASS_CONSENT_CHECKS on line 67, consent check on line 741)
    - backend/src/app/services/ai/face_embedder.py (EXPECTED_MODEL_HASH on line 36, hash validation on lines 301-305)
    - backend/src/app/services/face_cluster_service.py (clustering logic that assigns representative_face_id)
    - backend/src/app/repositories/face_group_repository.py (face group queries)
    - backend/scripts/fix_face_avatars.py (existing fix script showing the missing avatar pattern)
  </read_first>
  <behavior>
    - Test 1: BYPASS_CONSENT_CHECKS is False when RAWDRIVE_ENV is "production" regardless of RAWDRIVE_BYPASS_BIOMETRIC_CONSENT value
    - Test 2: BYPASS_CONSENT_CHECKS can be True only when RAWDRIVE_ENV is "development" or "test"
    - Test 3: face_embedder validates model hash on load and raises ValueError if mismatch
    - Test 4: After clustering, every FaceGroup has a non-null representative_face_id set to highest-confidence face
  </behavior>
  <action>
    1. In `backend/src/app/services/face_detection_service.py`:
       - Replace line 67 with environment-gated bypass:
         ```python
         _ENV = os.getenv("RAWDRIVE_ENV", "production").lower()
         BYPASS_CONSENT_CHECKS = (
             _ENV in ("development", "test")
             and os.getenv("RAWDRIVE_BYPASS_BIOMETRIC_CONSENT", "false").lower() == "true"
         )
         ```
       - Add a startup warning log if BYPASS_CONSENT_CHECKS is True: `logger.warning("BIOMETRIC CONSENT BYPASS ENABLED - dev/test only")`

    2. In `backend/src/app/services/ai/face_embedder.py`:
       - Compute the actual SHA-256 hash of the w600k_r50.onnx model file. If the model file is available in the Docker container, run: `docker exec rawdrive-backend python -c "import hashlib; print(hashlib.sha256(open('/app/models/w600k_r50.onnx','rb').read()).hexdigest())"` and set that as EXPECTED_MODEL_HASH.
       - If the model is not available at that path, check `backend/src/app/services/ai/` for model download logic to find the actual path. Set the hash to the computed value.
       - If the hash cannot be computed at plan time, set `EXPECTED_MODEL_HASH = os.getenv("FACE_MODEL_SHA256", None)` so it can be configured via env var, and add a `logger.warning("ONNX model hash not configured - integrity validation skipped")` when None.

    3. In `backend/src/app/services/face_cluster_service.py` (or the service that creates face groups):
       - After any clustering operation that creates/updates a FaceGroup, ensure `representative_face_id` is set to the face with the highest confidence score in the group.
       - Add a method `ensure_representative_face(group_id)` that:
         a. Queries all faces in the group ordered by confidence DESC
         b. Sets representative_face_id to the first face's id
         c. Verifies the face has a valid thumbnail_url
         d. If no face has thumbnail_url, triggers thumbnail generation for the representative face

    4. Append tests to `backend/tests/test_face_critical_fixes.py`:
       - test_consent_bypass_blocked_in_production
       - test_consent_bypass_allowed_in_dev
       - test_model_hash_validation_raises_on_mismatch
       - test_representative_face_always_set_after_clustering
  </action>
  <verify>
    <automated>cd /c/Users/admin/Desktop/RawDrive2 && docker exec rawdrive-backend pytest tests/test_face_critical_fixes.py -x -v 2>/dev/null || python -m pytest backend/tests/test_face_critical_fixes.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'RAWDRIVE_ENV' backend/src/app/services/face_detection_service.py returns 0
    - grep -q '_ENV in ("development", "test")' backend/src/app/services/face_detection_service.py returns 0
    - grep -q 'FACE_MODEL_SHA256' backend/src/app/services/ai/face_embedder.py returns 0 (or actual hash string present)
    - grep -q 'ensure_representative_face' backend/src/app/services/face_cluster_service.py returns 0
    - grep -c 'def test_' backend/tests/test_face_critical_fixes.py returns at least 8
  </acceptance_criteria>
  <done>Consent bypass impossible in production. Model integrity validated. Every face group has a representative face with thumbnail after clustering.</done>
</task>

</tasks>

<verification>
- All existing face-related tests still pass: `docker exec rawdrive-backend pytest tests/ -k "face" --tb=short`
- No 500 errors on GET /api/v1/face-groups endpoints
- API responses contain no `embedding` or `centroid` fields
- BYPASS_CONSENT_CHECKS evaluates to False when RAWDRIVE_ENV=production
</verification>

<success_criteria>
- Zero Pydantic validation errors on face group API endpoints
- Embedding vectors completely absent from all public API responses
- Biometric consent cannot be bypassed in production environment
- ONNX model hash validation active (or configurable via env)
- All face groups have representative faces after clustering
- 8+ regression tests covering all fixes
</success_criteria>

<output>
After completion, create `.planning/phases/14-faceid-deep-dive-and-enhancement/14-01-SUMMARY.md`
</output>
