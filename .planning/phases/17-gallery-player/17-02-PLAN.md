---
phase: 17-gallery-player
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - frontend/src/components/features/gallery/player/PlayerExifPanel.tsx
  - frontend/src/components/features/gallery/player/PlayerExifPanel.test.tsx
  - frontend/src/components/features/gallery/player/GalleryPlayer.tsx
  - frontend/src/components/features/gallery/player/index.ts
  - frontend/src/pages/public/PublicGalleryContent.tsx
  - frontend/src/pages/public/PublicGalleryLightbox.tsx
autonomous: false
requirements:
  - PLYR-02
  - PLYR-01
  - PLYR-03
  - PLYR-04

must_haves:
  truths:
    - "Visitor taps info icon and sees EXIF data (camera, lens, aperture, shutter speed, ISO) in a slide-up overlay with blur backdrop"
    - "Visitor clicks a photo in any gallery layout and the GalleryPlayer opens (not the old lightbox)"
    - "Visitor navigates photos via filmstrip, swipe, arrows, and keyboard in the wired player"
    - "Mobile visitor can pinch-to-zoom, swipe to navigate, double-tap to toggle 2x zoom in the wired player"
    - "EXIF panel shows formatted metadata from asset.metadata fields"
  artifacts:
    - path: "frontend/src/components/features/gallery/player/PlayerExifPanel.tsx"
      provides: "Slide-up EXIF overlay with blur backdrop"
      min_lines: 50
    - path: "frontend/src/pages/public/PublicGalleryContent.tsx"
      provides: "Wired GalleryPlayer replacing old PublicGalleryLightbox usage"
      contains: "GalleryPlayer"
  key_links:
    - from: "PublicGalleryContent.tsx"
      to: "GalleryPlayer"
      via: "Replaces PublicGalleryLightbox rendering"
      pattern: "GalleryPlayer"
    - from: "GalleryPlayer.tsx"
      to: "PlayerExifPanel"
      via: "Conditionally rendered when showExif is true"
      pattern: "PlayerExifPanel"
    - from: "PublicGalleryContent.tsx"
      to: "GalleryPlayerContext"
      via: "openPlayer(index) called on photo click instead of setLightboxAsset"
      pattern: "openPlayer"
---

<objective>
Build the EXIF metadata panel and wire GalleryPlayer into the live gallery page, replacing the old PublicGalleryLightbox.

Purpose: Complete the gallery player experience by adding EXIF metadata display and connecting the new player to the actual gallery page so visitors use it when clicking photos.

Output: EXIF panel component, GalleryPlayer wired into PublicGalleryContent, old lightbox usage removed, visual verification of full flow.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gallery-player/17-CONTEXT.md
@.planning/phases/17-gallery-player/17-01-SUMMARY.md

<interfaces>
<!-- Interfaces from Plan 01 that this plan depends on -->

From frontend/src/components/features/gallery/player/GalleryPlayer.tsx (created in Plan 01):
```typescript
// GalleryPlayer consumes useGalleryPlayer() and renders:
// - PlayerToolbar (top)
// - PlayerZoomContainer (center)
// - PlayerFilmstrip (bottom)
// - State: showExif boolean, toggled by toolbar info button and 'I' key
// Plan 02 adds: PlayerExifPanel rendered when showExif is true
```

From frontend/src/contexts/GalleryPlayerContext.tsx:
```typescript
export interface GalleryPlayerContextValue {
  isOpen: boolean;
  currentIndex: number;
  assets: PublicGalleryAsset[];
  openPlayer: (index: number) => void;
  closePlayer: () => void;
  goToNext: () => void;
  goToPrevious: () => void;
  currentAsset: PublicGalleryAsset | null;
}
```

From frontend/src/types/gallery.ts (metadata fields used for EXIF):
```typescript
// asset.metadata contains:
// make, model, lens, focal_length, aperture, shutter_speed, iso,
// date_taken, caption, ai_caption, ai_tags, tags
// asset.width, asset.height for resolution
```

From PublicGalleryContent.tsx current wiring (to be replaced):
```typescript
// Lines 130-131: const [lightboxAsset, setLightboxAsset] = useState(null);
// Line 159: setLightboxAsset(asset); setLightboxIndex(...)
// Line 243: {lightboxAsset && <PublicGalleryLightbox ... />}
// These are replaced with useGalleryPlayer().openPlayer(index)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build PlayerExifPanel and wire GalleryPlayer into PublicGalleryContent</name>
  <files>
    frontend/src/components/features/gallery/player/PlayerExifPanel.tsx
    frontend/src/components/features/gallery/player/PlayerExifPanel.test.tsx
    frontend/src/components/features/gallery/player/GalleryPlayer.tsx
    frontend/src/components/features/gallery/player/index.ts
    frontend/src/pages/public/PublicGalleryContent.tsx
  </files>
  <read_first>
    frontend/src/components/features/gallery/player/GalleryPlayer.tsx
    frontend/src/components/features/gallery/player/PlayerToolbar.tsx
    frontend/src/components/features/gallery/player/index.ts
    frontend/src/pages/public/PublicGalleryContent.tsx
    frontend/src/pages/public/PublicGalleryLightbox.tsx
    frontend/src/contexts/GalleryPlayerContext.tsx
    frontend/src/types/gallery.ts
  </read_first>
  <behavior>
    - Test 1: PlayerExifPanel renders nothing when asset has no metadata
    - Test 2: PlayerExifPanel renders camera make/model when present in metadata
    - Test 3: PlayerExifPanel renders aperture as "f/{value}" format
    - Test 4: PlayerExifPanel renders ISO, shutter speed, lens, focal length when present
    - Test 5: PlayerExifPanel renders resolution as "{width} x {height}"
    - Test 6: PlayerExifPanel has slide-up animation (framer motion animate prop)
    - Test 7: PlayerExifPanel renders caption and tags when present
  </behavior>
  <action>
    **Step 1: Create PlayerExifPanel.tsx**
    - Slide-up overlay panel positioned at bottom-right of player
    - Per decision: slide-up overlay with blur backdrop, toggled via info icon
    - Use Framer Motion `motion.div` with:
      - `initial={{ y: 20, opacity: 0 }}`
      - `animate={{ y: 0, opacity: 1 }}`
      - `exit={{ y: 20, opacity: 0 }}`
      - `transition={{ duration: 0.2, ease: 'easeOut' }}`
    - Styling: `bg-black/80 backdrop-blur-md rounded-lg p-4 text-white text-sm max-w-xs`
    - Position: `absolute right-4 bottom-24` (above filmstrip)
    - Props: `asset: PublicGalleryAsset`, `galleryId: string`
    - Display fields from `asset.metadata` in two-column layout (label left, value right):
      - Camera: `{metadata.make} {metadata.model}` (with Camera icon from lucide)
      - Lens: `{metadata.lens}`
      - Focal Length: `{metadata.focal_length}mm`
      - Aperture: `f/{metadata.aperture}`
      - Shutter: `{metadata.shutter_speed}`
      - ISO: `{metadata.iso}`
      - Resolution: `{asset.width} x {asset.height}`
      - Date Taken: formatted via `new Date(metadata.date_taken).toLocaleDateString()`
    - Only render rows where data exists (skip nullish fields)
    - If asset has no metadata at all, render nothing (return null)
    - Caption section: if `asset.caption || asset.ai_caption || asset.metadata?.caption` exists, show below metadata with separator
    - Tags section: if `asset.ai_tags || asset.metadata?.tags` exists, show as small pills (same pattern as existing lightbox)
    - Footer: "Press I to toggle" hint text
    - Wrap in `AnimatePresence` in the parent (GalleryPlayer.tsx)

    **Step 2: Update GalleryPlayer.tsx to include PlayerExifPanel**
    - Import PlayerExifPanel
    - Inside the player shell, conditionally render `<AnimatePresence>{showExif && currentAsset && <PlayerExifPanel asset={currentAsset} galleryId={currentAsset.gallery_id} />}</AnimatePresence>`
    - Ensure showExif resets to false on asset change (currentIndex change)
    - Pass `onToggleInfo` callback to PlayerToolbar that toggles showExif
    - Wire 'I' key to toggle showExif via useLightboxNavigation's `onToggleMetadata` callback

    **Step 3: Update player/index.ts barrel**
    - Add PlayerExifPanel export

    **Step 4: Wire GalleryPlayer into PublicGalleryContent.tsx**
    This is the critical wiring step. The goal: when a visitor clicks a photo in any layout, the new GalleryPlayer opens instead of the old PublicGalleryLightbox.

    In PublicGalleryContent.tsx:
    - Import `{ GalleryPlayer }` from the player barrel and `useGalleryPlayer` from context
    - Get `openPlayer` from `useGalleryPlayer()` hook (context is already provided by PublicGalleryShell)
    - Replace the photo click handler: instead of `setLightboxAsset(asset); setLightboxIndex(idx)`, call `openPlayer(idx)`
    - Remove the old lightbox rendering line: `{lightboxAsset && <PublicGalleryLightbox ... />}`
    - Add `<GalleryPlayer gallery={gallery} companyName={company_profile?.name} onDownload={handleDownload} isDownloading={isDownloading} />` -- this component reads its own state from GalleryPlayerContext
    - Remove unused state: `lightboxAsset`, `lightboxIndex`, `navigateLightbox` -- these are now managed by GalleryPlayerContext
    - Remove the `PublicGalleryLightbox` import (the file is preserved but no longer imported here)
    - The GalleryPlayer component needs to accept `gallery: GalleryDetailData` prop for download_policy, exif_visible checks. Update GalleryPlayer.tsx to accept and pass this through to PlayerToolbar and PlayerExifPanel.
    - Pass `companyName` prop for watermark display (preserve existing watermark logic from PublicGalleryLightbox)

    **Step 5: Write tests**
    Create `PlayerExifPanel.test.tsx` testing the 7 behaviors above.
  </action>
  <verify>
    <automated>cd frontend && pnpm test src/components/features/gallery/player/ --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - PlayerExifPanel renders slide-up overlay with blur backdrop showing EXIF fields
    - EXIF panel shows camera, lens, aperture (f/X), shutter, ISO, resolution, date when available
    - EXIF panel hidden when asset has no metadata
    - GalleryPlayer includes PlayerExifPanel toggled by info button and I key
    - PublicGalleryContent uses GalleryPlayer instead of PublicGalleryLightbox
    - Photo clicks in gallery layouts open GalleryPlayer via openPlayer(index)
    - Old lightbox state (lightboxAsset, lightboxIndex) removed from PublicGalleryContent
    - All player tests pass (Plan 01 tests + new ExifPanel tests)
  </acceptance_criteria>
  <done>
    EXIF panel displays metadata in slide-up overlay. GalleryPlayer is wired into the live gallery page. Clicking any photo opens the new player with zoom/pan/pinch, filmstrip, EXIF, and gesture navigation.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Gallery Player</name>
  <files>frontend/src/components/features/gallery/player/GalleryPlayer.tsx</files>
  <action>
    Start the dev server and visually verify all GalleryPlayer features work end-to-end in the browser. Follow the verification steps in how-to-verify. No code changes needed -- this is a human verification checkpoint.
  </action>
  <what-built>
    Complete GalleryPlayer with zoom/pan/pinch, swipe navigation, filmstrip thumbnails, EXIF overlay, keyboard navigation, and auto-hiding controls -- wired into the live public gallery page.
  </what-built>
  <how-to-verify>
    1. Start dev server: `cd frontend && pnpm dev`
    2. Navigate to a public gallery page (e.g., http://localhost:5173/g/{token})
    3. **Click any photo** -- verify fullscreen player opens with smooth animation and black 95% backdrop
    4. **Scroll wheel** on the photo -- verify zoom in/out (1x to 5x range)
    5. **Double-click** the photo -- verify it toggles to 2x zoom and back to 1x
    6. **Drag** while zoomed -- verify panning works smoothly
    7. **Click left/right arrows** (desktop) -- verify navigation between photos
    8. **Press arrow keys** -- verify keyboard navigation works
    9. **Press Escape** -- verify player closes
    10. **Reopen player, press I** -- verify EXIF panel slides up showing camera/lens/aperture/ISO data
    11. **Check filmstrip** at bottom -- verify thumbnails visible, current photo highlighted with border
    12. **Click a different filmstrip thumbnail** -- verify player jumps to that photo
    13. **On mobile/responsive** (resize browser to mobile width):
        - Swipe left/right -- verify photo navigation (50px threshold)
        - Pinch to zoom -- verify smooth zoom with momentum
        - Double-tap -- verify toggles 2x zoom
        - Swipe down -- verify player closes
    14. **Wait 3 seconds without moving mouse** -- verify controls auto-hide, then move mouse to reveal them
    15. **Press Escape or click backdrop** -- verify clean exit
  </how-to-verify>
  <verify>
    <automated>cd frontend && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>User has visually verified all GalleryPlayer features: zoom/pan/pinch, swipe navigation, filmstrip, EXIF panel, keyboard shortcuts, auto-hide controls, mobile gestures.</done>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && pnpm test src/components/features/gallery/player/ --run` passes all tests (Plan 01 + Plan 02)
- `cd frontend && pnpm lint` shows no errors
- `cd frontend && pnpm build` compiles without TypeScript errors
- Public gallery photo click opens GalleryPlayer (not old lightbox)
- EXIF panel shows metadata when available
- Filmstrip, zoom, swipe, keyboard all functional
</verification>

<success_criteria>
- EXIF panel displays camera, lens, aperture, shutter speed, ISO, resolution in slide-up overlay with blur backdrop
- GalleryPlayer replaces PublicGalleryLightbox in PublicGalleryContent
- Full player flow works: click photo -> fullscreen viewer -> zoom/pan/pinch -> swipe navigate -> filmstrip jump -> EXIF info -> close
- Mobile gestures: pinch-to-zoom, swipe navigate, double-tap zoom toggle, swipe down to close
- Visual verification approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/17-gallery-player/17-02-SUMMARY.md`
</output>
