---
phase: 19-downloads-delivery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/gallery-service/src/middleware/expiration.py
  - services/gallery-service/src/api/v1/public/galleries.py
  - services/notifications-service/src/workers/expiration_reminder_worker.py
  - services/notifications-service/src/services/task_queue_service.py
  - services/gallery-service/src/services/gallery_service.py
  - services/gallery-service/src/schemas/gallery.py
  - frontend/src/pages/public/ExpiredGalleryPage.tsx
  - services/gallery-service/tests/unit/test_expiration.py
  - services/notifications-service/tests/unit/test_expiration_reminder_worker.py
autonomous: true
requirements:
  - DWNL-03

must_haves:
  truths:
    - "Client visiting an expired gallery sees a clear 'Gallery Expired' page instead of gallery content"
    - "Photographer receives reminder emails at 7 days and 1 day before gallery expiration"
    - "Photographer receives notification when gallery has expired"
    - "Photographer can set expires_at date in gallery settings and it persists"
  artifacts:
    - path: "services/gallery-service/src/middleware/expiration.py"
      provides: "Middleware that checks gallery expiration before serving content"
      exports: ["check_gallery_expiration"]
    - path: "services/notifications-service/src/workers/expiration_reminder_worker.py"
      provides: "Scheduled worker scanning for upcoming expirations"
      exports: ["ExpirationReminderWorker"]
    - path: "frontend/src/pages/public/ExpiredGalleryPage.tsx"
      provides: "UI shown when gallery has expired"
      min_lines: 30
  key_links:
    - from: "services/gallery-service/src/api/v1/public/galleries.py"
      to: "services/gallery-service/src/middleware/expiration.py"
      via: "check_gallery_expiration called before returning gallery data"
      pattern: "check_gallery_expiration"
    - from: "services/notifications-service/src/workers/expiration_reminder_worker.py"
      to: "services/notifications-service/src/services/task_queue_service.py"
      via: "Enqueues email tasks for upcoming expirations"
      pattern: "task_queue.*enqueue|enqueue_task"
---

<objective>
Gallery expiration system: middleware blocking expired galleries, reminder email worker, expired gallery UI page, and gallery settings for expires_at.

Purpose: Enables photographers to set gallery expiration dates with automatic reminder emails, and shows clients a clear message when a gallery has expired.
Output: Expiration middleware, reminder worker, expired gallery page, gallery settings update, tests
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-downloads-delivery/19-CONTEXT.md

@services/gallery-service/src/api/v1/public/galleries.py
@services/gallery-service/src/services/gallery_service.py
@services/gallery-service/src/schemas/gallery.py
@services/gallery-service/src/config.py
@services/gallery-service/src/main.py
@services/gallery-service/src/services/postal_client.py
@services/notifications-service/src/workers/email_worker.py
@services/notifications-service/src/services/task_queue_service.py
@frontend/src/pages/public/PublicGalleryShell.tsx

<interfaces>
<!-- Key types from existing code -->

From services/gallery-service/src/schemas/gallery.py:
```python
class GalleryUpdateRequest(BaseModel):
    # ... existing fields
    # expires_at will be added here
```

From services/gallery-service/src/services/postal_client.py:
```python
class PostalClient:
    async def send_gallery_delivery(...)  # existing method pattern
    # New method will follow same pattern for expiration emails
```

From services/notifications-service/src/services/task_queue_service.py:
```python
class TaskQueueService:
    async def enqueue_task(task_type, payload, priority)
```

From services/notifications-service/src/workers/email_worker.py:
```python
# Redis-backed queue consumer with retry logic
# Pattern: poll queue -> render template -> send via SendGrid -> update status
```

From services/gallery-service/src/api/v1/public/galleries.py:
```python
# Public gallery access endpoints using magic link token validation
# Pattern: validate_magic_link(token) -> workspace_id, gallery_id
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expiration middleware + gallery settings update + expired gallery page</name>
  <files>
    services/gallery-service/src/middleware/expiration.py,
    services/gallery-service/src/api/v1/public/galleries.py,
    services/gallery-service/src/schemas/gallery.py,
    services/gallery-service/src/services/gallery_service.py,
    frontend/src/pages/public/ExpiredGalleryPage.tsx,
    frontend/src/pages/public/PublicGalleryShell.tsx,
    services/gallery-service/tests/unit/test_expiration.py
  </files>
  <behavior>
    - Test: check_gallery_expiration returns None (pass-through) when expires_at is NULL
    - Test: check_gallery_expiration returns None when expires_at is in the future
    - Test: check_gallery_expiration raises HTTPException 410 when expires_at is in the past
    - Test: GalleryUpdateRequest accepts expires_at as optional ISO datetime string
    - Test: update_gallery persists expires_at to database
  </behavior>
  <action>
    1. Create `services/gallery-service/src/middleware/expiration.py`:
       - `async def check_gallery_expiration(gallery_data: dict) -> None` — if gallery_data has expires_at and it is in the past (comparing to UTC now), raise HTTPException(status_code=410, detail={"error": "gallery_expired", "expired_at": expires_at_iso, "gallery_name": gallery_data.get("name"), "photographer_name": gallery_data.get("photographer_name")}).
       - This is a utility function called from the public gallery endpoint, not FastAPI middleware (simpler, testable).

    2. Integrate into `services/gallery-service/src/api/v1/public/galleries.py`:
       - After fetching gallery data via magic link validation, call `check_gallery_expiration(gallery_data)` before returning. This blocks all public access to expired galleries.
       - Add `expires_at` and `days_until_expiry` (computed) to the public gallery response so frontend can show countdown.

    3. Update `services/gallery-service/src/schemas/gallery.py`:
       - Add `expires_at: Optional[datetime] = None` to `GalleryUpdateRequest`.
       - Add `expires_at: Optional[datetime] = None` and `days_until_expiry: Optional[int] = None` to gallery response schemas.

    4. Update `services/gallery-service/src/services/gallery_service.py`:
       - Ensure `update_gallery` persists `expires_at` when provided.
       - Add `days_until_expiry` computation in gallery response builder.

    5. Create `frontend/src/pages/public/ExpiredGalleryPage.tsx`:
       - Simple, professional page: photographer logo (if available), gallery name, "This gallery has expired" message, "Contact the photographer for access" prompt, photographer contact info if public.
       - Tailwind styled, responsive, uses motion fade-in.
       - No download/interaction buttons.

    6. Update `frontend/src/pages/public/PublicGalleryShell.tsx`:
       - Catch 410 response from gallery API and render `ExpiredGalleryPage` instead of gallery content.
       - Pass expired_at, gallery_name, photographer_name from the 410 error detail.

    7. Write tests for expiration check logic.
  </action>
  <verify>
    <automated>docker exec rawdrive-gallery-service pytest tests/unit/test_expiration.py -x -v 2>/dev/null || cd services/gallery-service && python -m pytest tests/unit/test_expiration.py -x -v</automated>
  </verify>
  <done>
    - Expired galleries return 410 with structured error detail
    - Non-expired and no-expiration galleries pass through normally
    - Gallery settings accept and persist expires_at
    - Frontend shows ExpiredGalleryPage on 410 response
    - Days-until-expiry computed and returned in gallery response
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expiration reminder email worker</name>
  <files>
    services/notifications-service/src/workers/expiration_reminder_worker.py,
    services/notifications-service/src/services/task_queue_service.py,
    services/gallery-service/src/services/postal_client.py,
    services/notifications-service/tests/unit/test_expiration_reminder_worker.py
  </files>
  <behavior>
    - Test: worker finds galleries expiring in exactly 7 days and enqueues reminder email
    - Test: worker finds galleries expiring in exactly 1 day and enqueues urgent reminder email
    - Test: worker finds galleries expired today and enqueues expiration notice email
    - Test: worker does not re-send if reminder already sent (deduplication via Redis key)
    - Test: worker only processes galleries with non-null expires_at
  </behavior>
  <action>
    1. Create `services/notifications-service/src/workers/expiration_reminder_worker.py`:
       - `ExpirationReminderWorker` class following the pattern from `digest_worker.py` and `email_worker.py`.
       - `async def scan_expiring_galleries()`:
         a. Query galleries table (via direct DB connection, same pattern as other notification workers) for galleries WHERE expires_at IS NOT NULL AND workspace_id filtering.
         b. For each gallery, compute days_until = (expires_at - now).days
         c. If days_until == 7: enqueue "gallery_expiring_7d" email to gallery owner
         d. If days_until == 1: enqueue "gallery_expiring_1d" email to gallery owner
         e. If days_until == 0 (or < 0 and not yet notified): enqueue "gallery_expired" email
         f. Deduplication: check Redis key `expiration:notified:{gallery_id}:{milestone}` before sending. Set key with 8-day TTL for 7d, 2-day TTL for 1d, 30-day TTL for expired.
       - Schedule: runs once daily (configure via worker startup loop with 24h sleep or cron-style check).
       - Email payloads include: gallery_name, expires_at formatted, gallery_url (management URL for photographer), photo_count.

    2. Add email templates to postal_client or template_service for 3 email types:
       - `gallery_expiring_7d`: "Your gallery '{name}' expires in 7 days. Remind your clients to download their photos."
       - `gallery_expiring_1d`: "Your gallery '{name}' expires tomorrow! Clients will lose access after {date}."
       - `gallery_expired`: "Your gallery '{name}' has expired. Clients can no longer access it. You can extend the expiration in gallery settings."
       - Keep templates simple — plain text with HTML wrapper matching existing RawDrive email styling.

    3. Register the worker in notifications-service startup or as a separate entrypoint (follow existing worker registration pattern).

    4. Write unit tests mocking DB queries and Redis, verifying correct email enqueue for each milestone and deduplication.
  </action>
  <verify>
    <automated>docker exec rawdrive-notifications-service pytest tests/unit/test_expiration_reminder_worker.py -x -v 2>/dev/null || cd services/notifications-service && python -m pytest tests/unit/test_expiration_reminder_worker.py -x -v</automated>
  </verify>
  <done>
    - Worker scans for galleries expiring at 7d, 1d, and 0d milestones
    - Correct email type enqueued for each milestone
    - Deduplication prevents duplicate sends via Redis keys
    - Email payloads contain gallery_name, expires_at, gallery_url, photo_count
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- Gallery with expires_at in past returns 410 on public access
- Gallery with expires_at in future returns normal 200 with days_until_expiry field
- Gallery with no expires_at returns normal 200
- Reminder worker correctly identifies 7d/1d/expired galleries and enqueues emails
- Deduplication prevents re-sending for same milestone
</verification>

<success_criteria>
- Expired galleries blocked with professional 410 page
- Gallery settings accept and persist expires_at dates
- Frontend shows countdown and handles 410 gracefully
- Reminder emails sent at 7d and 1d before expiration, and on expiration
- Deduplication prevents duplicate emails
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-downloads-delivery/19-02-SUMMARY.md`
</output>
