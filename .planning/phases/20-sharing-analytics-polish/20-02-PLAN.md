---
phase: 20-sharing-analytics-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/public/components/BrandedPasswordPage.tsx
  - frontend/src/pages/public/components/GalleryMusicPlayer.tsx
  - frontend/src/pages/public/PublicGalleryShell.tsx
  - services/gallery-service/src/schemas/gallery.py
  - services/gallery-service/src/api/v1/galleries.py
  - services/gallery-service/src/services/gallery_service.py
  - frontend/src/pages/public/components/BrandedPasswordPage.test.tsx
  - frontend/src/pages/public/components/GalleryMusicPlayer.test.tsx
autonomous: true
requirements: [PROG-02, PROG-03]

must_haves:
  truths:
    - "Password-protected gallery shows branded entry page with photographer's logo, accent color, and welcome message"
    - "Photographer can upload background music audio file and visitors hear play/pause controls in gallery footer"
    - "Music autoplay is disabled; user must click play to start audio"
  artifacts:
    - path: "frontend/src/pages/public/components/BrandedPasswordPage.tsx"
      provides: "Custom-branded password entry page"
      min_lines: 60
    - path: "frontend/src/pages/public/components/GalleryMusicPlayer.tsx"
      provides: "Audio player with play/pause/volume controls"
      min_lines: 40
  key_links:
    - from: "frontend/src/pages/public/PublicGalleryShell.tsx"
      to: "frontend/src/pages/public/components/BrandedPasswordPage.tsx"
      via: "conditional render when gallery.is_password_protected and not authenticated"
      pattern: "BrandedPasswordPage"
    - from: "frontend/src/pages/public/PublicGalleryShell.tsx"
      to: "frontend/src/pages/public/components/GalleryMusicPlayer.tsx"
      via: "render in gallery footer when gallery.background_music_url exists"
      pattern: "GalleryMusicPlayer"
---

<objective>
Add branded password entry page for protected galleries and optional background music player for gallery slideshows.

Purpose: Password-protected galleries currently show a generic form. Photographers need branded entry with their logo and colors. Background music adds atmosphere for wedding/event gallery viewing.
Output: Polished password page + audio player in gallery footer.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-sharing-analytics-polish/20-CONTEXT.md

Key existing patterns:
- services/gallery-service/src/schemas/gallery.py — GalleryResponse, GalleryUpdateRequest with existing branding_profile_id
- services/gallery-service/src/api/v1/public/galleries.py — public gallery API with password validation
- services/gallery-service/src/utils/security.py — password hashing/verification
- frontend/src/pages/public/PublicGalleryShell.tsx — gallery page orchestrator
- Gallery already has password_protected flag and branding_profile_id for company profile logo/colors
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend gallery settings for music + branded password fields</name>
  <files>
    services/gallery-service/src/schemas/gallery.py,
    services/gallery-service/src/api/v1/galleries.py,
    services/gallery-service/src/services/gallery_service.py,
    backend/migrations/versions/0204_add_gallery_music_and_branding.py
  </files>
  <behavior>
    - GalleryUpdateRequest accepts: welcome_message (str, max 500), background_music_url (str, nullable)
    - GalleryResponse includes: welcome_message, background_music_url, branding_logo_url, branding_accent_color
    - Public gallery response includes same branding fields for password page rendering
    - PUT /api/v1/galleries/{id} persists welcome_message and background_music_url
  </behavior>
  <action>
    1. Add fields to gallery DB table via Alembic migration (0204):
       - `welcome_message TEXT NULL` — optional message shown on password page
       - `background_music_url VARCHAR(500) NULL` — R2 URL for uploaded audio file
       Note: branding_profile_id already exists and links to company profile with logo/accent_color

    2. Update `services/gallery-service/src/schemas/gallery.py`:
       - Add `welcome_message: Optional[str] = None` and `background_music_url: Optional[str] = None` to GalleryResponse
       - Add `welcome_message: Optional[str] = Field(None, max_length=500)` and `background_music_url: Optional[str] = None` to GalleryUpdateRequest
       - Add `branding_logo_url: Optional[str] = None` and `branding_accent_color: Optional[str] = None` to GalleryResponse (resolved from branding_profile)

    3. Update gallery_service.py to persist new fields on update and resolve branding fields from the linked company profile when returning gallery data.

    4. Ensure public gallery endpoint (`/api/v1/public/galleries/{token}`) includes welcome_message, background_music_url, and branding fields in the response so the frontend password page can render them without additional API calls.
  </action>
  <verify>
    <automated>docker exec rawdrive-backend alembic upgrade head && docker exec rawdrive-gallery-service pytest tests/ -x -v -k "gallery" --timeout=30</automated>
  </verify>
  <done>
    Gallery update accepts welcome_message and background_music_url; public gallery response includes branding fields for password page; migration applied successfully
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Branded password page + background music player UI</name>
  <files>
    frontend/src/pages/public/components/BrandedPasswordPage.tsx,
    frontend/src/pages/public/components/GalleryMusicPlayer.tsx,
    frontend/src/pages/public/PublicGalleryShell.tsx,
    frontend/src/pages/public/components/BrandedPasswordPage.test.tsx,
    frontend/src/pages/public/components/GalleryMusicPlayer.test.tsx
  </files>
  <behavior>
    - BrandedPasswordPage renders: photographer logo (from branding profile), gallery title, welcome_message, accent-colored password input + submit button, background uses accent color gradient
    - BrandedPasswordPage falls back to default RawDrive branding if no branding profile linked
    - GalleryMusicPlayer renders: fixed-bottom mini player bar with play/pause button, track name, volume slider, progress bar
    - GalleryMusicPlayer uses HTML5 Audio API, autoplay=false, user must click play
    - GalleryMusicPlayer hides when no background_music_url provided
  </behavior>
  <action>
    1. Create `frontend/src/pages/public/components/BrandedPasswordPage.tsx`:
       - Props: galleryTitle, welcomeMessage, brandingLogoUrl, brandingAccentColor, onPasswordSubmit(password: string)
       - Centered card layout with: logo (img or RawDrive default), gallery title (h1), welcome message (p), password input, submit button
       - Accent color applied via CSS custom properties: --brand-accent for button bg, input focus ring, gradient
       - Background: subtle gradient using accent color at low opacity
       - Responsive: full-screen on mobile, centered card on desktop
       - Error state: shake animation on wrong password with "Incorrect password" message
       - Use framer-motion for entrance animation (fade up)

    2. Create `frontend/src/pages/public/components/GalleryMusicPlayer.tsx`:
       - Props: musicUrl, trackName (optional, derived from filename)
       - Fixed bottom bar (above any existing footer), z-index below modals
       - Controls: play/pause button (Play/Pause lucide icons), volume slider (input range), progress bar (showing current time / duration)
       - Uses useRef for Audio element, useState for isPlaying/currentTime/duration/volume
       - Audio event listeners: timeupdate, loadedmetadata, ended
       - Minimize button to collapse to just play/pause icon
       - Respects dark/light theme via CSS custom properties
       - Does NOT autoplay — user must explicitly click play (Web Audio API best practices)

    3. Wire into PublicGalleryShell.tsx:
       - Before gallery content loads, if gallery is password-protected and not yet authenticated, render BrandedPasswordPage instead of gallery content
       - Pass branding fields from gallery API response to BrandedPasswordPage
       - After authentication, render GalleryMusicPlayer in footer if background_music_url exists
       - Music player should not interfere with GalleryPlayer (lightbox) — pause music when player opens

    4. Tests:
       - BrandedPasswordPage.test.tsx: renders logo, title, welcome message, calls onPasswordSubmit, shows error on empty submit
       - GalleryMusicPlayer.test.tsx: renders play button, does not autoplay, hides when no URL
  </action>
  <verify>
    <automated>cd frontend && pnpm test src/pages/public/components/BrandedPasswordPage.test.tsx src/pages/public/components/GalleryMusicPlayer.test.tsx --run</automated>
  </verify>
  <done>
    Password-protected gallery shows branded page with logo, colors, and message; background music player renders with play/pause/volume; autoplay disabled; tests pass
  </done>
</task>

</tasks>

<verification>
- Password-protected gallery shows branded entry with photographer logo and accent colors
- Wrong password shows error with shake animation
- Correct password reveals gallery with music player in footer (if music configured)
- Music plays only on user click, pauses when lightbox opens
- Gallery without branding profile shows clean default password page
</verification>

<success_criteria>
- Branded password page renders with logo, accent color, welcome message
- Music player plays/pauses/adjusts volume without autoplay
- All fields persist via gallery settings API
- Tests pass for both components
</success_criteria>

<output>
After completion, create `.planning/phases/20-sharing-analytics-polish/20-02-SUMMARY.md`
</output>
