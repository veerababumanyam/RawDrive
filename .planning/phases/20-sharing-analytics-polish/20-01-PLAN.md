---
phase: 20-sharing-analytics-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/gallery-service/src/api/v1/public/sharing.py
  - services/gallery-service/src/templates/gallery_shell.html
  - services/gallery-service/src/services/sharing_service.py
  - services/gallery-service/src/api/v1/public/__init__.py
  - frontend/src/components/features/gallery/sharing/SharePanel.tsx
  - frontend/src/components/features/gallery/sharing/QRCodeModal.tsx
  - frontend/src/components/features/gallery/sharing/EmbedCodeModal.tsx
  - frontend/src/pages/public/PublicGalleryShell.tsx
  - frontend/src/pages/public/components/GalleryThemeToggle.tsx
  - services/gallery-service/tests/unit/test_sharing_service.py
  - frontend/src/components/features/gallery/sharing/SharePanel.test.tsx
autonomous: true
requirements: [SHAR-01, SHAR-02, SHAR-03, SHAR-04]

must_haves:
  truths:
    - "Gallery link shared on social media shows cover photo, title, and photographer name in preview card"
    - "Photographer can generate and download a QR code for any gallery"
    - "Photographer can copy an iframe embed snippet and paste it into a website to render a gallery widget"
    - "Gallery visitor can toggle between dark and light mode with the preference persisting across page reloads"
  artifacts:
    - path: "services/gallery-service/src/api/v1/public/sharing.py"
      provides: "OG metadata HTML shell endpoint for crawlers"
      exports: ["router"]
    - path: "services/gallery-service/src/templates/gallery_shell.html"
      provides: "Jinja2 HTML template with OG meta tags for gallery pages"
      contains: "og:image"
    - path: "services/gallery-service/src/services/sharing_service.py"
      provides: "Service to build OG metadata and embed code from gallery data"
      exports: ["SharingService"]
    - path: "frontend/src/components/features/gallery/sharing/SharePanel.tsx"
      provides: "Share button with QR, embed, copy link options"
      min_lines: 50
    - path: "frontend/src/pages/public/components/GalleryThemeToggle.tsx"
      provides: "Dark/light mode toggle button with localStorage persistence"
      min_lines: 30
  key_links:
    - from: "services/gallery-service/src/api/v1/public/sharing.py"
      to: "services/gallery-service/src/templates/gallery_shell.html"
      via: "Jinja2 template rendering"
      pattern: "templates\\.get_template.*gallery_shell"
    - from: "frontend/src/pages/public/PublicGalleryShell.tsx"
      to: "frontend/src/pages/public/components/GalleryThemeToggle.tsx"
      via: "component import in gallery header"
      pattern: "GalleryThemeToggle"
---

<objective>
Add social sharing capabilities to galleries: OG meta tags for rich link previews, QR code generation, embeddable iframe widgets, and dark/light mode toggle.

Purpose: Galleries shared on social media currently show generic "RawDrive" text with no preview image. Photographers need rich previews, QR codes for event sharing, embed codes for websites, and theme control for visitors.
Output: 4 sharing features fully functional on public gallery pages.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-sharing-analytics-polish/20-CONTEXT.md

Key existing patterns:
- backend/src/app/templates/profile_shell.html — Jinja2 HTML shell with OG tags (Phase 11 pattern to replicate for galleries)
- backend/src/app/services/og_image_service.py — OG image generation service
- frontend/src/pages/public/PublicGalleryShell.tsx — gallery orchestrator where theme toggle goes
- qrcode.react ^4.2.0 already installed in frontend/package.json
- services/gallery-service/src/api/v1/public/galleries.py — existing public gallery API
- services/gallery-service/src/services/magic_link_service.py — magic link validation for public access
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gallery OG HTML shell endpoint + sharing service</name>
  <files>
    services/gallery-service/src/services/sharing_service.py,
    services/gallery-service/src/templates/gallery_shell.html,
    services/gallery-service/src/api/v1/public/sharing.py,
    services/gallery-service/src/api/v1/public/__init__.py,
    services/gallery-service/tests/unit/test_sharing_service.py
  </files>
  <behavior>
    - SharingService.build_og_metadata(gallery) returns dict with title, description, og_image, canonical_url
    - OG title format: "{gallery.title} by {photographer_name}"
    - OG description: "{photo_count} photos" or gallery.description if set
    - OG image: gallery cover_image_url or first asset thumbnail URL
    - GET /api/v1/public/galleries/{token}/page returns HTML with OG meta tags when User-Agent matches crawler (facebookexternalhit, Twitterbot, LinkedInBot, iMessageBot, WhatsApp, Slackbot, Discordbot)
    - GET /api/v1/public/galleries/{token}/page returns 302 redirect to SPA for normal browsers
    - GET /api/v1/public/galleries/{token}/embed returns embeddable HTML page (minimal chrome, no header/footer)
    - SharingService.generate_embed_code(gallery_url, width, height) returns iframe HTML string
  </behavior>
  <action>
    1. Create `services/gallery-service/src/services/sharing_service.py` with SharingService class:
       - `build_og_metadata(gallery, photographer_name, base_url)` — builds OG dict from gallery data
       - `generate_embed_code(gallery_url, width=800, height=600)` — returns `<iframe src="{url}/embed" width="{w}" height="{h}" ...>` string
       - `is_crawler(user_agent: str)` — checks against known crawler UA patterns

    2. Create `services/gallery-service/src/templates/gallery_shell.html` — Jinja2 template following the exact pattern from `backend/src/app/templates/profile_shell.html`:
       - og:type = "website" (not "profile")
       - og:title, og:description, og:image, og:url, og:site_name
       - twitter:card = "summary_large_image"
       - JSON-LD type: "ImageGallery" with photographer as author
       - Dark mode flash prevention script (same as profile_shell)

    3. Create `services/gallery-service/src/api/v1/public/sharing.py`:
       - `GET /api/v1/public/galleries/{token}/page` — validates magic link, if crawler UA return rendered HTML shell, else 302 redirect to SPA URL
       - `GET /api/v1/public/galleries/{token}/embed` — validates magic link, returns minimal gallery page suitable for iframe embedding (query params: layout, theme)
       - Add jinja2 to gallery-service requirements.txt if not present

    4. Register sharing router in `services/gallery-service/src/api/v1/public/__init__.py`

    5. Write tests in test_sharing_service.py:
       - Test OG metadata generation with/without cover image
       - Test crawler detection for each known UA
       - Test embed code generation with custom dimensions
  </action>
  <verify>
    <automated>docker exec rawdrive-gallery-service pytest tests/unit/test_sharing_service.py -x -v</automated>
  </verify>
  <done>
    Crawler user-agents hitting /page get HTML with OG tags; browsers get redirected to SPA; /embed returns minimal gallery page; all tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Share panel UI (QR, embed, copy link) + dark/light toggle</name>
  <files>
    frontend/src/components/features/gallery/sharing/SharePanel.tsx,
    frontend/src/components/features/gallery/sharing/QRCodeModal.tsx,
    frontend/src/components/features/gallery/sharing/EmbedCodeModal.tsx,
    frontend/src/pages/public/components/GalleryThemeToggle.tsx,
    frontend/src/pages/public/PublicGalleryShell.tsx,
    frontend/src/components/features/gallery/sharing/SharePanel.test.tsx
  </files>
  <behavior>
    - SharePanel renders share button that opens dropdown with: Copy Link, QR Code, Embed Code, native Web Share API (if available)
    - QRCodeModal shows QR code (using qrcode.react QRCodeSVG) with download-as-PNG button
    - EmbedCodeModal shows iframe code with width/height inputs and copy button
    - GalleryThemeToggle renders sun/moon icon button, toggles data-theme attribute on document.documentElement, persists to localStorage key "gallery-theme"
    - Copy Link uses navigator.clipboard.writeText with success toast
  </behavior>
  <action>
    1. Create `frontend/src/pages/public/components/GalleryThemeToggle.tsx`:
       - Toggle button with Sun/Moon icons (from lucide-react)
       - On mount, read localStorage "gallery-theme" (default to system preference via prefers-color-scheme)
       - On click, toggle between "dark"/"light", set document.documentElement.setAttribute("data-theme", theme), save to localStorage
       - CSS custom properties already exist from profile theme system — gallery pages should use same --bg-primary, --text-primary vars

    2. Create `frontend/src/components/features/gallery/sharing/QRCodeModal.tsx`:
       - Uses QRCodeSVG from qrcode.react (already installed)
       - Accepts galleryUrl prop
       - Renders QR code at 256x256
       - Download button: create canvas from SVG, toBlob, saveAs (use anchor download trick, no file-saver needed)
       - Customization: foreground color defaults to gallery accent color

    3. Create `frontend/src/components/features/gallery/sharing/EmbedCodeModal.tsx`:
       - Width/height number inputs (defaults 800x600)
       - Generates iframe HTML: `<iframe src="{galleryUrl}/embed" width="{w}" height="{h}" frameborder="0" allowfullscreen></iframe>`
       - Read-only textarea showing the code + Copy button
       - Preview toggle showing live iframe at small scale

    4. Create `frontend/src/components/features/gallery/sharing/SharePanel.tsx`:
       - Share button in gallery header toolbar
       - Dropdown menu with: Copy Link (clipboard API + toast), QR Code (opens QRCodeModal), Embed Code (opens EmbedCodeModal), Share (Web Share API if navigator.share available, hidden if not)
       - Uses sonner toast for copy confirmation

    5. Wire into PublicGalleryShell.tsx:
       - Add GalleryThemeToggle to gallery header (next to layout switcher)
       - Add SharePanel to gallery header toolbar (photographer-only, visible when authenticated)
       - Also add SharePanel to GalleryDetailPage toolbar for workspace view

    6. Write tests verifying SharePanel renders all options, QR modal renders QRCodeSVG, embed modal generates correct iframe HTML, theme toggle persists preference.
  </action>
  <verify>
    <automated>cd frontend && pnpm test src/components/features/gallery/sharing/SharePanel.test.tsx --run</automated>
  </verify>
  <done>
    Share panel shows copy/QR/embed/native-share options; QR code renders and downloads; embed code copies correctly; dark/light toggle switches theme and persists across reload
  </done>
</task>

</tasks>

<verification>
- curl the /page endpoint with Facebook crawler UA — HTML response contains og:image, og:title
- curl the /page endpoint with normal UA — 302 redirect
- Gallery share button shows all sharing options
- QR code scans correctly on mobile
- Embed iframe renders gallery
- Dark/light toggle switches theme and persists
</verification>

<success_criteria>
- Gallery links on social media show cover photo + title preview
- QR code generates and downloads for any gallery
- Embed code produces working iframe
- Dark/light toggle works with localStorage persistence
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-sharing-analytics-polish/20-01-SUMMARY.md`
</output>
