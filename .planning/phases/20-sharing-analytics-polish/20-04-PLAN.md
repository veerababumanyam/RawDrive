---
phase: 20-sharing-analytics-polish
plan: 04
type: execute
wave: 2
depends_on: ["20-03"]
files_modified:
  - frontend/src/components/features/gallery/analytics/GalleryAnalyticsTab.tsx
  - frontend/src/components/features/gallery/analytics/ViewsChart.tsx
  - frontend/src/components/features/gallery/analytics/DeviceBreakdown.tsx
  - frontend/src/components/features/gallery/analytics/GeoBreakdown.tsx
  - frontend/src/components/features/gallery/analytics/DownloadStats.tsx
  - frontend/src/hooks/useGalleryAnalytics.ts
  - frontend/src/pages/workspace/GalleryDetailPage.tsx
  - frontend/src/pages/workspace/GalleriesPage.tsx
  - frontend/src/pages/public/PublicGalleryShell.tsx
  - services/gallery-service/src/api/v1/galleries.py
  - services/gallery-service/src/schemas/gallery.py
  - frontend/src/components/features/gallery/analytics/GalleryAnalyticsTab.test.tsx
  - frontend/package.json
autonomous: true
requirements: [GANLT-01, GANLT-02, GANLT-03, GDISC-01, GDISC-02]

must_haves:
  truths:
    - "Gallery detail page has an Analytics tab showing views chart, visitor count, device breakdown, and geo breakdown"
    - "Download tracking section shows per-photo downloads, batch vs single, and total bandwidth"
    - "Device breakdown shows desktop/mobile/tablet percentages with visual chart"
    - "Geographic breakdown shows top countries with view counts"
    - "Gallery list page supports filtering by date range, status, client, and tags simultaneously"
    - "Gallery list can be sorted by engagement ranking (views, downloads, shares)"
    - "Visitor's browser sends view tracking beacon on public gallery load"
  artifacts:
    - path: "frontend/src/components/features/gallery/analytics/GalleryAnalyticsTab.tsx"
      provides: "Analytics dashboard tab for gallery detail page"
      min_lines: 80
    - path: "frontend/src/hooks/useGalleryAnalytics.ts"
      provides: "TanStack Query hook for fetching gallery analytics"
      exports: ["useGalleryAnalytics"]
    - path: "frontend/src/components/features/gallery/analytics/ViewsChart.tsx"
      provides: "Time series chart for views and visitors"
      min_lines: 40
  key_links:
    - from: "frontend/src/pages/workspace/GalleryDetailPage.tsx"
      to: "frontend/src/components/features/gallery/analytics/GalleryAnalyticsTab.tsx"
      via: "tab panel in gallery detail"
      pattern: "GalleryAnalyticsTab"
    - from: "frontend/src/hooks/useGalleryAnalytics.ts"
      to: "/api/v1/galleries/{id}/analytics"
      via: "TanStack Query fetch"
      pattern: "useQuery.*gallery.*analytics"
    - from: "frontend/src/pages/public/PublicGalleryShell.tsx"
      to: "/api/v1/public/galleries/{token}/view"
      via: "useEffect beacon on mount"
      pattern: "track.*view|view.*beacon"
---

<objective>
Build the gallery analytics dashboard UI and gallery discovery features: analytics tab with charts, view tracking beacon on public gallery, and filtering/ranking on gallery list page.

Purpose: Photographers need to see how galleries perform (views, devices, geography, downloads) and find their most engaging galleries quickly.
Output: Analytics tab on gallery detail + enhanced gallery list with filters and ranking.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-sharing-analytics-polish/20-CONTEXT.md
@.planning/phases/20-sharing-analytics-polish/20-03-SUMMARY.md

<interfaces>
<!-- From Plan 03 - analytics API response shape -->
```python
class GalleryAnalyticsResponse(BaseModel):
    gallery_id: UUID
    summary: GalleryAnalyticsSummary  # total_views, unique_visitors, avg_time_spent_seconds, views_today/week/month
    daily_stats: list[GalleryAnalyticsTimeSeries]  # date, views, unique_visitors
    device_breakdown: DeviceBreakdown  # desktop/mobile/tablet counts and percentages
    geo_breakdown: list[GeoEntry]  # country_code, country_name, count
    download_summary: dict  # total_downloads, total_bytes from gallery_downloads
    period_start: date
    period_end: date
```

From frontend/src/pages/workspace/GalleriesPage.tsx:
- Existing filters: searchQuery, sortBy (created_at|title|status|shoot_date), filterStatus (all|draft|published|archived)
- Uses useSearchParams for person filter
- TanStack Query for gallery list fetch
- Existing sort/filter UI with dropdown buttons

From frontend/src/pages/workspace/GalleryDetailPage.tsx:
- Tab-based layout with existing tabs for photos, settings, etc.
- Uses TanStack Query for gallery data
- ~1491 lines, add Analytics as new tab
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Analytics dashboard tab + view tracking beacon</name>
  <files>
    frontend/package.json,
    frontend/src/hooks/useGalleryAnalytics.ts,
    frontend/src/components/features/gallery/analytics/GalleryAnalyticsTab.tsx,
    frontend/src/components/features/gallery/analytics/ViewsChart.tsx,
    frontend/src/components/features/gallery/analytics/DeviceBreakdown.tsx,
    frontend/src/components/features/gallery/analytics/GeoBreakdown.tsx,
    frontend/src/components/features/gallery/analytics/DownloadStats.tsx,
    frontend/src/pages/workspace/GalleryDetailPage.tsx,
    frontend/src/pages/public/PublicGalleryShell.tsx,
    frontend/src/components/features/gallery/analytics/GalleryAnalyticsTab.test.tsx
  </files>
  <behavior>
    - useGalleryAnalytics(galleryId, period) fetches GET /api/v1/galleries/{id}/analytics?period={period}
    - GalleryAnalyticsTab shows: summary cards (total views, unique visitors, avg time), ViewsChart, DeviceBreakdown, GeoBreakdown, DownloadStats
    - ViewsChart renders time series line/area chart for views + visitors over period
    - DeviceBreakdown renders donut/pie chart with desktop/mobile/tablet percentages
    - GeoBreakdown renders horizontal bar chart of top 10 countries
    - DownloadStats shows total downloads, total bandwidth, links to existing download log
    - Period selector: 7d, 30d, 90d, All
    - Public gallery sends view beacon on mount with visitor_token, device info, and sends time_spent on unload
  </behavior>
  <action>
    1. Install recharts: `cd frontend && pnpm add recharts` (lightweight, well-maintained charting library)

    2. Create `frontend/src/hooks/useGalleryAnalytics.ts`:
       - Uses useQuery from @tanstack/react-query
       - Query key: ['gallery-analytics', galleryId, period]
       - Fetches from gallery-service API via service proxy
       - Returns typed GalleryAnalyticsResponse

    3. Create chart components in `frontend/src/components/features/gallery/analytics/`:
       - `ViewsChart.tsx`: recharts AreaChart with two areas (views in primary color, unique visitors in accent), ResponsiveContainer, XAxis (dates), YAxis, Tooltip, CartesianGrid
       - `DeviceBreakdown.tsx`: recharts PieChart with desktop/mobile/tablet slices, legend, percentage labels. Fallback to "No data" when all zeros
       - `GeoBreakdown.tsx`: horizontal BarChart of top countries with country names on Y axis, view counts on X axis. Use country-code-to-name mapping (static object, no library needed)
       - `DownloadStats.tsx`: summary cards showing total_downloads, total_bytes (formatted with formatFileSize from @rawdrive/shared-utils), link to download log tab

    4. Create `frontend/src/components/features/gallery/analytics/GalleryAnalyticsTab.tsx`:
       - Period selector buttons (7d/30d/90d/All) at top
       - Summary cards row: Total Views, Unique Visitors, Avg. Time Spent (formatted as Xm Ys), Views Today
       - Below cards: 2-column grid on desktop (ViewsChart spanning full width, DeviceBreakdown + GeoBreakdown side by side, DownloadStats full width)
       - Loading skeleton while data fetches
       - Empty state when no views yet ("No analytics data yet. Share your gallery to start tracking views.")
       - Respects dark mode via Tailwind dark: classes

    5. Wire GalleryAnalyticsTab into GalleryDetailPage.tsx:
       - Add "Analytics" tab (with BarChart3 lucide icon) to existing tab list
       - Lazy-load the tab content (only fetch analytics when tab is active)

    6. Add view tracking beacon to PublicGalleryShell.tsx:
       - On mount: POST /api/v1/public/galleries/{token}/view with { visitor_token (from fingerprintjs or localStorage UUID), device_type (from UA), browser, os, country_code (skip - server can derive) }
       - On unload (navigator.sendBeacon): POST /api/v1/public/galleries/{token}/view/time with { view_id, time_spent_seconds }
       - Store view_id in ref for unload beacon
       - Use navigator.sendBeacon for reliable delivery on tab close

    7. Tests: GalleryAnalyticsTab renders summary cards, shows empty state when no data, period selector changes query.
  </action>
  <verify>
    <automated>cd frontend && pnpm test src/components/features/gallery/analytics/GalleryAnalyticsTab.test.tsx --run</automated>
  </verify>
  <done>
    Analytics tab shows views chart, device/geo breakdown, download stats with period selector; view tracking beacon fires on public gallery load; time-spent sent on unload; recharts renders all charts
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gallery discovery filters + engagement ranking</name>
  <files>
    frontend/src/pages/workspace/GalleriesPage.tsx,
    services/gallery-service/src/api/v1/galleries.py,
    services/gallery-service/src/schemas/gallery.py
  </files>
  <behavior>
    - Gallery list API accepts: date_from, date_to, client_id, tags[] query params
    - Gallery list API accepts: sort_by with new options: views, downloads, shares (engagement ranking)
    - Gallery list response includes: view_count, download_count per gallery (from gallery_views and gallery_downloads aggregation)
    - Frontend filter bar adds: date range picker (two date inputs), client dropdown (from workspace clients), tag multi-select
    - Frontend sort options add: Most Viewed, Most Downloaded
    - "Most Popular" preset button applies sort_by=views + status=published
  </behavior>
  <action>
    1. Update `services/gallery-service/src/schemas/gallery.py`:
       - Add view_count (int, default 0) and download_count (int, default 0) to GalleryListItemResponse
       - These are computed fields from LEFT JOIN aggregation, not stored columns

    2. Update `services/gallery-service/src/api/v1/galleries.py`:
       - Extend GET /api/v1/galleries list endpoint to accept: date_from (date), date_to (date), client_id (UUID), tags (list[str]) query params
       - Add LEFT JOIN to gallery_views (COUNT) and gallery_downloads (COUNT) to the gallery list query to populate view_count and download_count
       - Support sort_by options: "views" (ORDER BY view_count DESC), "downloads" (ORDER BY download_count DESC)
       - Ensure all new filters include workspace_id in WHERE clause
       - Tag filter: WHERE tags @> ARRAY[$1]::text[] (PostgreSQL array containment)
       - Date filter: WHERE created_at BETWEEN $date_from AND $date_to
       - Client filter: WHERE client_id = $client_id

    3. Update `frontend/src/pages/workspace/GalleriesPage.tsx`:
       - Add to SortOption type: 'views' | 'downloads'
       - Add sort options in dropdown: "Most Viewed", "Most Downloaded"
       - Add filter controls in filter bar:
         a. Date range: two HTML date inputs (start/end) with "Clear" button
         b. Client: select dropdown populated from workspace clients API (existing endpoint)
         c. Tags: multi-select input with existing tag values from galleries (or free-text input with comma separation)
       - Add "Most Popular" preset button: sets sort='views', filterStatus='published', clears other filters
       - Show view_count and download_count in gallery cards (small stat badges: eye icon + count, download icon + count)
       - All filter state managed via useState, passed to useQuery params
       - Clear All Filters button resets everything

    4. Display engagement stats in gallery list items:
       - Grid view: small stat row at bottom of card (views icon + count, downloads icon + count)
       - List view: additional columns for views and downloads
  </action>
  <verify>
    <automated>cd frontend && pnpm test src/pages/workspace/GalleriesPage --run 2>/dev/null; cd /c/Users/admin/Desktop/RawDrive2 && docker exec rawdrive-gallery-service pytest tests/ -x -v -k "galleries" --timeout=30</automated>
  </verify>
  <done>
    Gallery list supports date/client/tag filters; sort by views and downloads works; gallery cards show view/download counts; "Most Popular" preset works; all queries workspace-scoped
  </done>
</task>

</tasks>

<verification>
- Gallery detail page shows Analytics tab with views chart, device breakdown, geo breakdown
- Period selector (7d/30d/90d/All) changes displayed data
- Public gallery visit creates a view record (visible in analytics after refresh)
- Gallery list filter bar shows date range, client, and tag filters
- Sorting by "Most Viewed" orders galleries by view count
- Gallery cards show view and download count badges
</verification>

<success_criteria>
- Analytics tab renders charts with real data from API
- View tracking beacon fires on public gallery access
- Time-spent tracking works via sendBeacon on unload
- Gallery list filters by date range, client, tags
- Engagement ranking sorts by views/downloads
- "Most Popular" preset works
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-sharing-analytics-polish/20-04-SUMMARY.md`
</output>
