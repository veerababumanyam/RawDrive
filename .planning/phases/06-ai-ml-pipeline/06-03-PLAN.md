---
phase: 06-ai-ml-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - backend/src/app/services/embedding_client.py
  - backend/src/app/workers/similarity_worker.py
  - services/ai-processing-service/src/workers/embedding_worker.py
  - services/ai-processing-service/src/workers/celery_app.py
  - backend/tests/test_similarity_worker.py
  - services/ai-processing-service/tests/test_embedding_worker.py
autonomous: true
requirements:
  - AI-02
  - AI-06

must_haves:
  truths:
    - "similarity_worker calls ai-processing-service HTTP API for CLIP embedding computation (not local ML)"
    - "Celery task exists for fire-and-forget embedding computation on upload"
    - "Embedding-based duplicate detection uses pgvector cosine similarity via EmbeddingRepository"
    - "similarity_worker _compute_batch_embeddings returns real 512-dim vectors from ai-processing-service"
  artifacts:
    - path: "backend/src/app/services/embedding_client.py"
      provides: "HTTP client to ai-processing-service embedding API"
      exports: ["EmbeddingClient", "get_embedding_client"]
    - path: "backend/src/app/workers/similarity_worker.py"
      provides: "Updated worker with real CLIP embedding via HTTP"
      contains: "embedding_client"
    - path: "services/ai-processing-service/src/workers/embedding_worker.py"
      provides: "Celery task for async embedding computation"
      contains: "celery_app.task"
  key_links:
    - from: "backend/src/app/workers/similarity_worker.py"
      to: "backend/src/app/services/embedding_client.py"
      via: "HTTP call to ai-processing-service"
      pattern: "embedding_client"
    - from: "backend/src/app/services/embedding_client.py"
      to: "services/ai-processing-service/src/api/v1/embeddings.py"
      via: "POST /api/v1/embed/images"
      pattern: "embed/images"
    - from: "backend/src/app/workers/similarity_worker.py"
      to: "backend/src/app/repositories/embedding_repository.py"
      via: "find_similar_by_embedding for duplicate detection"
      pattern: "find_similar_by_embedding"
---

<objective>
Wire the backend similarity_worker to call ai-processing-service for CLIP embeddings via HTTP, and add a Celery task for upload-triggered embedding computation.

Purpose: Replaces placeholder/TODO implementations in similarity_worker with real CLIP inference offloaded to ai-processing-service. Adds embedding-based duplicate detection using pgvector cosine similarity.
Output: EmbeddingClient HTTP service, updated similarity_worker, Celery embedding task, tests.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ai-ml-pipeline/06-RESEARCH.md
@.planning/phases/06-ai-ml-pipeline/06-01-SUMMARY.md
@.planning/phases/06-ai-ml-pipeline/06-02-SUMMARY.md

<interfaces>
<!-- From Plan 01: Embedding API on ai-processing-service -->
```python
# POST /api/v1/embed/images on ai-processing-service:8012
# Request: {"image_urls": ["object/key/1.jpg", "object/key/2.jpg"], "workspace_id": "uuid"}
# Response: {"embeddings": [{"url": "...", "embedding": [0.1, ...512 floats], "dimensions": 512}, ...], "processing_time_ms": 1234.5}
```

<!-- From Plan 02: EmbeddingRepository -->
```python
class EmbeddingRepository:
    async def store_embedding(workspace_id, asset_id, session_id, embedding: list[float], model_version="clip-vit-b-32")
    async def batch_store_embeddings(workspace_id, embeddings: list[dict])
    async def find_similar_by_embedding(workspace_id, embedding, exclude_asset_id, threshold=0.85, limit=10) -> list[dict]
def get_embedding_repository() -> EmbeddingRepository
```

<!-- Existing similarity_worker.py placeholders to replace: -->
<!-- _ensure_clip_model(): sets self._clip_model = "placeholder" -->
<!-- _compute_batch_embeddings(): returns [0.0] * 512 placeholder vectors -->
<!-- _cluster_embeddings(): returns [[emb] for emb in embeddings] (each photo = own cluster) -->

<!-- Celery app (ai-processing-service/src/workers/celery_app.py): -->
<!-- celery_app = Celery("ai-processing", broker=settings.REDIS_URL, backend=settings.REDIS_URL) -->
<!-- include=["workers.face_detection_worker"] -- add "workers.embedding_worker" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create EmbeddingClient and wire similarity_worker to ai-processing-service</name>
  <files>
    backend/src/app/services/embedding_client.py
    backend/src/app/workers/similarity_worker.py
    backend/tests/test_similarity_worker.py
  </files>
  <read_first>
    backend/src/app/workers/similarity_worker.py
    backend/src/app/config/settings.py
    backend/src/app/repositories/embedding_repository.py
    backend/src/app/services/embedding_client.py
  </read_first>
  <behavior>
    - Test 1: EmbeddingClient.embed_images sends POST to ai-processing-service:8012/api/v1/embed/images
    - Test 2: EmbeddingClient returns list of 512-dim float arrays
    - Test 3: similarity_worker._compute_batch_embeddings calls EmbeddingClient (not local model)
    - Test 4: similarity_worker._compute_batch_embeddings passes object_key from photo dicts
    - Test 5: similarity_worker._ensure_clip_model creates EmbeddingClient (no local torch import)
    - Test 6: After embedding, find_similar_by_embedding is called for duplicate detection per asset
  </behavior>
  <action>
    1. Create `backend/src/app/services/embedding_client.py`:
       - `EmbeddingClient` class with httpx.AsyncClient
       - `__init__(base_url: str)` defaulting to `http://rawdrive-ai-processing:8012` (Docker network name)
       - `async embed_images(image_urls: list[str], workspace_id: str) -> list[dict]` -- POST to /api/v1/embed/images
       - Parse response, return list of `{"url": str, "embedding": list[float]}` dicts
       - Timeout: 120 seconds (batch CLIP processing can be slow)
       - Retry: 1 retry on connection error
       - `get_embedding_client()` singleton factory
       - Read AI_PROCESSING_SERVICE_URL from settings (add to settings if needed, default `http://rawdrive-ai-processing:8012`)

    2. Update `similarity_worker.py`:
       - Replace `_ensure_clip_model()`: Create EmbeddingClient instead of placeholder. Remove `self._clip_model = "placeholder"`. Set `self._embedding_client = get_embedding_client()`.
       - Replace `_compute_batch_embeddings()`:
         a. Extract object_keys from photo dicts (use `processed_object_key` or `thumbnail_object_key`)
         b. Call `self._embedding_client.embed_images(object_keys, str(workspace_id))`
         c. Map results back to `{"asset_id": ..., "embedding": ...}` format
         d. Return real 512-dim embeddings
       - Add duplicate detection after embedding storage in `_compute_embeddings()`:
         After storing embeddings, for each embedding call `embedding_repo.find_similar_by_embedding()` with threshold from settings (DUPLICATE_CLIP_THRESHOLD, default 0.95). Log any duplicates found. Store duplicate pairs in a session metadata field or separate table.
       - Import EmbeddingRepository from plan 02

    3. Create `tests/test_similarity_worker.py`:
       - Mock EmbeddingClient to return fake embeddings
       - Mock EmbeddingRepository for store/find calls
       - Verify _compute_batch_embeddings uses HTTP client, not local model
       - Verify no `import torch` in similarity_worker.py
       - Verify duplicate detection calls find_similar_by_embedding
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_similarity_worker.py -x -v 2>/dev/null || echo "Tests created - run in Docker: docker exec rawdrive-backend pytest tests/test_similarity_worker.py -x"</automated>
  </verify>
  <done>
    - EmbeddingClient calls ai-processing-service HTTP API
    - similarity_worker uses EmbeddingClient for embedding computation (no local torch/CLIP)
    - Duplicate detection uses EmbeddingRepository.find_similar_by_embedding with cosine threshold
    - No torch import in backend/src/app/workers/similarity_worker.py
    - Tests verify HTTP-based embedding and duplicate detection flow
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Celery embedding task for upload-triggered computation</name>
  <files>
    services/ai-processing-service/src/workers/embedding_worker.py
    services/ai-processing-service/src/workers/celery_app.py
    services/ai-processing-service/tests/test_embedding_worker.py
  </files>
  <read_first>
    services/ai-processing-service/src/workers/celery_app.py
    services/ai-processing-service/src/workers/face_detection_worker.py
    services/ai-processing-service/src/models/clip_embedder.py
    services/ai-processing-service/src/services/r2_download.py
  </read_first>
  <behavior>
    - Test 1: compute_embedding task accepts asset_id, workspace_id, object_key parameters
    - Test 2: compute_embedding downloads image from R2, computes embedding, returns result dict
    - Test 3: compute_embedding task is registered in celery_app with max_retries=3
    - Test 4: compute_batch_embeddings processes list of assets
    - Test 5: Task routing sends embedding tasks to "embedding" queue
  </behavior>
  <action>
    1. Create `workers/embedding_worker.py`:
       - `@celery_app.task(bind=True, max_retries=3, queue="embedding")` decorator
       - `compute_embedding(self, asset_id: str, workspace_id: str, object_key: str) -> dict`:
         a. Download image from R2 using r2_download service
         b. Save to temp file
         c. Call get_clip_embedder().embed_image(temp_path)
         d. Return `{"asset_id": asset_id, "embedding": embedding.tolist(), "dimensions": 512}`
         e. Clean up temp file
       - `compute_batch_embeddings(self, assets: list[dict]) -> list[dict]`:
         a. Download all images from R2
         b. Call get_clip_embedder().embed_images(temp_paths)
         c. Return list of result dicts
       - Error handling: retry on transient errors (network, R2 timeout)

    2. Update `celery_app.py`:
       - Add `"workers.embedding_worker"` to the `include` list
       - Add task routing: `"workers.embedding_worker.compute_embedding": {"queue": "embedding"}`
       - Add queue definition for "embedding"

    3. Create `tests/test_embedding_worker.py`:
       - Mock get_clip_embedder and download_from_r2
       - Test single embedding computation
       - Test batch embedding computation
       - Test retry on failure
  </action>
  <verify>
    <automated>cd services/ai-processing-service && python -m pytest tests/test_embedding_worker.py -x -v 2>/dev/null || echo "Tests created - run in Docker for full validation"</automated>
  </verify>
  <done>
    - Celery task compute_embedding exists and is registered
    - Task downloads from R2, computes CLIP embedding, returns result
    - celery_app.py includes embedding_worker in task list
    - Task routing sends to "embedding" queue
    - Tests verify task registration and execution flow
  </done>
</task>

</tasks>

<verification>
- embedding_client.py contains httpx POST to `/api/v1/embed/images`
- similarity_worker.py does NOT contain `import torch` or `self._clip_model = "placeholder"`
- similarity_worker.py contains `embedding_client` and `find_similar_by_embedding`
- celery_app.py include list contains `"workers.embedding_worker"`
- embedding_worker.py contains `@celery_app.task` decorator
- All test files pass
</verification>

<success_criteria>
- Backend offloads all ML inference to ai-processing-service via HTTP
- similarity_worker replaces all placeholder implementations with real HTTP calls
- Embedding-based duplicate detection queries pgvector with cosine similarity
- Celery task available for fire-and-forget embedding on upload
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-ml-pipeline/06-03-SUMMARY.md`
</output>
