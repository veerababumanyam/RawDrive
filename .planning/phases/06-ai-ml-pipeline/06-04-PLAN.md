---
phase: 06-ai-ml-pipeline
plan: 04
type: tdd
wave: 2
depends_on: ["06-02"]
files_modified:
  - services/ai-processing-service/src/services/clustering_service.py
  - backend/src/app/workers/similarity_worker.py
  - services/ai-processing-service/tests/test_clustering.py
  - backend/tests/test_clustering_integration.py
autonomous: true
requirements:
  - AI-07

must_haves:
  truths:
    - "DBSCAN clustering groups similar photos using cosine distance matrix"
    - "eps parameter correctly derived from similarity_threshold (eps = 1 - threshold)"
    - "Singletons (noise points) are handled as individual groups"
    - "Cluster results contain asset_ids grouped by similarity"
  artifacts:
    - path: "services/ai-processing-service/src/services/clustering_service.py"
      provides: "DBSCAN clustering with cosine distance"
      exports: ["cluster_embeddings"]
    - path: "services/ai-processing-service/tests/test_clustering.py"
      provides: "Unit tests for DBSCAN clustering"
  key_links:
    - from: "backend/src/app/workers/similarity_worker.py"
      to: "services/ai-processing-service/src/services/clustering_service.py"
      via: "HTTP call or direct import for clustering"
      pattern: "cluster_embeddings"
---

<objective>
Implement DBSCAN clustering for grouping similar photos using cosine distance on CLIP embeddings.

Purpose: Replaces the placeholder single-photo-per-cluster logic in similarity_worker with real density-based clustering that identifies groups of visually similar photos for curation review.
Output: ClusteringService with DBSCAN, updated similarity_worker _cluster_embeddings, comprehensive TDD tests.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ai-ml-pipeline/06-RESEARCH.md

<interfaces>
<!-- similarity_worker._cluster_embeddings current placeholder: -->
```python
async def _cluster_embeddings(self, embeddings: list[dict], similarity_threshold: float) -> list[list[dict]]:
    # TODO: Implement actual clustering algorithm
    return [[emb] for emb in embeddings]  # each photo = own cluster
```
<!-- Input format: [{"asset_id": UUID, "embedding": [float * 512]}, ...] -->
<!-- Output format: [[{"asset_id": UUID, "embedding": [...]}, ...], ...] (list of clusters) -->

<!-- Research-recommended DBSCAN pattern: -->
```python
from sklearn.cluster import DBSCAN
from scipy.spatial.distance import pdist, squareform
# eps = 1 - similarity_threshold (e.g., 0.85 threshold -> eps=0.15)
# min_samples = 2 (at least 2 photos to form a group)
# metric = "precomputed" with cosine distance matrix
```
</interfaces>
</context>

<feature>
  <name>DBSCAN Photo Clustering</name>
  <files>services/ai-processing-service/src/services/clustering_service.py, services/ai-processing-service/tests/test_clustering.py</files>
  <behavior>
    - cluster_embeddings(embeddings, similarity_threshold=0.85, min_samples=2) returns list of clusters
    - Each cluster is a list of embedding dicts (same format as input)
    - Similar photos (cosine similarity >= threshold) are grouped together
    - Noise points (DBSCAN label=-1) become singleton clusters
    - With identical embeddings (cosine dist=0), all are in one cluster
    - With random orthogonal embeddings, all are singletons
    - Empty input returns empty list
    - Single photo input returns one singleton cluster
    - eps = 1 - similarity_threshold conversion is correct
  </behavior>
  <implementation>
    1. Create clustering_service.py with cluster_embeddings function
    2. Update similarity_worker._cluster_embeddings to use it (via HTTP or direct call)
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD - Create clustering_service.py with DBSCAN implementation</name>
  <files>
    services/ai-processing-service/src/services/clustering_service.py
    services/ai-processing-service/tests/test_clustering.py
  </files>
  <read_first>
    services/ai-processing-service/requirements.txt
    services/ai-processing-service/src/config.py
  </read_first>
  <behavior>
    - Test 1: Empty embeddings returns empty list
    - Test 2: Single embedding returns [[embedding]] (one singleton cluster)
    - Test 3: Two identical embeddings grouped into one cluster
    - Test 4: Two orthogonal embeddings become two singleton clusters
    - Test 5: Five embeddings where 3 are similar (cosine > 0.85) and 2 are different -> 3 clusters (1 group of 3, 2 singletons)
    - Test 6: similarity_threshold=0.5 groups more liberally than 0.95
    - Test 7: All same embedding -> one big cluster
    - Test 8: min_samples parameter respected
  </behavior>
  <action>
    RED phase: Write test_clustering.py first with all 8 test cases using synthetic numpy vectors:
    - Use `np.random.seed(42)` for reproducible tests
    - Create helper to generate similar/dissimilar embeddings
    - Import cluster_embeddings from clustering_service (will fail initially)

    GREEN phase: Create `services/clustering_service.py`:
    ```python
    def cluster_embeddings(
        embeddings: list[dict],
        similarity_threshold: float = 0.85,
        min_samples: int = 2,
    ) -> list[list[dict]]:
    ```
    - Convert embeddings to numpy array
    - Compute pairwise cosine distance matrix: `squareform(pdist(vectors, metric="cosine"))`
    - Run DBSCAN: `DBSCAN(eps=1-similarity_threshold, min_samples=min_samples, metric="precomputed").fit_predict(distance_matrix)`
    - Group by labels: label >= 0 are clusters, label == -1 are singletons
    - Return list of clusters (each cluster is list of original embedding dicts)

    REFACTOR phase: Ensure edge cases handled (empty, single, all identical).
  </action>
  <verify>
    <automated>cd services/ai-processing-service && python -m pytest tests/test_clustering.py -x -v</automated>
  </verify>
  <done>
    - test_clustering.py has 8 test cases, all pass
    - clustering_service.py uses DBSCAN with cosine distance
    - eps correctly computed as 1 - similarity_threshold
    - Singletons handled (noise points become individual clusters)
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire clustering into similarity_worker._cluster_embeddings</name>
  <files>
    backend/src/app/workers/similarity_worker.py
    backend/tests/test_clustering_integration.py
  </files>
  <read_first>
    backend/src/app/workers/similarity_worker.py
    backend/src/app/services/embedding_client.py
  </read_first>
  <action>
    1. Update `similarity_worker.py` `_cluster_embeddings()`:
       - Replace the placeholder `return [[emb] for emb in embeddings]`
       - Option A (preferred): Call ai-processing-service clustering endpoint via HTTP.
         Add a POST `/api/v1/cluster` endpoint to ai-processing-service that accepts embeddings and returns clusters. Add to embedding_client: `async cluster_embeddings(embeddings, threshold) -> list[list[dict]]`.
       - Option B: If clustering is lightweight enough, install scikit-learn in backend and call cluster_embeddings directly. However, per PERF-01 this should be in ai-processing-service.
       - Use Option A: Add `/api/v1/cluster` endpoint to ai-processing-service (new file `api/v1/clustering.py`), register in main.py.

    2. Add `api/v1/clustering.py` to ai-processing-service:
       - POST `/cluster` accepts `{"embeddings": [{"asset_id": "...", "embedding": [...]}], "similarity_threshold": 0.85}`
       - Returns `{"clusters": [[{"asset_id": "...", "embedding": [...]}], ...], "num_clusters": N, "num_singletons": M}`
       - Calls clustering_service.cluster_embeddings internally

    3. Create `tests/test_clustering_integration.py` in backend:
       - Mock HTTP call to ai-processing-service
       - Verify similarity_worker._cluster_embeddings sends correct payload
       - Verify returned clusters are passed to _create_group_data
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_clustering_integration.py -x -v 2>/dev/null || echo "Tests created - run in Docker for full validation"</automated>
  </verify>
  <done>
    - similarity_worker._cluster_embeddings calls ai-processing-service /api/v1/cluster
    - Placeholder `[[emb] for emb in embeddings]` is removed
    - Clustering endpoint registered in ai-processing-service
    - Tests verify HTTP call and cluster result handling
  </done>
</task>

</tasks>

<verification>
- clustering_service.py contains `DBSCAN` and `pdist` and `cosine`
- similarity_worker.py does NOT contain `return [[emb] for emb in embeddings]`
- ai-processing-service has /api/v1/cluster endpoint
- All test files pass: `pytest tests/test_clustering.py tests/test_clustering_integration.py -x`
</verification>

<success_criteria>
- DBSCAN clustering produces meaningful groups from CLIP embeddings
- similarity_worker delegates clustering to ai-processing-service
- Edge cases handled: empty, single, all-same, all-different
- All TDD tests pass (RED -> GREEN -> REFACTOR completed)
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-ml-pipeline/06-04-SUMMARY.md`
</output>
