---
phase: 16-gallery-layout-engine-progressive-loading
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - frontend/src/components/features/gallery/layouts/JustifiedLayout.tsx
  - frontend/src/components/features/gallery/layouts/MosaicLayout.tsx
  - frontend/src/components/features/gallery/layouts/EnhancedMasonryLayout.tsx
  - frontend/src/components/features/gallery/layouts/GalleryLayoutEngine.tsx
  - frontend/src/components/features/gallery/layouts/index.ts
autonomous: true
requirements: [LYOT-01, LYOT-02, LYOT-03]

must_haves:
  truths:
    - "Justified layout renders photos in uniform-height rows with aspect-ratio-aware balancing"
    - "Mosaic layout renders photos in varied tile sizes creating a magazine-style grid"
    - "Enhanced masonry layout renders with improved column balancing and responsive breakpoints"
    - "All three layouts use ProgressiveImage for LQIP blur-up loading"
    - "All three layouts are responsive across mobile, tablet, and desktop"
  artifacts:
    - path: "frontend/src/components/features/gallery/layouts/JustifiedLayout.tsx"
      provides: "Flickr-style justified row layout using justified-layout npm package"
      exports: ["JustifiedLayout"]
      min_lines: 60
    - path: "frontend/src/components/features/gallery/layouts/MosaicLayout.tsx"
      provides: "Magazine-style mosaic with varied tile sizes based on aspect ratio"
      exports: ["MosaicLayout"]
      min_lines: 80
    - path: "frontend/src/components/features/gallery/layouts/EnhancedMasonryLayout.tsx"
      provides: "CSS column-count masonry with JS reordering for chronological order"
      exports: ["EnhancedMasonryLayout"]
      min_lines: 80
  key_links:
    - from: "frontend/src/components/features/gallery/layouts/JustifiedLayout.tsx"
      to: "justified-layout"
      via: "npm import for row geometry computation"
      pattern: "import justifiedLayout from 'justified-layout'"
    - from: "frontend/src/components/features/gallery/layouts/GalleryLayoutEngine.tsx"
      to: "JustifiedLayout, MosaicLayout, EnhancedMasonryLayout"
      via: "strategy dispatch switch cases"
      pattern: "case.*justified.*JustifiedLayout"
---

<objective>
Implement three layout renderers: JustifiedLayout (using justified-layout npm package), MosaicLayout (algorithmic tile sizing), and EnhancedMasonryLayout (CSS column-count with JS reordering). Wire all three into GalleryLayoutEngine's strategy dispatcher.

Purpose: Delivers the three core layout modes (LYOT-01, LYOT-02, LYOT-03) that distinguish a professional gallery from a basic photo grid.
Output: Three layout renderer components wired into the layout engine.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-gallery-layout-engine-progressive-loading/16-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan implements against -->

From frontend/src/components/features/gallery/layouts/types.ts (created in Plan 01):
```typescript
export interface LayoutAsset {
  asset_id: string;
  src: string;
  lqip?: string;
  width: number;
  height: number;
  aspectRatio: number;
  alt: string;
  onClick?: () => void;
}

export interface LayoutRendererProps {
  assets: LayoutAsset[];
  containerWidth: number;
  gap?: number;
  onAssetClick?: (asset: LayoutAsset, index: number) => void;
}
```

From frontend/src/components/features/gallery/layouts/ProgressiveImage.tsx (created in Plan 01):
```typescript
// Props: { src: string; lqip?: string; width: number; height: number; alt: string; className?: string; onClick?: () => void; style?: React.CSSProperties }
export const ProgressiveImage: React.FC<ProgressiveImageProps>;
```

From justified-layout npm package (installed in Plan 01):
```typescript
// import justifiedLayout from 'justified-layout';
// const geometry = justifiedLayout(aspectRatios: number[], config: { containerWidth, targetRowHeight, boxSpacing });
// geometry.boxes: Array<{ top, left, width, height }>
// geometry.containerHeight: number
```

From frontend/src/components/features/gallery/layouts/SmartMasonryGrid.tsx (existing reference):
```typescript
// Has calculateOptimalHeight, ResizeObserver pattern, column balancing logic
// Uses CSS absolute positioning with computed top/left/width/height
// GAP_SIZES = { sm: 4, md: 8, lg: 12 }
// DEFAULT_COLUMNS = { sm: 2, md: 3, lg: 4, xl: 5 }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement JustifiedLayout and MosaicLayout renderers</name>
  <files>
    frontend/src/components/features/gallery/layouts/JustifiedLayout.tsx
    frontend/src/components/features/gallery/layouts/JustifiedLayout.test.tsx
    frontend/src/components/features/gallery/layouts/MosaicLayout.tsx
    frontend/src/components/features/gallery/layouts/MosaicLayout.test.tsx
  </files>
  <read_first>
    frontend/src/components/features/gallery/layouts/types.ts (LayoutRendererProps contract)
    frontend/src/components/features/gallery/layouts/ProgressiveImage.tsx (blur-up image component)
    frontend/src/components/features/gallery/layouts/SmartMasonryGrid.tsx (existing patterns for ResizeObserver, gap sizes)
    frontend/node_modules/justified-layout/README.md (if exists, for API reference)
  </read_first>
  <behavior>
    - JustifiedLayout: computes row geometry using justified-layout library with targetRowHeight 240px
    - JustifiedLayout: renders absolutely positioned ProgressiveImages based on geometry.boxes coordinates
    - JustifiedLayout: container height set from geometry.containerHeight
    - JustifiedLayout: responsive — targetRowHeight adjusts: 180px on mobile (<640px), 220px on tablet, 240px on desktop
    - MosaicLayout: assigns tile span based on aspect ratio: landscape (>1.4) gets 2-col span, portrait (<0.7) gets 2-row span, square gets 1x1
    - MosaicLayout: first image (hero) always gets 2x2 span
    - MosaicLayout: uses CSS Grid with grid-auto-flow: dense for gap filling
    - MosaicLayout: responsive grid columns: 2 on mobile, 3 on tablet, 4 on desktop
    - Both use ProgressiveImage for each photo cell
  </behavior>
  <action>
    1. Create frontend/src/components/features/gallery/layouts/JustifiedLayout.tsx:
       - Import justifiedLayout from 'justified-layout'
       - Accept LayoutRendererProps
       - Extract aspectRatios array from assets: assets.map(a => a.aspectRatio)
       - Compute geometry: justifiedLayout(aspectRatios, { containerWidth, targetRowHeight: rowHeight, boxSpacing: gap || 8 })
       - Determine rowHeight responsively: use window.innerWidth check — <640px: 180, <1024px: 220, else: 240
       - Render container div with position: relative, height: geometry.containerHeight
       - For each geometry.boxes[i], render ProgressiveImage with position: absolute, top/left/width/height from box
       - Recalculate on containerWidth change (useMemo dependency)

    2. Create frontend/src/components/features/gallery/layouts/MosaicLayout.tsx:
       - Accept LayoutRendererProps
       - Base grid: CSS Grid with 4 columns on desktop (grid-template-columns: repeat(4, 1fr)), 3 tablet, 2 mobile
       - Tile span algorithm per asset:
         - Index 0 (hero): gridColumn: span 2, gridRow: span 2
         - aspectRatio > 1.4 (landscape): gridColumn: span 2 (wide tile)
         - aspectRatio < 0.7 (portrait): gridRow: span 2 (tall tile)
         - else: 1x1 standard tile
       - Enable grid-auto-flow: dense so CSS fills gaps automatically
       - Each tile renders ProgressiveImage wrapped in a div with the computed grid spans
       - ProgressiveImage gets style={{ width: '100%', height: '100%' }} within each grid cell
       - Gap from props, default 8px

    3. Write tests that verify:
       - JustifiedLayout renders correct number of ProgressiveImage children
       - JustifiedLayout container has position: relative
       - MosaicLayout hero image (first) has gridColumn span 2 and gridRow span 2
       - MosaicLayout renders all assets as ProgressiveImage
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/features/gallery/layouts/JustifiedLayout.test.tsx src/components/features/gallery/layouts/MosaicLayout.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - JustifiedLayout calls justified-layout library with aspect ratios and containerWidth
    - JustifiedLayout positions images absolutely using geometry.boxes coordinates
    - MosaicLayout uses CSS Grid with grid-auto-flow: dense
    - MosaicLayout hero image spans 2x2
    - Both renderers use ProgressiveImage for LQIP blur-up
    - Both are responsive (different column/row configs per breakpoint)
    - All tests pass
  </acceptance_criteria>
  <done>JustifiedLayout computes Flickr-style uniform-height rows; MosaicLayout creates magazine-style varied tiles with hero image prominence; both render ProgressiveImage with LQIP</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement EnhancedMasonryLayout and wire all three into GalleryLayoutEngine</name>
  <files>
    frontend/src/components/features/gallery/layouts/EnhancedMasonryLayout.tsx
    frontend/src/components/features/gallery/layouts/EnhancedMasonryLayout.test.tsx
    frontend/src/components/features/gallery/layouts/GalleryLayoutEngine.tsx
    frontend/src/components/features/gallery/layouts/index.ts
  </files>
  <read_first>
    frontend/src/components/features/gallery/layouts/SmartMasonryGrid.tsx (existing masonry logic — reference for column balancing)
    frontend/src/components/features/gallery/layouts/GalleryLayoutEngine.tsx (strategy dispatcher from Plan 01)
    frontend/src/components/features/gallery/layouts/types.ts (LayoutRendererProps)
    frontend/src/components/features/gallery/layouts/JustifiedLayout.tsx (just created — sibling pattern)
    frontend/src/components/features/gallery/layouts/index.ts (current exports)
  </read_first>
  <behavior>
    - EnhancedMasonryLayout: uses CSS column-count with JS reordering for chronological display
    - EnhancedMasonryLayout: reorders assets from left-to-right reading order into column-first fill order
    - EnhancedMasonryLayout: responsive columns: 2 on mobile (<640px), 3 on tablet (<1024px), 4 on desktop (<1536px), 5 on xl
    - EnhancedMasonryLayout: each item uses break-inside: avoid CSS
    - GalleryLayoutEngine dispatch: 'justified' -> JustifiedLayout, 'mosaic' -> MosaicLayout, 'masonry' -> EnhancedMasonryLayout
  </behavior>
  <action>
    1. Create frontend/src/components/features/gallery/layouts/EnhancedMasonryLayout.tsx:
       - Accept LayoutRendererProps
       - CSS column-count layout (not absolute positioning like SmartMasonryGrid)
       - Responsive column count via media queries or containerWidth check:
         - <640px: 2 columns
         - <1024px: 3 columns
         - <1536px: 4 columns
         - >=1536px: 5 columns
       - JS reordering algorithm to fix chronological order:
         CSS column-count fills top-to-bottom per column. For chronological left-to-right display,
         reorder the assets array: given N columns, asset at visual position i should be placed at
         column = i % numColumns, row = floor(i / numColumns). Rebuild the array so items fill
         columns in the right order: for each column c, items are those at indices c, c+numColumns, c+2*numColumns, etc.
         This means the reordered array = [0, numCols, 2*numCols, ..., 1, numCols+1, 2*numCols+1, ...] mapped from original indices.
       - Each item wrapped in div with break-inside: avoid, margin-bottom: gap
       - Each item renders ProgressiveImage with full width, natural height (aspect-ratio CSS)
       - Track containerWidth via ResizeObserver on container ref

    2. Update frontend/src/components/features/gallery/layouts/GalleryLayoutEngine.tsx:
       - Import JustifiedLayout, MosaicLayout, EnhancedMasonryLayout
       - Update switch cases:
         - 'justified' -> <JustifiedLayout {...layoutProps} />
         - 'mosaic' -> <MosaicLayout {...layoutProps} />
         - 'masonry' -> <EnhancedMasonryLayout {...layoutProps} />
         - Keep 'grid' -> GridLayout
         - Keep remaining as placeholders (filmstrip, slideshow, collage, timeline)
       - Ensure AnimatePresence key changes when layout changes for transition animation

    3. Update frontend/src/components/features/gallery/layouts/index.ts:
       - Export JustifiedLayout, MosaicLayout, EnhancedMasonryLayout

    4. Write tests that verify:
       - EnhancedMasonryLayout reorders assets for chronological display in column-count layout
       - EnhancedMasonryLayout renders ProgressiveImage for each asset
       - EnhancedMasonryLayout items have break-inside: avoid
  </action>
  <verify>
    <automated>cd frontend && pnpm exec vitest run src/components/features/gallery/layouts/EnhancedMasonryLayout.test.tsx --reporter=verbose 2>&1 | tail -15 && pnpm exec tsc --noEmit 2>&1 | grep "error" | head -5; echo "TypeCheck exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - EnhancedMasonryLayout uses CSS column-count (not absolute positioning)
    - EnhancedMasonryLayout reorders assets array for chronological left-to-right reading order
    - EnhancedMasonryLayout responsive: 2/3/4/5 columns at sm/md/lg/xl breakpoints
    - GalleryLayoutEngine dispatches 'justified' to JustifiedLayout
    - GalleryLayoutEngine dispatches 'mosaic' to MosaicLayout
    - GalleryLayoutEngine dispatches 'masonry' to EnhancedMasonryLayout
    - All three layouts render ProgressiveImage for blur-up
    - TypeScript compiles without new errors
    - All tests pass
  </acceptance_criteria>
  <done>EnhancedMasonryLayout renders CSS column-count masonry with JS chronological reordering; GalleryLayoutEngine dispatches all 3 new layouts plus grid; TypeScript clean</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm exec tsc --noEmit` passes (no new type errors)
- `cd frontend && pnpm exec vitest run src/components/features/gallery/layouts/` passes all layout tests
- GalleryLayoutEngine switch covers grid, masonry, justified, mosaic
- All layouts use ProgressiveImage (grep confirms import in each file)
</verification>

<success_criteria>
- Three layout renderers fully implemented and rendering with ProgressiveImage
- GalleryLayoutEngine dispatches to correct renderer based on LayoutStyle
- Justified uses justified-layout npm package geometry
- Mosaic uses CSS Grid with dense auto-flow and hero prominence
- Enhanced masonry uses CSS column-count with JS chronological reordering
- All responsive across mobile/tablet/desktop
</success_criteria>

<output>
After completion, create `.planning/phases/16-gallery-layout-engine-progressive-loading/16-02-SUMMARY.md`
</output>
