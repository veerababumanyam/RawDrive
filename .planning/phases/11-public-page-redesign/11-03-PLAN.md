---
phase: 11-public-page-redesign
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - frontend/src/pages/public/PublicPersonalProfilePage.tsx
  - frontend/src/pages/public/PublicProfilePage.tsx
  - frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx
  - frontend/src/components/features/profile/shared/__tests__/PublicProfileRenderer.test.tsx
autonomous: false
requirements: [PUBPG-01, PUBPG-02, PUBPG-03, PUBPG-04, PUBPG-06, SEO-01, SEO-02, SEO-03, SEO-04]
must_haves:
  truths:
    - "Personal profile page at /u/:slug renders with animated background, responsive grid, and dark mode"
    - "Company profile page at /p/:slug renders with animated background, responsive grid, and dark mode"
    - "Both pages use react-helmet-async to update meta tags client-side for SPA navigation"
    - "Bento grid visually reflows from 1 to 4 columns across breakpoints"
    - "Animated theme background is visible and does not cause jank"
    - "Dark mode renders correct colors when system preference is dark"
  artifacts:
    - path: "frontend/src/pages/public/PublicPersonalProfilePage.tsx"
      provides: "Personal profile page wired to enhanced renderer"
      contains: "PublicProfileRenderer"
    - path: "frontend/src/pages/public/PublicProfilePage.tsx"
      provides: "Company profile page wired to enhanced renderer"
      contains: "PublicProfileRenderer"
  key_links:
    - from: "frontend/src/pages/public/PublicPersonalProfilePage.tsx"
      to: "frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx"
      via: "renders PublicProfileRenderer with profileType='personal'"
      pattern: "profileType.*personal"
    - from: "frontend/src/pages/public/PublicProfilePage.tsx"
      to: "frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx"
      via: "renders PublicProfileRenderer with profileType='company'"
      pattern: "profileType.*company"
---

<objective>
Wire enhanced frontend components to page routes and verify the full visual experience across both profile types.

Purpose: Connect the responsive bento grid, animated backgrounds, and dark mode from Plan 01 with the public page components, ensure react-helmet-async client-side meta tags work alongside the backend HTML shell from Plan 02, and visually verify the complete experience.

Output: Updated PublicPersonalProfilePage and PublicProfilePage wired to enhanced PublicProfileRenderer, integration test for renderer, visual checkpoint.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-public-page-redesign/11-CONTEXT.md
@.planning/phases/11-public-page-redesign/11-01-SUMMARY.md
@.planning/phases/11-public-page-redesign/11-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From frontend/src/components/features/profile/public/animations/AnimatedBackgroundRenderer.tsx:
```typescript
type AnimationType = 'gradient-shift' | 'particles' | 'wave' | 'aurora' | 'none';
export const AnimatedBackgroundRenderer: React.FC<{
  animationType?: AnimationType;
  themeTokens: ThemeTokens;
  children: React.ReactNode;
}>;
```

From frontend/src/hooks/useColorScheme.ts:
```typescript
export function useColorScheme(): 'light' | 'dark';
```

From frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts:
```typescript
export interface ThemeTokens {
  // ... all existing tokens plus:
  '--theme-animation-type': string;
}
```

<!-- From Plan 02 outputs -->
Backend endpoints:
- GET /api/v1/u/{slug}/page -> HTML shell with OG/meta/JSON-LD
- GET /api/v1/u/{slug}/og-image -> 1200x630 PNG
- GET /api/v1/p/{slug}/page -> HTML shell with OG/meta/JSON-LD
- GET /api/v1/p/{slug}/og-image -> 1200x630 PNG
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire Public Pages to Enhanced Renderer + Client-Side Meta Tags</name>
  <files>
    frontend/src/pages/public/PublicPersonalProfilePage.tsx,
    frontend/src/pages/public/PublicProfilePage.tsx,
    frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx,
    frontend/src/components/features/profile/shared/__tests__/PublicProfileRenderer.test.tsx
  </files>
  <read_first>
    frontend/src/pages/public/PublicPersonalProfilePage.tsx,
    frontend/src/pages/public/PublicProfilePage.tsx,
    frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx,
    frontend/src/components/features/profile/shared/__tests__/PublicProfileRenderer.test.tsx,
    frontend/src/components/features/profile/public/animations/AnimatedBackgroundRenderer.tsx,
    frontend/src/hooks/useColorScheme.ts,
    frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts
  </read_first>
  <behavior>
    - Test: PublicProfileRenderer renders AnimatedBackgroundRenderer wrapper
    - Test: PublicProfileRenderer passes animation type from theme tokens to AnimatedBackgroundRenderer
    - Test: PublicProfileRenderer applies dark mode class based on useColorScheme
    - Test: PublicPersonalProfilePage renders Helmet with og:title from profile data
    - Test: PublicProfilePage renders Helmet with og:title from profile data
  </behavior>
  <action>
    1. Enhance `frontend/src/components/features/profile/shared/__tests__/PublicProfileRenderer.test.tsx`:
       - Add test: renders AnimatedBackgroundRenderer component
       - Add test: passes correct animationType based on theme
       - Add test: applies dark class when useColorScheme returns 'dark'
       - Mock useColorScheme and AnimatedBackgroundRenderer

    2. Update `frontend/src/pages/public/PublicPersonalProfilePage.tsx`:
       - Ensure react-helmet-async `<Helmet>` is used with:
         - `<title>{displayName} | RawDrive</title>`
         - `<meta name="description" content={bio} />`
         - `<meta property="og:title" content={displayName} />`
         - `<meta property="og:description" content={bio} />`
         - `<meta property="og:image" content={`${API_BASE}/api/v1/u/${slug}/og-image`} />`
         - `<meta property="og:url" content={`${FRONTEND_URL}/u/${slug}`} />`
         - `<meta name="twitter:card" content="summary_large_image" />`
         - `<link rel="canonical" href={`${FRONTEND_URL}/u/${slug}`} />`
       - These are for SPA navigation (when user navigates client-side). The backend HTML shell handles the initial server-rendered tags for crawlers.
       - Verify PublicProfileRenderer is passed themeId from profile data

    3. Update `frontend/src/pages/public/PublicProfilePage.tsx`:
       - Same Helmet pattern but with company profile data:
         - og:title from company_name
         - og:description from tagline
         - og:image from `/api/v1/p/${slug}/og-image`
         - canonical from `/p/${slug}`
       - Verify PublicProfileRenderer is passed themeId from profile data

    4. Verify `PublicProfileRenderer.tsx` (from Plan 01) correctly:
       - Uses `useColorScheme()` instead of static useMemo for dark mode
       - Wraps content in `<AnimatedBackgroundRenderer>`
       - Reads `'--theme-animation-type'` from resolved tokens
       - If any of these are missing (Plan 01 didn't complete them), add them now

    5. Run full profile test suite to ensure no regressions.
  </action>
  <verify>
    <automated>cd frontend && pnpm test src/components/features/profile/shared/__tests__/PublicProfileRenderer.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "og:title" frontend/src/pages/public/PublicPersonalProfilePage.tsx
    - grep -q "og:image" frontend/src/pages/public/PublicPersonalProfilePage.tsx
    - grep -q "twitter:card" frontend/src/pages/public/PublicPersonalProfilePage.tsx
    - grep -q "og:title" frontend/src/pages/public/PublicProfilePage.tsx
    - grep -q "og:image" frontend/src/pages/public/PublicProfilePage.tsx
    - grep -q "AnimatedBackgroundRenderer" frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx
    - grep -q "useColorScheme" frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx
  </acceptance_criteria>
  <done>Both public page components render enhanced PublicProfileRenderer with animated backgrounds, responsive grid, dark mode. Client-side Helmet meta tags match backend HTML shell. All renderer tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual Verification of Public Profile Pages</name>
  <files>none</files>
  <action>
    Human visually verifies the complete public page redesign across both profile types.
    This checkpoint validates the combined output of Plans 01, 02, and 03 Task 1.

    Complete public page redesign includes:
    - Responsive bento grid (1->2->3->4 columns)
    - Animated theme backgrounds (gradient-shift, particles, wave, aurora)
    - Glassmorphism card effects with hover lift
    - Dark mode auto-detection
    - SEO meta tags (OG, Twitter Card, JSON-LD) via backend HTML shell
    - OG image generation (1200x630 PNG with gradient + avatar)

    Verification steps:
    1. Start dev server: `cd frontend && pnpm dev`
    2. Visit http://localhost:5173/u/test-slug (personal profile)
       - Verify: Animated background is visible (gradient shifting or particles floating)
       - Verify: Glass cards have frosted effect with subtle borders
       - Verify: Sections stagger-animate in on load
    3. Resize browser to test responsive breakpoints:
       - 375px width: Single column, stacked sections
       - 768px width: 2-column grid
       - 1024px width: 3-column grid
       - 1280px width: 4-column grid
    4. Toggle system dark mode (OS settings or browser devtools):
       - Verify: Background, cards, text all switch to dark variant
       - Verify: No flash of light mode on reload
    5. Visit http://localhost:5173/p/test-slug (company profile)
       - Verify: Same quality as personal profile
       - Verify: Company-specific sections render correctly
    6. Test SEO (in terminal):
       - Run: curl -s http://localhost:8000/api/v1/u/test-slug/page | grep 'og:title'
       - Run: curl -s http://localhost:8000/api/v1/u/test-slug/page | grep 'application/ld+json'
       - Run: curl -sI http://localhost:8000/api/v1/u/test-slug/og-image (expect image/png)
    7. Test reduced motion:
       - In browser devtools, enable "Emulate CSS media feature prefers-reduced-motion: reduce"
       - Verify: All animations stop, static gradient background shown
    8. Hover over glass cards:
       - Verify: Subtle scale-up and lift effect
  </action>
  <verify>Human visual approval of both /u/:slug and /p/:slug pages at all breakpoints, dark mode, animations, and SEO meta tags</verify>
  <done>User types "approved" confirming both profile pages are visually premium, responsive, animated, dark-mode aware, and SEO-discoverable</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm test src/components/features/profile --run` -- all profile tests pass
- `cd frontend && pnpm lint` -- no lint errors
- `docker exec rawdrive-backend pytest tests/test_og_image_service.py tests/test_seo_service.py tests/test_personal_profile_seo.py -v` -- all backend tests pass
- Visual verification of both /u/ and /p/ pages at all breakpoints
- curl verification of HTML shell meta tags
</verification>

<success_criteria>
- Both /u/:slug and /p/:slug render premium responsive layouts with animated backgrounds
- Dark mode works automatically based on system preference
- SEO meta tags are present in server-rendered HTML (verifiable with curl)
- OG images generate correctly
- All tests (frontend + backend) pass
- Human visual approval received
</success_criteria>

<output>
After completion, create `.planning/phases/11-public-page-redesign/11-03-SUMMARY.md`
</output>
