---
phase: 11-public-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/features/profile/ProfileBentoGrid.tsx
  - frontend/src/components/features/profile/ProfileGridItem.tsx
  - frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx
  - frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts
  - frontend/src/components/features/profile/public/GlassContainer.tsx
  - frontend/src/components/features/profile/public/animations/GradientShiftBackground.tsx
  - frontend/src/components/features/profile/public/animations/ParticleBackground.tsx
  - frontend/src/components/features/profile/public/animations/WaveBackground.tsx
  - frontend/src/components/features/profile/public/animations/AuroraBackground.tsx
  - frontend/src/components/features/profile/public/animations/AnimatedBackgroundRenderer.tsx
  - frontend/src/hooks/useColorScheme.ts
  - frontend/src/constants/themes.ts
  - frontend/src/components/features/profile/public/__tests__/AnimatedBackgrounds.test.tsx
  - frontend/src/components/features/profile/__tests__/ProfileBentoGrid.test.tsx
autonomous: true
requirements: [PUBPG-01, PUBPG-02, PUBPG-03, PUBPG-04, PUBPG-06]
must_haves:
  truths:
    - "Personal profile (/u/:slug) renders responsive layout at 375px, 768px, 1024px, 1280px breakpoints"
    - "Company profile (/p/:slug) renders responsive layout at all breakpoints"
    - "Bento grid reflows from 1 column (mobile) to 2 (tablet) to 3 (desktop) to 4 (wide)"
    - "Each theme renders with an animated background (gradient-shift, particles, wave, or aurora)"
    - "Animations respect prefers-reduced-motion with static fallback"
    - "Dark mode activates automatically via prefers-color-scheme media query"
    - "Glass cards have proper backdrop-filter blur with mobile performance optimization"
  artifacts:
    - path: "frontend/src/components/features/profile/ProfileBentoGrid.tsx"
      provides: "Responsive 1->2->3->4 column grid"
      contains: "grid-cols-1"
    - path: "frontend/src/components/features/profile/public/animations/AnimatedBackgroundRenderer.tsx"
      provides: "Theme animation dispatcher"
      exports: ["AnimatedBackgroundRenderer"]
    - path: "frontend/src/hooks/useColorScheme.ts"
      provides: "Dark mode detection hook"
      exports: ["useColorScheme"]
  key_links:
    - from: "frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx"
      to: "AnimatedBackgroundRenderer"
      via: "wraps content in animated background based on theme animation_type"
      pattern: "AnimatedBackgroundRenderer"
    - from: "frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts"
      to: "constants/themes.ts"
      via: "reads animation_type from theme definition"
      pattern: "animation_type"
---

<objective>
Redesign the public profile frontend to be responsive, animated, and dark-mode aware.

Purpose: Transform both /u/:slug and /p/:slug pages from basic layouts into premium Linktree-level experiences with glassmorphism, animated theme backgrounds, responsive bento grid, and automatic dark mode.

Output: Enhanced ProfileBentoGrid (responsive breakpoints), 4 animated background components, AnimatedBackgroundRenderer dispatcher, useColorScheme hook, dark mode integration in PublicProfileRenderer, updated theme tokens with animation_type.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-public-page-redesign/11-CONTEXT.md
@.planning/phases/11-public-page-redesign/11-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts:
```typescript
export interface ThemeTokens {
  '--theme-bg': string;
  '--theme-surface': string;
  '--theme-text': string;
  '--theme-text-secondary': string;
  '--theme-accent': string;
  '--theme-primary': string;
  '--theme-border': string;
  '--theme-font-heading': string;
  '--theme-font-body': string;
  '--theme-radius': string;
  '--theme-shadow': string;
  '--theme-gradient': string;
}
export function resolveThemeTokens(themeId: string, prefersDark: boolean): ThemeTokens;
export function applyThemeToContainer(tokens: ThemeTokens, element?: HTMLElement): void;
export function removeThemeFromContainer(element?: HTMLElement): void;
```

From frontend/src/components/features/profile/shared/SectionRegistry.ts:
```typescript
export type ProfileType = 'personal' | 'company';
export interface SectionRegistryEntry {
  id: string;
  component: ComponentType<SectionProps>;
  supportedTypes: ProfileType[];
  requiredData: string[];
  gridSpan?: { cols: number; rows: number };
  order: number;
}
export function getSectionsForProfile(type: ProfileType, profileData: Record<string, unknown>): SectionRegistryEntry[];
```

From frontend/src/components/features/profile/ProfileGridItem.tsx:
```typescript
export interface ProfileGridItemProps {
  children: React.ReactNode;
  theme: ProfileTheme;
  colSpan?: 1 | 2 | 3 | 4 | 'full';
  rowSpan?: 1 | 2;
  className?: string;
  delay?: number;
}
```

From frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx:
```typescript
interface PublicProfileRendererProps {
  profileData: Record<string, unknown>;
  profileType: ProfileType;
  themeId?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Responsive Bento Grid + Stagger Animations + Dark Mode Hook</name>
  <files>
    frontend/src/components/features/profile/ProfileBentoGrid.tsx,
    frontend/src/components/features/profile/ProfileGridItem.tsx,
    frontend/src/hooks/useColorScheme.ts,
    frontend/src/components/features/profile/__tests__/ProfileBentoGrid.test.tsx
  </files>
  <read_first>
    frontend/src/components/features/profile/ProfileBentoGrid.tsx,
    frontend/src/components/features/profile/ProfileGridItem.tsx,
    frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx,
    frontend/src/components/features/profile/public/GlassContainer.tsx,
    frontend/src/lib/utils.ts
  </read_first>
  <behavior>
    - Test: ProfileBentoGrid renders with responsive grid classes (grid-cols-1 default, sm:grid-cols-2, lg:grid-cols-3, xl:grid-cols-4)
    - Test: ProfileBentoGrid applies responsive gap classes (gap-3, sm:gap-4, lg:gap-5, xl:gap-6)
    - Test: ProfileGridItem uses Framer Motion variants pattern (not individual initial/animate)
    - Test: ProfileGridItem applies hover:scale-[1.02] and hover:-translate-y-1 for lift effect
    - Test: useColorScheme returns 'light' by default and 'dark' when matchMedia matches
  </behavior>
  <action>
    1. Create `frontend/src/components/features/profile/__tests__/ProfileBentoGrid.test.tsx`:
       - Test responsive grid classes are present in rendered output
       - Test that motion.div wrapper is used for stagger container
       - Mock framer-motion for testability

    2. Create `frontend/src/hooks/useColorScheme.ts`:
       - Hook returns 'light' | 'dark' based on `window.matchMedia('(prefers-color-scheme: dark)')`
       - Initialize from matchMedia synchronously (no flash)
       - Listen for `change` events and update state
       - SSR-safe: return 'light' if `typeof window === 'undefined'`

    3. Enhance `frontend/src/components/features/profile/ProfileBentoGrid.tsx`:
       - Change grid classes from `grid-cols-1 md:grid-cols-4` to responsive cascade:
         `grid-cols-1` (375px+), `sm:grid-cols-2 sm:gap-4` (768px+), `lg:grid-cols-3 lg:gap-5` (1024px+), `xl:grid-cols-4 xl:gap-6` (1280px+)
       - Wrap children in `motion.div` with containerVariants for stagger:
         ```
         const containerVariants = {
           hidden: { opacity: 0 },
           visible: { opacity: 1, transition: { staggerChildren: 0.1, delayChildren: 0.2, when: "beforeChildren" } }
         };
         ```
       - Add padding responsive: `p-3 sm:p-4 lg:p-6`

    4. Enhance `frontend/src/components/features/profile/ProfileGridItem.tsx`:
       - Replace individual `initial`/`animate` props with `variants` pattern for parent-driven stagger:
         ```
         const itemVariants = {
           hidden: { opacity: 0, y: 20, scale: 0.95 },
           visible: { opacity: 1, y: 0, scale: 1, transition: { duration: 0.5, ease: [0.25, 0.46, 0.45, 0.94] } }
         };
         ```
       - Add hover effect classes: `hover:scale-[1.02] hover:-translate-y-1 transition-transform duration-300`
       - Add `motion-reduce:transition-none motion-reduce:hover:transform-none` for reduced motion
       - Remove the `delay` prop (stagger handles timing now)
       - Use CSS custom properties from theme tokens instead of theme object for surface/border/shadow:
         `style={{ backgroundColor: 'var(--theme-surface)', borderColor: 'var(--theme-border)', borderRadius: 'var(--theme-radius)' }}`
       - Add glassmorphism: `backdrop-blur-sm border border-white/20 dark:border-white/10`
       - Mobile blur reduction: `backdrop-blur-[8px] sm:backdrop-blur-[12px]`
  </action>
  <verify>
    <automated>cd frontend && pnpm test src/components/features/profile/__tests__/ProfileBentoGrid.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "grid-cols-1" frontend/src/components/features/profile/ProfileBentoGrid.tsx
    - grep -q "sm:grid-cols-2" frontend/src/components/features/profile/ProfileBentoGrid.tsx
    - grep -q "lg:grid-cols-3" frontend/src/components/features/profile/ProfileBentoGrid.tsx
    - grep -q "xl:grid-cols-4" frontend/src/components/features/profile/ProfileBentoGrid.tsx
    - grep -q "staggerChildren" frontend/src/components/features/profile/ProfileBentoGrid.tsx
    - grep -q "itemVariants" frontend/src/components/features/profile/ProfileGridItem.tsx
    - grep -q "hover:scale" frontend/src/components/features/profile/ProfileGridItem.tsx
    - grep -q "useColorScheme" frontend/src/hooks/useColorScheme.ts
    - grep -q "prefers-color-scheme" frontend/src/hooks/useColorScheme.ts
  </acceptance_criteria>
  <done>ProfileBentoGrid renders responsive 1->2->3->4 columns with stagger entrance. ProfileGridItem uses variants + hover lift + glassmorphism. useColorScheme hook detects system dark mode. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Animated Theme Backgrounds + Theme Engine Enhancement + Renderer Wiring</name>
  <files>
    frontend/src/components/features/profile/public/animations/GradientShiftBackground.tsx,
    frontend/src/components/features/profile/public/animations/ParticleBackground.tsx,
    frontend/src/components/features/profile/public/animations/WaveBackground.tsx,
    frontend/src/components/features/profile/public/animations/AuroraBackground.tsx,
    frontend/src/components/features/profile/public/animations/AnimatedBackgroundRenderer.tsx,
    frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts,
    frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx,
    frontend/src/components/features/profile/public/GlassContainer.tsx,
    frontend/src/constants/themes.ts,
    frontend/src/components/features/profile/public/__tests__/AnimatedBackgrounds.test.tsx
  </files>
  <read_first>
    frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts,
    frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx,
    frontend/src/components/features/profile/public/GlassContainer.tsx,
    frontend/src/constants/themes.ts,
    frontend/src/components/features/profile/shared/SectionRegistry.ts,
    frontend/src/hooks/useColorScheme.ts
  </read_first>
  <behavior>
    - Test: AnimatedBackgroundRenderer renders GradientShiftBackground when animationType='gradient-shift'
    - Test: AnimatedBackgroundRenderer renders ParticleBackground when animationType='particles'
    - Test: AnimatedBackgroundRenderer renders WaveBackground when animationType='wave'
    - Test: AnimatedBackgroundRenderer renders AuroraBackground when animationType='aurora'
    - Test: AnimatedBackgroundRenderer renders static gradient fallback when prefers-reduced-motion
    - Test: AnimatedBackgroundRenderer renders GlassContainer (default orbs) when animationType='none' or undefined
    - Test: resolveThemeTokens returns '--theme-animation-type' field
  </behavior>
  <action>
    1. Create test file `frontend/src/components/features/profile/public/__tests__/AnimatedBackgrounds.test.tsx`:
       - Test each animation type dispatches to correct component
       - Test reduced motion fallback renders static gradient div
       - Mock `useReducedMotion` from framer-motion

    2. Add `animation_type` field to `ThemeTokens` in `UnifiedThemeEngine.ts`:
       - Add `'--theme-animation-type': string;` to ThemeTokens interface
       - Add it to THEME_CSS_KEYS array
       - In `resolveThemeTokens`, read `animation_type` from theme object, default to 'gradient-shift'
       - Return `'--theme-animation-type': theme.animation_type ?? 'gradient-shift'`

    3. Add `animation_type` field to each theme in `frontend/src/constants/themes.ts`:
       - Add `animation_type?: 'gradient-shift' | 'particles' | 'wave' | 'aurora' | 'none'` to Theme type
       - Assign per theme: minimal themes -> 'gradient-shift', dark themes -> 'aurora', pastel themes -> 'particles', bold themes -> 'wave', cinematic themes -> 'gradient-shift'
       - Update the Theme type in `frontend/src/types/profileEditor.ts` if that is where Theme is defined

    4. Create 4 animated background components in `frontend/src/components/features/profile/public/animations/`:

       **GradientShiftBackground.tsx**: Pure CSS gradient animation using `background-size: 400% 400%` with `@keyframes gradient-shift` animating `background-position` from `0% 50%` to `100% 50%` over 15s. Uses `--theme-primary` and `--theme-accent` CSS vars for gradient stops. `will-change: background-position`. Renders children inside.

       **ParticleBackground.tsx**: 15-20 floating `motion.div` circles (4-8px diameter) with randomized positions, using Framer Motion `animate` with `y: [0, -30, 0]`, `x: [0, 15, 0]`, `opacity: [0.3, 0.7, 0.3]` on infinite loop with randomized duration (8-15s). Particles use `--theme-accent` with varying opacity. Absolute positioned, pointer-events-none, z-0.

       **WaveBackground.tsx**: SVG-based wave at bottom of container. 2-3 overlapping `<path>` elements with sinusoidal curves. Animate via CSS `@keyframes wave-shift` translating x position. Use `--theme-primary` and `--theme-accent` for wave fill colors with 20-30% opacity. Fixed position, bottom-0.

       **AuroraBackground.tsx**: 3 large gradient blurs (similar to existing GlassContainer orbs but with different animation). Use CSS `@keyframes aurora` with larger translate ranges (50-100px) and slower timing (20-30s). Colors from `--theme-primary`, `--theme-accent`. Mix-blend-mode: screen for dark, multiply for light.

    5. Create `AnimatedBackgroundRenderer.tsx`:
       ```typescript
       type AnimationType = 'gradient-shift' | 'particles' | 'wave' | 'aurora' | 'none';
       interface Props { animationType?: AnimationType; themeTokens: ThemeTokens; children: React.ReactNode; }
       ```
       - Import `useReducedMotion` from 'framer-motion'
       - If reduced motion: render static div with `background: themeTokens['--theme-gradient']` + children
       - Switch on animationType to render correct background component wrapping children
       - Default ('none'/undefined): render existing `GlassContainer` (orb animation)

    6. Update `PublicProfileRenderer.tsx`:
       - Import `useColorScheme` from `@/hooks/useColorScheme`
       - Import `AnimatedBackgroundRenderer` from animations
       - Replace static `useMemo` for `prefersDark` with `useColorScheme()` hook (reactive to changes)
       - Read `'--theme-animation-type'` from resolved tokens
       - Wrap the content div with `<AnimatedBackgroundRenderer animationType={animType} themeTokens={tokens}>`
       - Add `dark:` variant classes for text contrast

    7. Enhance `GlassContainer.tsx`:
       - Add `prefers-color-scheme: dark` responsive classes using Tailwind `dark:` prefix
       - Ensure blur values scale down on mobile: blur-[60px] on mobile, blur-[80px] on sm, blur-[100px] on lg (already present, verify)
       - Add `color-scheme: light dark` to root style
  </action>
  <verify>
    <automated>cd frontend && pnpm test src/components/features/profile/public/__tests__/AnimatedBackgrounds.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "AnimatedBackgroundRenderer" frontend/src/components/features/profile/public/animations/AnimatedBackgroundRenderer.tsx
    - grep -q "gradient-shift" frontend/src/components/features/profile/public/animations/GradientShiftBackground.tsx
    - grep -q "useReducedMotion" frontend/src/components/features/profile/public/animations/AnimatedBackgroundRenderer.tsx
    - grep -q "animation_type" frontend/src/components/features/profile/shared/UnifiedThemeEngine.ts
    - grep -q "AnimatedBackgroundRenderer" frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx
    - grep -q "useColorScheme" frontend/src/components/features/profile/shared/PublicProfileRenderer.tsx
    - grep -q "animation_type" frontend/src/constants/themes.ts
  </acceptance_criteria>
  <done>4 animated background components created. AnimatedBackgroundRenderer dispatches by theme animation_type with reduced motion fallback. UnifiedThemeEngine resolves animation_type. PublicProfileRenderer wraps content in animated background and uses reactive dark mode detection. All animated background tests pass.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm test src/components/features/profile --run` -- all profile tests pass
- `cd frontend && pnpm lint` -- no lint errors
- Both `/u/:slug` and `/p/:slug` pages render without console errors
- Bento grid reflows at each breakpoint (visual check in browser devtools responsive mode)
</verification>

<success_criteria>
- ProfileBentoGrid shows 1/2/3/4 columns at 375/768/1024/1280px breakpoints
- ProfileGridItem uses stagger variants with hover lift effect
- 4 animated backgrounds render per theme type with reduced motion fallback
- Dark mode auto-switches via prefers-color-scheme
- All profile component tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-public-page-redesign/11-01-SUMMARY.md`
</output>
