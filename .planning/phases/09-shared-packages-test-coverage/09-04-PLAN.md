---
phase: 09-shared-packages-test-coverage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/test_ai_concurrency.py
  - backend/tests/security/test_permission_checks.py
autonomous: true
requirements:
  - TEST-04
  - TEST-07

must_haves:
  truths:
    - "AI worker concurrency tests verify parallel embedding and clustering do not corrupt data"
    - "Security enforcement tests verify permission checks, workspace isolation, and timing-safe comparison"
  artifacts:
    - path: "backend/tests/integration/test_ai_concurrency.py"
      provides: "AI worker concurrency tests"
      min_lines: 60
    - path: "backend/tests/security/test_permission_checks.py"
      provides: "Security permission enforcement tests"
      min_lines: 60
  key_links:
    - from: "backend/tests/integration/test_ai_concurrency.py"
      to: "backend/src/app/workers/similarity_worker.py"
      via: "import and invoke worker functions"
      pattern: "similarity_worker|embedding|clustering"
    - from: "backend/tests/security/test_permission_checks.py"
      to: "backend/src/app/middleware/"
      via: "testing middleware enforcement"
      pattern: "permission|role|workspace_id"
---

<objective>
Write backend tests for AI worker concurrency (CLIP embedding batch processing, clustering race conditions) and security enforcement (permission checks, workspace isolation, timing-safe comparison).

Purpose: AI workers process photos in parallel and must not corrupt shared state. Security enforcement tests validate that RBAC and tenant isolation hold under all conditions.
Output: Two new test files covering AI concurrency and security permission enforcement.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-shared-packages-test-coverage/09-RESEARCH.md
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AI worker concurrency tests (CLIP embedding, clustering)</name>
  <read_first>
    - backend/tests/conftest.py
    - backend/src/app/workers/similarity_worker.py (or wherever CLIP embedding task lives)
    - backend/tests/test_similarity_worker.py (existing test patterns)
    - backend/tests/test_clustering_integration.py (existing clustering test patterns)
    - backend/tests/test_embedding_repository.py (existing embedding test patterns)
  </read_first>
  <files>backend/tests/integration/test_ai_concurrency.py</files>
  <behavior>
    - test_parallel_embedding_tasks_no_data_corruption: Two concurrent embedding tasks for different photos produce correct, non-mixed embeddings
    - test_batch_embedding_partial_failure: A batch of 5 images where 1 fails still produces embeddings for the other 4
    - test_concurrent_duplicate_detection: Two simultaneous duplicate detection runs for different workspaces do not cross-contaminate results
    - test_clustering_idempotent: Running clustering twice on the same dataset produces the same groups
    - test_embedding_task_retry_on_transient_failure: Embedding task retries on transient error (e.g., HTTP timeout) and succeeds on second attempt
  </behavior>
  <action>
    Create backend/tests/integration/test_ai_concurrency.py.

    Steps:
    1. Read similarity_worker.py to understand the Celery task signatures and how embeddings are computed/stored
    2. Read existing test_similarity_worker.py and test_clustering_integration.py for mock patterns
    3. Write concurrency tests using asyncio.gather (for async) or threading (for sync Celery tasks) to simulate parallel execution
    4. Mock the CLIP model HTTP call (to ai-processing-service) to return deterministic embeddings based on input image ID
    5. Mock the database/repository layer to use an in-memory dict, allowing assertions on final state
    6. For batch partial failure: mock one image's HTTP call to raise, verify others succeed
    7. For idempotency: run clustering twice with same mock embeddings, assert same group assignments

    Key mocking targets:
    - HTTP client to ai-processing-service (return deterministic 512-dim vectors)
    - EmbeddingRepository (use dict-backed mock)
    - R2 storage client (return dummy image bytes)
    - Redis cache (use dict-backed mock)

    Do NOT test actual CLIP model inference -- these are concurrency/integration tests, not ML tests.
  </action>
  <verify>
    <automated>docker exec rawdrive-backend pytest tests/integration/test_ai_concurrency.py -x -v 2>/dev/null || cd C:/Users/admin/Desktop/RawDrive2/backend && python -m pytest tests/integration/test_ai_concurrency.py -x -v 2>/dev/null || echo "Run pytest manually"</automated>
  </verify>
  <acceptance_criteria>
    - test_ai_concurrency.py contains at least 4 test functions
    - Tests mock CLIP HTTP calls (no real model inference)
    - grep -c "async def test_\|def test_" backend/tests/integration/test_ai_concurrency.py returns >= 4
    - All tests pass
  </acceptance_criteria>
  <done>AI worker concurrency tests verify parallel embedding, batch partial failure, concurrent duplicate detection, and clustering idempotency</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Security enforcement tests (permissions, workspace isolation, timing-safe)</name>
  <read_first>
    - backend/tests/conftest.py
    - backend/tests/security/test_a2a_timing_safe.py (existing timing-safe pattern)
    - backend/tests/security/test_comment_workspace_isolation.py (existing isolation pattern)
    - backend/src/app/middleware/auth.py (or auth middleware location)
    - backend/src/app/api/v1/auth.py (for RBAC decorator patterns)
  </read_first>
  <files>backend/tests/security/test_permission_checks.py</files>
  <behavior>
    - test_unauthenticated_request_returns_401: Request without JWT to protected endpoint returns 401
    - test_expired_token_returns_401: Request with expired JWT returns 401
    - test_invalid_token_returns_401: Request with malformed JWT returns 401
    - test_viewer_cannot_delete: User with viewer role cannot access DELETE endpoints (returns 403)
    - test_viewer_can_read: User with viewer role CAN access GET endpoints (returns 200)
    - test_admin_can_delete: User with admin role CAN access DELETE endpoints (returns 200)
    - test_cross_workspace_access_denied: User from workspace A accessing workspace B resource gets 403 or empty result
    - test_a2a_key_timing_safe_verification: Verifies hmac.compare_digest is used (extends existing test if needed)
  </behavior>
  <action>
    Create backend/tests/security/test_permission_checks.py.

    Steps:
    1. Read the auth middleware to understand how JWT validation works, what roles exist, and how permissions are checked
    2. Read existing security tests for patterns (test_a2a_timing_safe.py, test_comment_workspace_isolation.py)
    3. Create helper fixtures:
       - `expired_token_headers`: JWT with exp in the past
       - `viewer_headers`: JWT with viewer role
       - `admin_headers`: JWT with admin role
       - `workspace_a_headers` / `workspace_b_headers`: JWTs for different workspaces
    4. Test authentication: no token, expired token, malformed token all return 401
    5. Test authorization: viewer vs admin role on DELETE endpoints (pick a real endpoint like DELETE /api/v1/galleries/{id})
    6. Test workspace isolation: request with workspace A token accessing workspace B data returns 403 or empty
    7. For timing-safe: if existing test_a2a_timing_safe.py already covers this thoroughly, reference it in a comment. Otherwise add a test that patches hmac.compare_digest to track calls and verifies it IS called during A2A key validation.

    Use the httpx AsyncClient for all endpoint tests. Mock only the repository/service layer, not the auth middleware itself (we're testing the middleware works correctly).
  </action>
  <verify>
    <automated>docker exec rawdrive-backend pytest tests/security/test_permission_checks.py -x -v 2>/dev/null || cd C:/Users/admin/Desktop/RawDrive2/backend && python -m pytest tests/security/test_permission_checks.py -x -v 2>/dev/null || echo "Run pytest manually"</automated>
  </verify>
  <acceptance_criteria>
    - test_permission_checks.py contains at least 6 test functions
    - Tests cover authentication (401), authorization (403), and workspace isolation
    - grep -c "async def test_\|def test_" backend/tests/security/test_permission_checks.py returns >= 6
    - All tests pass
  </acceptance_criteria>
  <done>Security enforcement tests verify unauthenticated/expired/invalid tokens return 401, role-based access works, and cross-workspace access is denied</done>
</task>

</tasks>

<verification>
Run all security and AI concurrency tests:
```bash
docker exec rawdrive-backend pytest tests/integration/test_ai_concurrency.py tests/security/test_permission_checks.py tests/security/ -v
```
All new and existing security tests should pass.
</verification>

<success_criteria>
- 2 new test files created
- test_ai_concurrency.py has >= 4 tests covering parallel execution scenarios
- test_permission_checks.py has >= 6 tests covering auth, RBAC, and isolation
- All tests pass
- Existing security tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/09-shared-packages-test-coverage/09-04-SUMMARY.md`
</output>
