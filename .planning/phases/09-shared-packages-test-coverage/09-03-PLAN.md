---
phase: 09-shared-packages-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/features/upload/__tests__/UploadDropzone.test.tsx
  - frontend/src/components/features/upload/__tests__/UploadQueue.test.tsx
  - frontend/src/components/features/upload/__tests__/UploadProgressPanel.test.tsx
  - frontend/src/pages/public/__tests__/SignInPage.test.tsx
  - frontend/src/pages/public/__tests__/SignUpPage.test.tsx
  - frontend/src/pages/public/__tests__/ForgotPasswordPage.test.tsx
autonomous: true
requirements:
  - TEST-05
  - TEST-06

must_haves:
  truths:
    - "Upload component tests verify dropzone rendering, queue display, and progress tracking"
    - "Auth page tests verify sign-in, sign-up, and forgot-password form rendering and validation"
  artifacts:
    - path: "frontend/src/components/features/upload/__tests__/UploadDropzone.test.tsx"
      provides: "Upload dropzone component tests"
      min_lines: 30
    - path: "frontend/src/components/features/upload/__tests__/UploadQueue.test.tsx"
      provides: "Upload queue component tests"
      min_lines: 30
    - path: "frontend/src/pages/public/__tests__/SignInPage.test.tsx"
      provides: "Sign-in page component tests"
      min_lines: 40
    - path: "frontend/src/pages/public/__tests__/SignUpPage.test.tsx"
      provides: "Sign-up page component tests"
      min_lines: 40
    - path: "frontend/src/pages/public/__tests__/ForgotPasswordPage.test.tsx"
      provides: "Forgot password page component tests"
      min_lines: 30
  key_links:
    - from: "frontend/src/pages/public/__tests__/SignInPage.test.tsx"
      to: "frontend/src/pages/public/SignInPage.tsx"
      via: "import and render"
      pattern: "import.*SignInPage"
    - from: "frontend/src/components/features/upload/__tests__/UploadDropzone.test.tsx"
      to: "frontend/src/components/features/upload/UploadDropzone.tsx"
      via: "import and render"
      pattern: "import.*UploadDropzone"
---

<objective>
Write frontend component tests for gallery upload workflows (UploadDropzone, UploadQueue, UploadProgressPanel) and auth pages (SignIn, SignUp, ForgotPassword).

Purpose: These are zero-test components that handle critical user-facing flows. Tests catch rendering regressions and validate form behavior.
Output: 6 new test files covering upload and auth page components.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-shared-packages-test-coverage/09-RESEARCH.md
@frontend/src/test/setup.ts
@frontend/vite.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upload component tests (UploadDropzone, UploadQueue, UploadProgressPanel)</name>
  <read_first>
    - frontend/src/components/features/upload/UploadDropzone.tsx
    - frontend/src/components/features/upload/UploadQueue.tsx
    - frontend/src/components/features/upload/UploadProgressPanel.tsx
    - frontend/src/test/setup.ts
    - frontend/vite.config.ts (test section only)
  </read_first>
  <files>
    frontend/src/components/features/upload/__tests__/UploadDropzone.test.tsx
    frontend/src/components/features/upload/__tests__/UploadQueue.test.tsx
    frontend/src/components/features/upload/__tests__/UploadProgressPanel.test.tsx
  </files>
  <behavior>
    UploadDropzone:
    - Renders drop area with drag-and-drop instructions text
    - Shows file input for manual file selection
    - Accepts image file types (jpg, png, raw, etc.)

    UploadQueue:
    - Renders list of queued files with names
    - Shows upload status per file (pending, uploading, complete, error)
    - Displays file count or empty state

    UploadProgressPanel:
    - Shows overall upload progress (percentage or bar)
    - Displays count of completed vs total files
    - Shows error count if any uploads failed
  </behavior>
  <action>
    Create __tests__/ directory under upload/ and add 3 test files.

    For EACH component:
    1. Read the component source to understand its props interface, hooks used, and rendered output
    2. Identify all external dependencies to mock (hooks like useUpload, services, react-router-dom, contexts)
    3. Write tests following existing gallery test patterns (see frontend/src/components/features/gallery/__tests__/ for reference if needed)

    Key patterns:
    - Use `vi.mock()` for hooks and services the components depend on
    - Use `render()` from @testing-library/react
    - Use `screen.getByText()`, `screen.getByRole()` for assertions
    - Wrap in MemoryRouter if component uses routing hooks
    - Wrap in QueryClientProvider if component uses react-query
    - Use `userEvent.setup()` for interaction tests (drag/drop, click)

    Test BEHAVIOR not implementation: assert on visible text, roles, and user-visible state changes. Do NOT assert on internal state or implementation details.

    If a component is too tightly coupled to test (e.g., all logic in parent), test what IS renderable and add a comment noting the coupling.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Desktop/RawDrive2/frontend && pnpm test src/components/features/upload/__tests__/ --run 2>/dev/null || echo "Check test output"</automated>
  </verify>
  <acceptance_criteria>
    - 3 test files exist in frontend/src/components/features/upload/__tests__/
    - Each file has at least 2 test cases
    - grep -r "describe(" frontend/src/components/features/upload/__tests__/ returns 3 matches
    - All tests pass with pnpm test
  </acceptance_criteria>
  <done>Upload component tests cover dropzone rendering, queue display, and progress tracking with all tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auth page component tests (SignIn, SignUp, ForgotPassword)</name>
  <read_first>
    - frontend/src/pages/public/SignInPage.tsx
    - frontend/src/pages/public/SignUpPage.tsx
    - frontend/src/pages/public/ForgotPasswordPage.tsx
    - frontend/src/test/setup.ts
  </read_first>
  <files>
    frontend/src/pages/public/__tests__/SignInPage.test.tsx
    frontend/src/pages/public/__tests__/SignUpPage.test.tsx
    frontend/src/pages/public/__tests__/ForgotPasswordPage.test.tsx
  </files>
  <behavior>
    SignInPage:
    - Renders email and password input fields
    - Renders a "Sign In" submit button
    - Shows validation error when submitting empty form
    - Shows error message on invalid credentials (mocked API failure)
    - Navigates to sign-up page via link

    SignUpPage:
    - Renders name, email, password fields
    - Renders a "Sign Up" submit button
    - Shows validation errors for invalid email format
    - Shows validation errors for weak password
    - Navigates to sign-in page via link

    ForgotPasswordPage:
    - Renders email input field
    - Renders a submit button
    - Shows success message after submission (mocked API success)
    - Shows error for invalid email format
  </behavior>
  <action>
    Create __tests__/ directory under pages/public/ and add 3 test files.

    For EACH page component:
    1. Read the page source to understand form fields, validation, API calls, and navigation
    2. Mock all API service calls (vi.mock the service module)
    3. Mock react-router-dom (useNavigate, Link)
    4. Wrap rendering in providers: MemoryRouter + QueryClientProvider (create a shared renderWithProviders helper at the top of each file or in a test-utils file)

    Test patterns:
    - Form field presence: `screen.getByLabelText(/email/i)` or `screen.getByPlaceholderText(/email/i)` -- read component to see if labels or placeholders are used
    - Submit button: `screen.getByRole('button', { name: /sign in/i })`
    - Validation: `await user.click(submitButton)` then `await waitFor(() => expect(screen.getByText(/required|invalid/i)))`
    - API error: Mock service to reject, submit form, assert error message appears
    - Navigation: Assert Link component renders with correct `to` prop

    IMPORTANT: Read each page component FIRST. Form field names, validation library (zod? react-hook-form?), and API service imports vary. Do NOT assume field names from research -- verify from source.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Desktop/RawDrive2/frontend && pnpm test src/pages/public/__tests__/ --run 2>/dev/null || echo "Check test output"</automated>
  </verify>
  <acceptance_criteria>
    - 3 test files exist in frontend/src/pages/public/__tests__/
    - Each file has at least 3 test cases
    - grep -r "describe(" frontend/src/pages/public/__tests__/ returns 3 matches
    - All tests pass with pnpm test
  </acceptance_criteria>
  <done>Auth page tests cover form rendering, validation, error handling, and navigation for SignIn, SignUp, and ForgotPassword</done>
</task>

</tasks>

<verification>
Run all frontend tests to ensure no regressions:
```bash
cd C:/Users/admin/Desktop/RawDrive2/frontend && pnpm test --run
```
All new and existing tests should pass.
</verification>

<success_criteria>
- 6 new test files created (3 upload + 3 auth pages)
- Total test count >= 15 across all 6 files
- All tests pass with `pnpm test --run`
- No existing tests broken by new test additions
</success_criteria>

<output>
After completion, create `.planning/phases/09-shared-packages-test-coverage/09-03-SUMMARY.md`
</output>
