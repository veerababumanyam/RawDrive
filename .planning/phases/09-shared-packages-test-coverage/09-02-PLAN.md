---
phase: 09-shared-packages-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/test_auth_flows.py
  - backend/tests/integration/test_multi_tenant.py
  - backend/tests/integration/test_email_integration.py
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "Auth flow integration tests verify login, signup, token refresh, and logout"
    - "Multi-tenant isolation tests prove workspace_id enforcement across endpoints"
    - "Email integration tests verify verification, reset, and invitation email sending"
  artifacts:
    - path: "backend/tests/integration/test_auth_flows.py"
      provides: "Auth flow integration tests"
      min_lines: 80
    - path: "backend/tests/integration/test_multi_tenant.py"
      provides: "Multi-tenant isolation tests"
      min_lines: 60
    - path: "backend/tests/integration/test_email_integration.py"
      provides: "Email sending integration tests"
      min_lines: 60
  key_links:
    - from: "backend/tests/integration/test_auth_flows.py"
      to: "backend/src/app/api/v1/auth.py"
      via: "httpx AsyncClient POST/GET to auth endpoints"
      pattern: "client\\.(post|get).*auth"
    - from: "backend/tests/integration/test_multi_tenant.py"
      to: "backend/src/app/middleware/"
      via: "workspace_id JWT extraction and query filtering"
      pattern: "workspace_id"
    - from: "backend/tests/integration/test_email_integration.py"
      to: "backend/src/app/services/email_service.py"
      via: "mocked EmailService send calls"
      pattern: "email.*send|send.*email"
---

<objective>
Write backend integration tests covering auth flows, multi-tenant isolation, and email sending for all critical paths built in Phases 1-8.

Purpose: These tests validate that the core security and communication features work end-to-end through the FastAPI ASGI app, catching regressions before deployment.
Output: Three new test files in backend/tests/integration/ that pass via pytest in Docker.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-shared-packages-test-coverage/09-RESEARCH.md
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auth flow integration tests (login, signup, token refresh, logout)</name>
  <read_first>
    - backend/tests/conftest.py
    - backend/src/app/api/v1/auth.py (first 80 lines for endpoint signatures)
    - backend/tests/test_session_service.py (for existing patterns)
  </read_first>
  <files>backend/tests/integration/test_auth_flows.py</files>
  <behavior>
    - test_login_valid_credentials: POST /api/v1/auth/login with valid email+password returns 200 with access_token and refresh_token
    - test_login_invalid_password: POST /api/v1/auth/login with wrong password returns 401
    - test_login_nonexistent_user: POST /api/v1/auth/login with unknown email returns 401 (not 404, to avoid user enumeration)
    - test_signup_creates_user: POST /api/v1/auth/register with valid data returns 201 and user object
    - test_signup_duplicate_email: POST /api/v1/auth/register with existing email returns 409
    - test_token_refresh: POST /api/v1/auth/refresh with valid refresh_token returns new access_token
    - test_token_refresh_invalid: POST /api/v1/auth/refresh with invalid token returns 401
    - test_logout_invalidates_session: POST /api/v1/auth/logout clears session, subsequent auth calls fail
  </behavior>
  <action>
    Create backend/tests/integration/test_auth_flows.py using the existing conftest.py fixtures (client, mock_db, etc.).

    Pattern to follow:
    - Use `@pytest.mark.asyncio` on all test functions
    - Use the `client` fixture (httpx AsyncClient with ASGITransport)
    - Mock the repository layer (UserRepository) to return controlled user objects
    - Mock password hashing to avoid bcrypt overhead in tests
    - Mock JWT creation/validation to control token values
    - Test the FULL request/response cycle through FastAPI, not just service methods

    Read the auth.py router first to get exact endpoint paths and request body schemas. Adapt test payloads to match actual schemas (field names may differ from research examples).

    Each test should be self-contained with its own mock setup. Use `unittest.mock.patch` or `pytest.monkeypatch` for mocking.
  </action>
  <verify>
    <automated>docker exec rawdrive-backend pytest tests/integration/test_auth_flows.py -x -v 2>/dev/null || cd C:/Users/admin/Desktop/RawDrive2/backend && python -m pytest tests/integration/test_auth_flows.py -x -v 2>/dev/null || echo "Run pytest manually in Docker or locally"</automated>
  </verify>
  <acceptance_criteria>
    - test_auth_flows.py contains at least 6 test functions
    - All tests use httpx AsyncClient (not raw function calls)
    - Tests cover login success, login failure, signup, token refresh, and logout
    - grep -c "async def test_" backend/tests/integration/test_auth_flows.py returns >= 6
  </acceptance_criteria>
  <done>Auth flow integration tests pass, covering login/signup/refresh/logout with both success and failure cases</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-tenant isolation + email integration tests</name>
  <read_first>
    - backend/tests/conftest.py
    - backend/tests/integration/test_workspace_authorization.py (existing pattern for workspace tests)
    - backend/tests/integration/test_rsvp_workspace_isolation.py (existing pattern)
    - backend/tests/test_email_verification.py (existing email test pattern)
    - backend/tests/test_invitation_emails.py (existing email test pattern)
  </read_first>
  <files>
    backend/tests/integration/test_multi_tenant.py
    backend/tests/integration/test_email_integration.py
  </files>
  <behavior>
    Multi-tenant (test_multi_tenant.py):
    - test_gallery_workspace_isolation: Galleries from workspace A are NOT visible to workspace B user
    - test_asset_workspace_isolation: Assets created in workspace A cannot be accessed from workspace B
    - test_comment_workspace_isolation: Comments are filtered by workspace_id
    - test_workspace_id_from_jwt_not_body: Server extracts workspace_id from JWT, ignoring any client-provided value

    Email integration (test_email_integration.py):
    - test_verification_email_sent_on_signup: Signup triggers email_service.send with verification template
    - test_password_reset_email_sent: Password reset request triggers email with reset token link
    - test_invitation_email_sent: Creating invitation triggers email to invitee
    - test_email_service_failure_non_blocking: Email send failure does not crash the endpoint (graceful degradation)
  </behavior>
  <action>
    Create two test files:

    **test_multi_tenant.py:**
    - Read existing test_workspace_authorization.py and test_rsvp_workspace_isolation.py to understand the workspace mocking pattern
    - Create fixtures for two workspaces (workspace_a_headers, workspace_b_headers) with different JWT workspace_id claims
    - Test that GET endpoints for galleries, assets, and comments filter by workspace_id
    - Test that the server ignores workspace_id in request body and uses JWT instead
    - Mock repositories to return workspace-specific data

    **test_email_integration.py:**
    - Read existing test_email_verification.py and test_invitation_emails.py for patterns
    - If existing tests already cover the requirements well, consolidate and extend them
    - If gaps exist (e.g., no test for email failure graceful degradation), add those tests
    - Mock EmailService.send to capture calls without actually sending
    - Verify correct template names and recipient addresses in mock calls
    - Test that endpoint returns success even when email sending fails (mock raising exception)
  </action>
  <verify>
    <automated>docker exec rawdrive-backend pytest tests/integration/test_multi_tenant.py tests/integration/test_email_integration.py -x -v 2>/dev/null || cd C:/Users/admin/Desktop/RawDrive2/backend && python -m pytest tests/integration/test_multi_tenant.py tests/integration/test_email_integration.py -x -v 2>/dev/null || echo "Run pytest manually"</automated>
  </verify>
  <acceptance_criteria>
    - test_multi_tenant.py contains at least 3 test functions with workspace isolation assertions
    - test_email_integration.py contains at least 3 test functions covering verification, reset, and invitation emails
    - grep -c "workspace_id" backend/tests/integration/test_multi_tenant.py returns >= 3
    - grep -c "email" backend/tests/integration/test_email_integration.py returns >= 5
  </acceptance_criteria>
  <done>Multi-tenant isolation tests prove workspace boundary enforcement; email integration tests verify all transactional email triggers</done>
</task>

</tasks>

<verification>
Run all new integration tests together:
```bash
docker exec rawdrive-backend pytest tests/integration/test_auth_flows.py tests/integration/test_multi_tenant.py tests/integration/test_email_integration.py -v
```
All tests should pass.
</verification>

<success_criteria>
- 3 new test files exist in backend/tests/integration/
- Combined test count >= 14 (6 auth + 4 tenant + 4 email)
- All tests pass via pytest
- Tests use httpx AsyncClient pattern (not direct service calls)
</success_criteria>

<output>
After completion, create `.planning/phases/09-shared-packages-test-coverage/09-02-SUMMARY.md`
</output>
