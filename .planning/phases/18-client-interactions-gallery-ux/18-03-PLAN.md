---
phase: 18-client-interactions-gallery-ux
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/features/gallery/AIToolTooltip.tsx
  - frontend/src/components/features/gallery/AIToolTooltip.test.tsx
  - frontend/src/components/features/gallery/GalleryToolbar.tsx
  - frontend/src/components/features/gallery/BulkActionBar.tsx
  - frontend/src/components/features/gallery/BulkActionBar.test.tsx
  - frontend/src/components/features/gallery/GallerySettingsPresets.tsx
  - frontend/src/components/features/gallery/GallerySettingsPresets.test.tsx
  - frontend/src/components/features/gallery/GallerySettingsPanel.tsx
  - frontend/src/components/features/gallery/AIProcessingStatus.tsx
  - frontend/src/components/features/gallery/AIProcessingStatus.test.tsx
  - frontend/src/components/features/gallery/SubGalleryPermissionBadge.tsx
  - frontend/src/components/features/gallery/SubGalleryPermissionBadge.test.tsx
autonomous: true
requirements:
  - GALUX-01
  - GALUX-02
  - GALUX-03
  - GALUX-04
  - GALUX-05

must_haves:
  truths:
    - "Each AI tool in gallery toolbar shows tooltip with 1-2 sentence description on hover"
    - "Selecting one or more photos shows a sticky bulk action toolbar at the bottom of the screen"
    - "Gallery settings panel offers one-click presets that configure access, downloads, and watermark together"
    - "AI processing shows progress bar with estimated completion time and per-photo status"
    - "Sub-galleries show a badge indicating permission inheritance with an override toggle"
  artifacts:
    - path: "frontend/src/components/features/gallery/AIToolTooltip.tsx"
      provides: "Tooltip wrapper for AI toolbar buttons"
      exports: ["AIToolTooltip"]
    - path: "frontend/src/components/features/gallery/GallerySettingsPresets.tsx"
      provides: "One-click settings preset buttons"
      exports: ["GallerySettingsPresets"]
    - path: "frontend/src/components/features/gallery/AIProcessingStatus.tsx"
      provides: "AI processing progress bar with per-photo status"
      exports: ["AIProcessingStatus"]
    - path: "frontend/src/components/features/gallery/SubGalleryPermissionBadge.tsx"
      provides: "Permission inheritance badge with override toggle"
      exports: ["SubGalleryPermissionBadge"]
  key_links:
    - from: "frontend/src/components/features/gallery/AIToolTooltip.tsx"
      to: "frontend/src/components/features/gallery/GalleryToolbar.tsx"
      via: "Wraps each AI action button in GalleryToolbar"
      pattern: "AIToolTooltip"
    - from: "frontend/src/components/features/gallery/GallerySettingsPresets.tsx"
      to: "frontend/src/components/features/gallery/GallerySettingsPanel.tsx"
      via: "Rendered at top of settings panel"
      pattern: "GallerySettingsPresets"
    - from: "frontend/src/components/features/gallery/BulkActionBar.tsx"
      to: "frontend/src/components/features/gallery/GalleryToolbar.tsx"
      via: "Shown when selectedAssetIds.size >= 1"
      pattern: "selectedAssetIds.*size"
---

<objective>
Implement five gallery management UX improvements: AI tooltips, enhanced bulk action toolbar, one-click settings presets, AI processing progress indicator, and sub-gallery permission badges.

Purpose: Improve gallery management experience for photographers — discoverable AI tools, efficient bulk operations, quick gallery configuration, and clear permission visibility.
Output: Five new/enhanced components integrated into existing gallery management views.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/features/gallery/GalleryToolbar.tsx
@frontend/src/components/features/gallery/BulkActionBar.tsx
@frontend/src/components/features/gallery/GallerySettingsPanel.tsx
@frontend/src/components/features/gallery/AISettings.tsx
@frontend/src/components/features/gallery/SubGallerySelector.tsx

<interfaces>
<!-- Key types the executor needs. -->

From frontend/src/components/features/gallery/BulkActionBar.tsx:
```typescript
export interface BulkActionBarProps {
  selectedAssetIds: Set<string>;
  // existing props for delete, move, download handlers
}
export const BulkActionBar: React.FC<BulkActionBarProps>;
```

From frontend/src/components/features/gallery/GallerySettingsPanel.tsx:
```typescript
export interface GallerySettingsPanelProps {
  // gallery settings props
}
// Sections: 'general' | 'access' | 'downloads' | 'branding' | 'interactions' | 'watermark' | 'findme' | 'slideshow' | 'notifications' | 'ai'
```

From frontend/src/components/features/gallery/GalleryToolbar.tsx:
```typescript
export interface GalleryToolbarProps {
  // toolbar config props including AI actions
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AIToolTooltip + enhanced BulkActionBar + AIProcessingStatus</name>
  <files>
    frontend/src/components/features/gallery/AIToolTooltip.tsx,
    frontend/src/components/features/gallery/AIToolTooltip.test.tsx,
    frontend/src/components/features/gallery/GalleryToolbar.tsx,
    frontend/src/components/features/gallery/BulkActionBar.tsx,
    frontend/src/components/features/gallery/__tests__/BulkActions.test.tsx,
    frontend/src/components/features/gallery/AIProcessingStatus.tsx,
    frontend/src/components/features/gallery/AIProcessingStatus.test.tsx
  </files>
  <read_first>
    frontend/src/components/features/gallery/GalleryToolbar.tsx,
    frontend/src/components/features/gallery/BulkActionBar.tsx,
    frontend/src/components/features/gallery/__tests__/BulkActions.test.tsx,
    frontend/src/components/features/gallery/AISettings.tsx
  </read_first>
  <behavior>
    - AIToolTooltip shows description on hover (desktop) with 200ms delay
    - AIToolTooltip shows description on long-press (mobile, 500ms hold)
    - Tooltip appears above/below button depending on viewport position
    - BulkActionBar is sticky at bottom when >= 1 photo selected (currently may require more)
    - BulkActionBar shows count badge and batch edit/tag/download/delete buttons
    - AIProcessingStatus shows per-photo progress bar with estimated time remaining
    - AIProcessingStatus replaces generic "Some analyses failed" with per-photo status icons
  </behavior>
  <action>
    1. Create AIToolTooltip.tsx:
       - Props: { label: string; description: string; children: React.ReactNode; position?: 'top' | 'bottom' }
       - Desktop: onMouseEnter with 200ms setTimeout, onMouseLeave clears timeout and hides
       - Mobile: onTouchStart starts 500ms timer, onTouchEnd/onTouchMove cancels
       - Tooltip UI: absolute positioned div with bg-gray-900 text-white text-xs rounded-lg px-3 py-2 shadow-lg max-w-xs
       - Arrow indicator pointing to the button
       - z-index high enough to float above gallery content
       - Define AI tool descriptions as a constant map: { auto_tag: "Automatically tags photos...", face_detect: "Identifies faces...", auto_cull: "Removes blurry...", smart_album: "Groups photos..." } etc.

    2. Wrap each AI action button in GalleryToolbar.tsx with AIToolTooltip:
       - Read existing AI action buttons in the toolbar
       - Wrap each with <AIToolTooltip label={toolName} description={AI_DESCRIPTIONS[toolKey]}>

    3. Enhance BulkActionBar.tsx:
       - Make sticky: add `fixed bottom-0 left-0 right-0 z-40` (or sticky bottom-0 within scroll container)
       - Show when selectedAssetIds.size >= 1 (lower threshold from current behavior if needed)
       - Add batch edit and tag buttons alongside existing delete/move/download
       - Add slide-up animation via framer-motion (y: 100 -> y: 0)
       - Show selected count badge: "{N} selected" pill
       - Confirmation dialog for destructive actions (delete) — simple confirm modal

    4. Create AIProcessingStatus.tsx:
       - Props: { galleryId: string; totalPhotos: number; processedCount: number; failedCount: number; status: 'idle' | 'processing' | 'complete' | 'partial_failure' }
       - Progress bar: processedCount / totalPhotos with percentage label
       - Estimated time: rough calculation based on elapsed time and remaining count
       - Per-photo status: list of recently processed photos with check/X/spinner icons
       - Replace "Some analyses failed" generic message with: "{failedCount} of {totalPhotos} failed — click to retry"
       - Collapsible detail view showing individual photo statuses
       - Wire into GalleryToolbar or AISettings section where AI status is currently shown
  </action>
  <verify>
    <automated>cd C:/Users/admin/Desktop/RawDrive2/frontend && npx vitest run src/components/features/gallery/AIToolTooltip.test.tsx src/components/features/gallery/__tests__/BulkActions.test.tsx src/components/features/gallery/AIProcessingStatus.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - AI tooltips appear on hover (desktop) and long-press (mobile) with descriptions
    - BulkActionBar sticky at bottom when >= 1 photo selected with slide-up animation
    - AIProcessingStatus shows per-photo progress with estimated time
    - All tests pass
  </acceptance_criteria>
  <done>AI tools have descriptive tooltips, bulk actions activate on single selection with sticky bar, AI processing shows detailed progress with per-photo status</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GallerySettingsPresets + SubGalleryPermissionBadge</name>
  <files>
    frontend/src/components/features/gallery/GallerySettingsPresets.tsx,
    frontend/src/components/features/gallery/GallerySettingsPresets.test.tsx,
    frontend/src/components/features/gallery/GallerySettingsPanel.tsx,
    frontend/src/components/features/gallery/SubGalleryPermissionBadge.tsx,
    frontend/src/components/features/gallery/SubGalleryPermissionBadge.test.tsx,
    frontend/src/components/features/gallery/SubGallerySelector.tsx
  </files>
  <read_first>
    frontend/src/components/features/gallery/GallerySettingsPanel.tsx,
    frontend/src/components/features/gallery/DownloadSettings.tsx,
    frontend/src/components/features/gallery/WatermarkSettings.tsx,
    frontend/src/components/features/gallery/SubGallerySelector.tsx,
    frontend/src/pages/workspace/GalleryDetailPage.tsx
  </read_first>
  <behavior>
    - Settings presets show 4 buttons: Proofing, Delivery, Sharing, Premium Delivery
    - Clicking a preset configures access_level, download_policy, and watermark_enabled together
    - Active preset is highlighted based on current settings matching a preset's values
    - SubGalleryPermissionBadge shows "Inherits from parent" or "Custom permissions"
    - Override toggle switches between inherited and custom permissions per sub-gallery
  </behavior>
  <action>
    1. Create GallerySettingsPresets.tsx:
       - Props: { currentSettings: { access_level: string; download_policy: string; watermark_enabled: boolean }; onApplyPreset: (preset: PresetConfig) => void }
       - Define 4 preset configurations:
         - Proofing: { access_level: 'password_protected', download_policy: 'view_only', watermark_enabled: true } — "Clients view and select, no downloads"
         - Delivery: { access_level: 'password_protected', download_policy: 'original_allowed', watermark_enabled: false } — "Clients download full resolution"
         - Sharing: { access_level: 'public', download_policy: 'web_only', watermark_enabled: true } — "Public viewing with watermarks"
         - Premium Delivery: { access_level: 'password_protected', download_policy: 'original_allowed', watermark_enabled: false } — "Full originals, private access"
       - Render as 4 card buttons in a 2x2 grid
       - Each card: icon (lucide-react), preset name, 1-line description
       - Active state: if currentSettings matches a preset, highlight that card with ring-2 ring-primary
       - onClick calls onApplyPreset with the preset config

    2. Integrate into GallerySettingsPanel.tsx:
       - Add GallerySettingsPresets at the top of the panel, above the section tabs
       - "Quick Setup" label above the preset cards
       - onApplyPreset calls the gallery update API to save all three settings at once
       - After applying, the individual setting sections (access, downloads, watermark) reflect the new values

    3. Create SubGalleryPermissionBadge.tsx:
       - Props: { subGalleryId: string; parentGalleryId: string; inheritsPermissions: boolean; onToggleOverride: (subGalleryId: string, override: boolean) => void }
       - Badge: pill-shaped, colored:
         - Inheriting: blue badge "Inherits from parent" with link icon
         - Overridden: amber badge "Custom permissions" with unlock icon
       - Toggle switch next to badge: "Override permissions"
       - When toggling to override: show brief confirmation "This sub-gallery will use its own access settings"
       - When toggling back to inherit: show confirmation "This sub-gallery will use parent gallery settings"

    4. Wire SubGalleryPermissionBadge into the sub-gallery management UI:
       - Find where sub-galleries are listed (likely in GalleryDetailPage or a sub-gallery section)
       - Add badge next to each sub-gallery name/row
       - onToggleOverride calls gallery API to update the sub-gallery's permission_override field
  </action>
  <verify>
    <automated>cd C:/Users/admin/Desktop/RawDrive2/frontend && npx vitest run src/components/features/gallery/GallerySettingsPresets.test.tsx src/components/features/gallery/SubGalleryPermissionBadge.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - 4 preset buttons render with correct icons and descriptions
    - Clicking a preset applies access + download + watermark settings together
    - Active preset highlighted when current settings match
    - SubGalleryPermissionBadge shows inheritance status with toggle
    - Toggle switch updates permission override with confirmation
    - All tests pass
  </acceptance_criteria>
  <done>Gallery settings offer one-click presets for common configurations; sub-galleries show clear permission inheritance badges with override toggles</done>
</task>

</tasks>

<verification>
- AI tooltips appear on all AI toolbar buttons with descriptions
- Bulk action bar appears sticky at bottom when any photo selected
- Settings presets apply 3 settings at once and highlight active preset
- AI processing shows progress bar with per-photo status
- Sub-gallery permission badges show inheritance with override toggle
- All tests pass: `cd frontend && npx vitest run src/components/features/gallery/AIToolTooltip.test.tsx src/components/features/gallery/__tests__/BulkActions.test.tsx src/components/features/gallery/GallerySettingsPresets.test.tsx src/components/features/gallery/AIProcessingStatus.test.tsx src/components/features/gallery/SubGalleryPermissionBadge.test.tsx --reporter=verbose`
</verification>

<success_criteria>
- All 5 GALUX requirements implemented: tooltips, bulk actions, presets, AI status, permissions
- Each component has tests covering primary behavior
- Components integrate into existing gallery management views without breaking current functionality
</success_criteria>

<output>
After completion, create `.planning/phases/18-client-interactions-gallery-ux/18-03-SUMMARY.md`
</output>
