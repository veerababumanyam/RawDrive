---
phase: 18-client-interactions-gallery-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/features/gallery/public/FavoriteButton.tsx
  - frontend/src/components/features/gallery/public/FavoriteButton.test.tsx
  - frontend/src/components/features/gallery/public/SelectionQuotaBar.tsx
  - frontend/src/components/features/gallery/public/SelectionQuotaBar.test.tsx
  - frontend/src/components/features/gallery/public/CommentPanel.tsx
  - frontend/src/components/features/gallery/public/CommentPanel.test.tsx
  - frontend/src/contexts/GalleryInteractionContext.tsx
  - frontend/src/pages/public/PublicGalleryContent.tsx
  - frontend/src/components/features/gallery/player/PlayerToolbar.tsx
autonomous: true
requirements:
  - INTR-01
  - INTR-02
  - INTR-03

must_haves:
  truths:
    - "Client clicks a heart icon on a photo in the public gallery and the favorite persists across page reloads"
    - "Photographer sets a selection quota and client sees a progress bar showing selected count vs allowed maximum"
    - "Client leaves a comment on a photo during proofing and sees it appear in the comment panel"
  artifacts:
    - path: "frontend/src/components/features/gallery/public/FavoriteButton.tsx"
      provides: "Heart icon overlay with optimistic toggle"
      exports: ["FavoriteButton"]
    - path: "frontend/src/components/features/gallery/public/SelectionQuotaBar.tsx"
      provides: "Progress bar showing selections vs quota"
      exports: ["SelectionQuotaBar"]
    - path: "frontend/src/components/features/gallery/public/CommentPanel.tsx"
      provides: "Slide-from-right comment panel in GalleryPlayer"
      exports: ["CommentPanel"]
  key_links:
    - from: "frontend/src/components/features/gallery/public/FavoriteButton.tsx"
      to: "frontend/src/contexts/GalleryInteractionContext.tsx"
      via: "useGalleryInteraction().toggleFavorite(assetId)"
      pattern: "toggleFavorite"
    - from: "frontend/src/components/features/gallery/public/SelectionQuotaBar.tsx"
      to: "frontend/src/contexts/GalleryInteractionContext.tsx"
      via: "useGalleryInteraction().selectionCount / selectionLimit"
      pattern: "selectionCount.*selectionLimit"
    - from: "frontend/src/components/features/gallery/public/CommentPanel.tsx"
      to: "services/gallery-service/src/api/v1/public/proofing.py"
      via: "POST /{gallery_id}/proof/comment"
      pattern: "proof/comment"
---

<objective>
Wire existing backend proofing APIs to new frontend UI components for client favorites, selection quotas, and per-photo comments in the public gallery.

Purpose: Enable client-photographer proofing workflow — clients can heart photos, select within quotas, and leave feedback on individual photos.
Output: FavoriteButton, SelectionQuotaBar, CommentPanel components wired to GalleryInteractionContext and proofing API.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/contexts/GalleryInteractionContext.tsx
@frontend/src/pages/public/PublicGalleryContent.tsx
@frontend/src/components/features/gallery/player/PlayerToolbar.tsx
@frontend/src/components/features/gallery/player/GalleryPlayer.tsx
@services/gallery-service/src/api/v1/public/proofing.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From frontend/src/contexts/GalleryInteractionContext.tsx:
```typescript
export interface GalleryInteractionContextValue {
  favorites: Set<string>;
  selections: Set<string>;
  toggleFavorite: (assetId: string) => Promise<void>;
  toggleSelection: (assetId: string) => Promise<void>;
  selectionLimit: number | null;
  isAtSelectionLimit: boolean;
  visitorToken: string | null;
  isProofingEnabled: boolean;
  favoriteCount: number;
  selectionCount: number;
}
```

From services/gallery-service/src/api/v1/public/proofing.py:
```python
# Existing endpoints — NO new backend needed:
POST /{gallery_id}/proof/favorite   # toggle_favorite
POST /{gallery_id}/proof/select     # toggle_selection
POST /{gallery_id}/proof/comment    # add_comment
POST /{gallery_id}/proof/batch      # batch_proofing
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FavoriteButton + SelectionQuotaBar components</name>
  <files>
    frontend/src/components/features/gallery/public/FavoriteButton.tsx,
    frontend/src/components/features/gallery/public/FavoriteButton.test.tsx,
    frontend/src/components/features/gallery/public/SelectionQuotaBar.tsx,
    frontend/src/components/features/gallery/public/SelectionQuotaBar.test.tsx,
    frontend/src/pages/public/PublicGalleryContent.tsx,
    frontend/src/contexts/GalleryInteractionContext.tsx
  </files>
  <read_first>
    frontend/src/contexts/GalleryInteractionContext.tsx,
    frontend/src/pages/public/PublicGalleryContent.tsx,
    frontend/src/components/features/gallery/player/PlayerToolbar.tsx,
    services/gallery-service/src/api/v1/public/proofing.py
  </read_first>
  <behavior>
    - FavoriteButton renders heart icon; filled red when favorited, outline when not
    - Clicking FavoriteButton calls toggleFavorite(assetId) from GalleryInteractionContext
    - FavoriteButton shows optimistic state immediately (no loading spinner)
    - SelectionQuotaBar shows "N of M selected" text with progress bar fill
    - SelectionQuotaBar bar color changes from blue to amber at 90% quota, red at 100%
    - SelectionQuotaBar hidden when selectionLimit is null (unlimited)
    - SelectionQuotaBar shows "Selection complete!" when at limit
  </behavior>
  <action>
    1. Create FavoriteButton.tsx in frontend/src/components/features/gallery/public/:
       - Props: { assetId: string; className?: string; size?: 'sm' | 'md' | 'lg' }
       - Uses useGalleryInteraction() to read favorites Set and call toggleFavorite
       - Heart icon from lucide-react (Heart, filled variant via fill prop)
       - Optimistic toggle: icon state from favorites.has(assetId)
       - Tailwind: absolute positioned overlay (top-2 right-2), white drop shadow for visibility on photos
       - Animate scale on click via Tailwind transition-transform
       - Only renders when isProofingEnabled is true

    2. Create SelectionQuotaBar.tsx in same directory:
       - Props: { className?: string }
       - Uses useGalleryInteraction() for selectionCount, selectionLimit, isAtSelectionLimit
       - Returns null if selectionLimit is null
       - Progress bar: div with width percentage = (selectionCount / selectionLimit) * 100
       - Color logic: <90% = blue-500, 90-99% = amber-500, 100% = red-500
       - Text: "{selectionCount} of {selectionLimit} selected" or "Selection complete!" at limit
       - Tailwind h-2 rounded-full bg with transition-all duration-300

    3. Wire FavoriteButton into PublicGalleryContent.tsx:
       - Add FavoriteButton overlay on each photo card/thumbnail in the gallery grid
       - Position absolute top-right of each photo container

    4. Wire SelectionQuotaBar below the gallery header in PublicGalleryContent.tsx

    5. Add FavoriteButton to PlayerToolbar.tsx so it appears in GalleryPlayer too

    6. Extend GalleryInteractionContext if needed: add a `comments` Map and `addComment` method stub (implemented in Task 2) for future comment support. Ensure `fetchVisitorActions` on mount loads existing favorites/selections from the proofing API GET endpoint.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Desktop/RawDrive2/frontend && npx vitest run src/components/features/gallery/public/FavoriteButton.test.tsx src/components/features/gallery/public/SelectionQuotaBar.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - FavoriteButton toggles heart fill on click, reads from GalleryInteractionContext
    - SelectionQuotaBar shows progress with color thresholds at 90%/100%
    - Both components render in PublicGalleryContent and GalleryPlayer
    - Tests pass covering toggle behavior, color thresholds, and null-limit hiding
  </acceptance_criteria>
  <done>Heart icon overlay on gallery photos toggles favorites via context; quota progress bar shows selection count vs limit with color changes at 90%/100%; both components tested and wired into public gallery and player views</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CommentPanel for per-photo comments in GalleryPlayer</name>
  <files>
    frontend/src/components/features/gallery/public/CommentPanel.tsx,
    frontend/src/components/features/gallery/public/CommentPanel.test.tsx,
    frontend/src/contexts/GalleryInteractionContext.tsx,
    frontend/src/components/features/gallery/player/GalleryPlayer.tsx,
    frontend/src/components/features/gallery/player/PlayerToolbar.tsx
  </files>
  <read_first>
    frontend/src/components/features/gallery/player/GalleryPlayer.tsx,
    frontend/src/components/features/gallery/player/PlayerToolbar.tsx,
    frontend/src/contexts/GalleryInteractionContext.tsx,
    services/gallery-service/src/api/v1/public/proofing.py,
    services/gallery-service/src/services/proofing_service.py
  </read_first>
  <behavior>
    - CommentPanel slides in from right when comment button clicked in PlayerToolbar
    - Panel shows existing comments for current photo (fetched from proofing API)
    - User can type a comment and submit it
    - Submitted comment appears immediately (optimistic) in the list
    - Comment shows visitor name (or "Anonymous"), timestamp, and text
    - Panel can be closed by clicking X or clicking outside
    - Empty state shows "No comments yet" message
  </behavior>
  <action>
    1. Extend GalleryInteractionContext.tsx:
       - Add `comments: Map<string, Comment[]>` state (keyed by assetId)
       - Add `addComment(assetId: string, text: string): Promise<void>` method
       - Add `loadComments(assetId: string): Promise<void>` method
       - Comment type: { id: string; visitor_name: string; text: string; created_at: string }
       - addComment calls POST /{gallery_id}/proof/comment with { asset_id, comment_text, visitor_token }
       - Optimistic: append to local comments Map immediately, rollback on error

    2. Create CommentPanel.tsx:
       - Props: { assetId: string; isOpen: boolean; onClose: () => void }
       - Uses framer-motion for slide-in animation (x: '100%' -> x: 0) from right
       - Fixed position panel: right-0, top-0, h-full, w-80 (or w-full on mobile), bg-white dark:bg-gray-900
       - Header: "Comments" title + close X button
       - Comment list: scrollable, each comment shows avatar placeholder, name, timestamp (relative via formatRelativeDate from @rawdrive/shared-utils), text
       - Input area at bottom: textarea + send button, disabled when empty
       - Calls loadComments(assetId) on open
       - Flat comments (not nested) per Claude's discretion on threading depth
       - Backdrop overlay (bg-black/30) on mobile to indicate modal

    3. Add comment toggle button to PlayerToolbar.tsx (MessageCircle icon from lucide-react)
       - Tracks `isCommentPanelOpen` state in GalleryPlayer or PlayerToolbar
       - Passes isOpen/onClose to CommentPanel

    4. Mount CommentPanel in GalleryPlayer.tsx alongside the player content
       - Panel overlays on top of player, does not shift photo content
  </action>
  <verify>
    <automated>cd C:/Users/admin/Desktop/RawDrive2/frontend && npx vitest run src/components/features/gallery/public/CommentPanel.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - CommentPanel slides from right with animation when comment button clicked
    - Existing comments load for current photo
    - New comment submits to proofing API and appears in list optimistically
    - Panel closes on X click
    - Tests pass covering open/close, comment submission, empty state
  </acceptance_criteria>
  <done>Per-photo comment panel integrated into GalleryPlayer with slide-from-right animation, comment submission via proofing API, optimistic updates, and empty state handling</done>
</task>

</tasks>

<verification>
- FavoriteButton renders on each photo in PublicGalleryContent and in GalleryPlayer PlayerToolbar
- SelectionQuotaBar appears below gallery header when selection quota is set
- CommentPanel opens from PlayerToolbar and submits comments to proofing API
- All three components read/write through GalleryInteractionContext
- All tests pass: `cd frontend && npx vitest run src/components/features/gallery/public/ --reporter=verbose`
</verification>

<success_criteria>
- Client can click heart icon on any photo to toggle favorite (persists via proofing API)
- Client sees "N of M selected" progress bar with color changes at 90%/100% quota
- Client can open comment panel in player, see existing comments, and submit new ones
- All interactions use optimistic updates through GalleryInteractionContext
</success_criteria>

<output>
After completion, create `.planning/phases/18-client-interactions-gallery-ux/18-01-SUMMARY.md`
</output>
