---
phase: 18-client-interactions-gallery-ux
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - frontend/src/hooks/useProofingWebSocket.ts
  - frontend/src/hooks/useProofingWebSocket.test.ts
  - frontend/src/contexts/GalleryInteractionContext.tsx
  - services/gallery-service/src/api/v1/websocket.py
  - services/gallery-service/src/services/proofing_service.py
autonomous: true
requirements:
  - INTR-04

must_haves:
  truths:
    - "Photographer sees client favorites/selections appear in their dashboard in real-time without page refresh"
    - "Multiple clients interacting with same gallery see each other's actions via WebSocket broadcast"
  artifacts:
    - path: "frontend/src/hooks/useProofingWebSocket.ts"
      provides: "WebSocket hook for real-time proofing sync"
      exports: ["useProofingWebSocket"]
    - path: "services/gallery-service/src/api/v1/websocket.py"
      provides: "WebSocket endpoint for gallery proofing events"
      exports: ["websocket_endpoint"]
  key_links:
    - from: "services/gallery-service/src/services/proofing_service.py"
      to: "services/gallery-service/src/api/v1/websocket.py"
      via: "Redis pub/sub channel gallery:{gallery_id}:proofing"
      pattern: "redis.*publish.*proofing"
    - from: "frontend/src/hooks/useProofingWebSocket.ts"
      to: "frontend/src/contexts/GalleryInteractionContext.tsx"
      via: "WebSocket message updates context state"
      pattern: "useProofingWebSocket"
---

<objective>
Add WebSocket real-time sync so photographers see client proofing actions (favorites, selections, comments) appear instantly in their dashboard, and multiple clients on the same gallery see each other's interactions.

Purpose: Close the real-time feedback loop — proofing is collaborative and photographers need immediate visibility into client decisions.
Output: useProofingWebSocket hook, gallery-service WebSocket endpoint, GalleryInteractionContext WebSocket integration.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-client-interactions-gallery-ux/18-01-SUMMARY.md
@frontend/src/hooks/useSocket.ts
@frontend/src/contexts/GalleryInteractionContext.tsx
@services/gallery-service/src/services/proofing_service.py

<interfaces>
<!-- Key types from Plan 01 and existing code. -->

From frontend/src/hooks/useSocket.ts:
```typescript
export interface WebSocketEvent {
  type: string;
  // ... event data
}

export interface UseSocketOptions {
  url?: string;
  autoConnect?: boolean;
  reconnectDelay?: number;
  // ...
}

export function useSocket(options: UseSocketOptions): UseSocketReturn;
```

From services/gallery-service/src/services/proofing_service.py:
```python
# _publish_proofing_update already publishes to Redis pub/sub:
async def _publish_proofing_update(self, gallery_id, action_type, asset_id, visitor_id, data):
    channel = f"gallery:{gallery_id}:proofing"
    await redis_client.publish(channel, message)
```

From GalleryInteractionContext (after Plan 01):
```typescript
export interface GalleryInteractionContextValue {
  favorites: Set<string>;
  selections: Set<string>;
  comments: Map<string, Comment[]>;
  toggleFavorite: (assetId: string) => Promise<void>;
  toggleSelection: (assetId: string) => Promise<void>;
  addComment: (assetId: string, text: string) => Promise<void>;
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gallery-service WebSocket endpoint + useProofingWebSocket hook</name>
  <files>
    services/gallery-service/src/api/v1/websocket.py,
    frontend/src/hooks/useProofingWebSocket.ts,
    frontend/src/hooks/useProofingWebSocket.test.ts,
    services/gallery-service/src/api/v1/__init__.py
  </files>
  <read_first>
    services/gallery-service/src/services/proofing_service.py,
    services/gallery-service/src/api/v1/__init__.py,
    frontend/src/hooks/useSocket.ts,
    services/gallery-service/src/config.py,
    services/notifications-service/src/main.py
  </read_first>
  <behavior>
    - WebSocket endpoint at /ws/gallery/{gallery_id}/proofing accepts connections
    - Endpoint subscribes to Redis channel gallery:{gallery_id}:proofing
    - When proofing_service publishes an event, WebSocket forwards to all connected clients
    - Frontend hook connects to WebSocket and dispatches events to update local state
    - Hook reconnects automatically on disconnect with exponential backoff
    - Hook ignores events from own visitor_token (prevents echo)
  </behavior>
  <action>
    1. Create services/gallery-service/src/api/v1/websocket.py:
       - FastAPI WebSocket endpoint: @router.websocket("/ws/gallery/{gallery_id}/proofing")
       - Accept connection, optionally validate visitor_token from query param
       - Subscribe to Redis pub/sub channel `gallery:{gallery_id}:proofing` using aioredis
       - Forward received messages to WebSocket client as JSON
       - Handle disconnect gracefully, unsubscribe from Redis channel
       - Support multiple concurrent connections per gallery (each client gets own subscription)
       - Event payload structure: { type: "favorite_toggle" | "selection_toggle" | "comment_added", asset_id: string, visitor_id: string, data: object, timestamp: string }

    2. Register WebSocket router in gallery-service api/v1/__init__.py

    3. Create frontend/src/hooks/useProofingWebSocket.ts:
       - Follows same pattern as existing useSocket.ts but specialized for proofing
       - Props: { galleryId: string; visitorToken: string | null; enabled?: boolean }
       - Connects to ws://{gallery-service-host}/ws/gallery/{galleryId}/proofing?visitor_token={token}
       - Returns { onFavoriteToggle, onSelectionToggle, onCommentAdded } callback registration
       - Filters out events where event.visitor_id matches own visitorToken (no echo)
       - Reconnect with exponential backoff (1s, 2s, 4s, 8s max)
       - Only connects when enabled=true (default: true when proofing is enabled)

    4. Write test for useProofingWebSocket: mock WebSocket, verify event filtering, reconnect behavior
  </action>
  <verify>
    <automated>cd C:/Users/admin/Desktop/RawDrive2/frontend && npx vitest run src/hooks/useProofingWebSocket.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - Gallery-service has WebSocket endpoint that subscribes to Redis proofing channel
    - Frontend hook connects, receives events, filters own events
    - Tests pass for event filtering and callback invocation
  </acceptance_criteria>
  <done>WebSocket endpoint in gallery-service forwards Redis proofing events to connected clients; frontend hook receives and filters events with reconnection</done>
</task>

<task type="auto">
  <name>Task 2: Wire WebSocket into GalleryInteractionContext for live state sync</name>
  <files>
    frontend/src/contexts/GalleryInteractionContext.tsx
  </files>
  <read_first>
    frontend/src/contexts/GalleryInteractionContext.tsx,
    frontend/src/hooks/useProofingWebSocket.ts
  </read_first>
  <action>
    1. In GalleryInteractionProvider, add useProofingWebSocket hook:
       - Pass galleryId and visitorToken from existing context state
       - Register callbacks for each event type:
         - onFavoriteToggle: update favorites Set (add/remove asset_id based on event data)
         - onSelectionToggle: update selections Set, recalculate selectionCount
         - onCommentAdded: append to comments Map for the relevant asset_id
       - Events from OTHER visitors update local state immediately (real-time sync)
       - Events from OWN visitor are already filtered out by the hook (no double-update)

    2. Ensure state updates are batched to avoid unnecessary re-renders:
       - Use functional setState: setFavorites(prev => new Set([...prev, assetId])) pattern
       - Comment list updates: setComments(prev => new Map(prev).set(assetId, [...(prev.get(assetId) || []), newComment]))

    3. The photographer's dashboard (workspace GalleryDetailPage) also uses this context or receives WebSocket events — ensure the WebSocket URL works for authenticated users too (photographer sees real-time updates).
       - If GalleryDetailPage uses different state management, add useProofingWebSocket there with JWT auth instead of visitor_token.
       - Check GalleryDetailPage.tsx to see how proofing data is currently displayed and wire WebSocket updates into that view.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Desktop/RawDrive2/frontend && npx vitest run src/contexts/ --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - GalleryInteractionContext receives WebSocket events and updates favorites/selections/comments in real-time
    - Photographer dashboard receives proofing events via same WebSocket channel
    - No echo: own visitor's actions don't cause duplicate state updates
  </acceptance_criteria>
  <done>Real-time proofing sync working end-to-end: client action -> proofing API -> Redis pub/sub -> WebSocket -> other clients + photographer dashboard state update</done>
</task>

</tasks>

<verification>
- Client A favorites a photo; Client B (same gallery) sees heart appear without refresh
- Photographer opens gallery detail; sees favorites/selections count update live
- WebSocket reconnects after connection drop
- All tests pass: `cd frontend && npx vitest run src/hooks/useProofingWebSocket.test.ts src/contexts/ --reporter=verbose`
</verification>

<success_criteria>
- Proofing actions broadcast via WebSocket to all connected clients and photographer dashboard
- State updates in real-time without page refresh
- Own actions don't echo back (filtered by visitor_token)
- Connection resilient with automatic reconnection
</success_criteria>

<output>
After completion, create `.planning/phases/18-client-interactions-gallery-ux/18-02-SUMMARY.md`
</output>
