---
phase: 03-ai-service-stabilization
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - services/ai-processing-service/src/api/v1/faces.py
  - services/ai-processing-service/tests/test_lazy_imports.py
autonomous: true
gap_closure: true
requirements: [AIS-03]

must_haves:
  truths:
    - "Importing api.v1 module does NOT trigger cv2, numpy, or PIL imports"
    - "Importing main module does NOT trigger cv2, numpy, or PIL imports"
    - "Face API endpoints still function correctly when called (cv2/numpy/PIL load on demand)"
  artifacts:
    - path: "services/ai-processing-service/src/api/v1/faces.py"
      provides: "Face detection API with lazy cv2/numpy/PIL imports"
      contains: "from __future__ import annotations"
    - path: "services/ai-processing-service/tests/test_lazy_imports.py"
      provides: "Regression tests covering API layer lazy imports"
      contains: "TestApiLayerLazyImports"
  key_links:
    - from: "tests/test_lazy_imports.py"
      to: "api.v1"
      via: "importlib.import_module"
      pattern: "import_module.*api\\.v1"
    - from: "api/v1/faces.py helpers"
      to: "cv2, numpy, PIL"
      via: "lazy import inside function body"
      pattern: "def _decode_base64_image|def _load_image_from_url|def _get_image_source"
---

<objective>
Close the AIS-03 verification gap: eager cv2/numpy/PIL imports in api/v1/faces.py that defeat the lazy-import fix applied to model/service files.

Purpose: The verification found that faces.py has top-level `import cv2`, `import numpy as np`, `from PIL import Image` (lines 20-23). This file is imported unconditionally at startup via `main.py -> api/v1/__init__.py -> faces.py`, causing heavy ML libraries to load at module import time. This violates AIS-03 (heavy ML imports must be lazy).

Output: faces.py with lazy imports inside helper/endpoint functions, regression test covering the API layer import chain.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-service-stabilization/03-01-SUMMARY.md
@.planning/phases/03-ai-service-stabilization/03-VERIFICATION.md

<interfaces>
<!-- Key files the executor needs to understand -->

From services/ai-processing-service/src/api/v1/faces.py (lines 20-23, THE BUG):
```python
import cv2                    # line 20 — EAGER, must move into function bodies
import numpy as np            # line 21 — EAGER, must move into function bodies
from fastapi import APIRouter, HTTPException, status  # line 22 — OK, keep
from PIL import Image         # line 23 — EAGER, must move into function bodies
```

From services/ai-processing-service/src/api/v1/__init__.py (the import chain):
```python
from api.v1.faces import router as faces_router  # line 8 — triggers faces.py load
```

Functions in faces.py that USE cv2/numpy/PIL (imports must move here):
- `_decode_base64_image(base64_data: str) -> np.ndarray` — uses Image.open, cv2.cvtColor, np.array
- `_load_image_from_url(url: str) -> np.ndarray` — uses Image.open, cv2.cvtColor, np.array
- `_get_image_source(...)` — uses cv2.imread
- `_convert_embedding_result_to_response(...)` — uses cv2.imencode
- `compare_faces(request)` — uses np.array

From services/ai-processing-service/tests/test_lazy_imports.py:
```python
def _clean_module(module_name: str, also_remove: list[str] | None = None):
    """Remove a module and its submodules from sys.modules."""
# Existing test classes: TestClipEmbedderLazyImports, TestRealEsrganLazyImports, TestDatabaseLazyImports
# MISSING: TestApiLayerLazyImports — must be added
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add API-layer lazy import regression tests (TDD RED)</name>
  <files>services/ai-processing-service/tests/test_lazy_imports.py</files>
  <read_first>
    - services/ai-processing-service/tests/test_lazy_imports.py
    - services/ai-processing-service/src/api/v1/__init__.py
    - services/ai-processing-service/src/api/v1/faces.py (lines 1-25 only)
  </read_first>
  <behavior>
    - Test 1: Importing `api.v1` does NOT cause `cv2` to appear in sys.modules
    - Test 2: Importing `api.v1` does NOT cause `numpy` to appear in sys.modules
    - Test 3: Importing `api.v1` does NOT cause `PIL` to appear in sys.modules
  </behavior>
  <action>
Add a new test class `TestApiLayerLazyImports` to `tests/test_lazy_imports.py` with three test methods:

1. `test_no_cv2_at_api_import` — clean cv2 and api.v1 from sys.modules, import `api.v1`, assert `cv2` not in sys.modules
2. `test_no_numpy_at_api_import` — clean numpy and api.v1 from sys.modules, import `api.v1`, assert `numpy` not in sys.modules
3. `test_no_pil_at_api_import` — clean PIL and api.v1 from sys.modules, import `api.v1`, assert `PIL` not in sys.modules

Use the existing `_clean_module` helper. Also clean `api.v1.faces` since that is the module with the eager imports. Pattern follows existing TestClipEmbedderLazyImports exactly.

These tests MUST FAIL against the current faces.py (RED phase). Do NOT modify faces.py in this task.
  </action>
  <verify>
    <automated>cd services/ai-processing-service && python -m pytest tests/test_lazy_imports.py::TestApiLayerLazyImports -x --timeout=30 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "class TestApiLayerLazyImports" services/ai-processing-service/tests/test_lazy_imports.py returns 1
    - grep -c "test_no_cv2_at_api_import" services/ai-processing-service/tests/test_lazy_imports.py returns 1
    - grep -c "test_no_numpy_at_api_import" services/ai-processing-service/tests/test_lazy_imports.py returns 1
    - grep -c "test_no_pil_at_api_import" services/ai-processing-service/tests/test_lazy_imports.py returns 1
    - Tests FAIL (RED) because faces.py still has eager imports
  </acceptance_criteria>
  <done>Three new test methods exist in TestApiLayerLazyImports. They fail because api/v1/faces.py still has top-level cv2/numpy/PIL imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make cv2/numpy/PIL imports lazy in faces.py (TDD GREEN)</name>
  <files>services/ai-processing-service/src/api/v1/faces.py</files>
  <read_first>
    - services/ai-processing-service/src/api/v1/faces.py
  </read_first>
  <action>
Modify `services/ai-processing-service/src/api/v1/faces.py` to make cv2, numpy, and PIL imports lazy:

1. Add `from __future__ import annotations` as the FIRST line after the module docstring (before any other imports). This ensures type annotations like `np.ndarray` remain as strings and don't require numpy at import time.

2. Add a `TYPE_CHECKING` guard for type-hint-only imports:
```python
from typing import TYPE_CHECKING
if TYPE_CHECKING:
    import numpy as np
```

3. REMOVE the three top-level imports:
   - `import cv2` (line 20)
   - `import numpy as np` (line 21)
   - `from PIL import Image` (line 23)

4. Add lazy imports INSIDE each function that uses them:

   - `_decode_base64_image`: add `import cv2`, `import numpy as np`, `from PIL import Image` at function start
   - `_load_image_from_url`: add `import cv2`, `import numpy as np`, `from PIL import Image` at function start
   - `_get_image_source`: add `import cv2` at function start (uses cv2.imread)
   - `_convert_embedding_result_to_response`: add `import cv2` at function start (uses cv2.imencode)
   - `compare_faces`: add `import numpy as np` at function start (uses np.array)

5. Return type annotations that reference `np.ndarray` stay as-is — `from __future__ import annotations` makes them strings, so numpy is NOT needed at class/function definition time.

Do NOT change any business logic. Only move imports from module-level to function-level.
  </action>
  <verify>
    <automated>cd services/ai-processing-service && python -m pytest tests/test_lazy_imports.py -x --timeout=30 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "^import cv2" services/ai-processing-service/src/api/v1/faces.py returns 0
    - grep -c "^import numpy" services/ai-processing-service/src/api/v1/faces.py returns 0
    - grep -c "^from PIL" services/ai-processing-service/src/api/v1/faces.py returns 0
    - grep -c "from __future__ import annotations" services/ai-processing-service/src/api/v1/faces.py returns 1
    - All tests in test_lazy_imports.py pass (7 total: 4 existing + 3 new)
  </acceptance_criteria>
  <done>faces.py has zero top-level cv2/numpy/PIL imports. All 7 lazy import tests pass (GREEN). The API layer no longer loads heavy ML libraries at service startup.</done>
</task>

</tasks>

<verification>
1. All 7 tests in test_lazy_imports.py pass: `cd services/ai-processing-service && python -m pytest tests/test_lazy_imports.py -v`
2. All existing tests still pass: `cd services/ai-processing-service && python -m pytest tests/ -x --timeout=30`
3. No top-level heavy imports in faces.py: `grep -n "^import cv2\|^import numpy\|^from PIL" services/ai-processing-service/src/api/v1/faces.py` returns empty
4. `from __future__ import annotations` present: `head -15 services/ai-processing-service/src/api/v1/faces.py | grep "from __future__"`
</verification>

<success_criteria>
- AIS-03 gap CLOSED: importing `api.v1` (and by extension `main`) does not trigger cv2, numpy, or PIL imports
- All 7 lazy import tests pass
- All 11+ existing tests continue to pass
- faces.py has `from __future__ import annotations` and zero top-level cv2/numpy/PIL imports
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-service-stabilization/03-02-SUMMARY.md`
</output>
