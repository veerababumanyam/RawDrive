---
phase: 03-ai-service-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/ai-processing-service/src/main.py
  - services/ai-processing-service/src/config.py
  - services/ai-processing-service/src/core/database.py
  - services/ai-processing-service/src/core/redis.py
  - services/ai-processing-service/src/models/clip_embedder.py
  - services/ai-processing-service/src/models/real_esrgan.py
  - services/ai-processing-service/src/services/face_detection_service.py
  - services/ai-processing-service/src/services/face_embedding_service.py
  - services/ai-processing-service/requirements.txt
  - services/ai-processing-service/Dockerfile
  - services/ai-processing-service/tests/conftest.py
  - services/ai-processing-service/tests/test_health.py
  - services/ai-processing-service/tests/test_lazy_imports.py
  - services/ai-processing-service/tests/test_database.py
autonomous: true
requirements: [AIS-01, AIS-02, AIS-03]

must_haves:
  truths:
    - "ai-processing-service container starts without crashing"
    - "/health/live returns 200 when process is running"
    - "/health/ready returns 200 when DB and Redis are connected, 503 otherwise"
    - "Service starts successfully with MILVUS_ENABLED=false (default)"
    - "No heavy ML imports (torch, cv2, transformers) execute at module load time"
  artifacts:
    - path: "services/ai-processing-service/src/main.py"
      provides: "Fixed health endpoints at /health/live and /health/ready, resilient lifespan without sys.exit"
      contains: "/health/live"
    - path: "services/ai-processing-service/src/core/database.py"
      provides: "database_healthcheck function, lazy milvus import"
      exports: ["database_healthcheck"]
    - path: "services/ai-processing-service/src/core/redis.py"
      provides: "redis_healthcheck function"
      exports: ["redis_healthcheck"]
    - path: "services/ai-processing-service/src/config.py"
      provides: "MILVUS_ENABLED defaults to False"
      contains: "default=False"
    - path: "services/ai-processing-service/src/models/clip_embedder.py"
      provides: "Lazy torch/transformers imports"
    - path: "services/ai-processing-service/tests/test_health.py"
      provides: "Health endpoint tests for AIS-01"
    - path: "services/ai-processing-service/tests/test_lazy_imports.py"
      provides: "Import-time verification tests for AIS-03"
    - path: "services/ai-processing-service/tests/test_database.py"
      provides: "pgvector fallback tests for AIS-02"
  key_links:
    - from: "services/ai-processing-service/src/main.py"
      to: "core.database.database_healthcheck"
      via: "import in /health/ready handler"
      pattern: "database_healthcheck"
    - from: "services/ai-processing-service/src/main.py"
      to: "core.redis.redis_healthcheck"
      via: "import in /health/ready handler"
      pattern: "redis_healthcheck"
    - from: "services/ai-processing-service/Dockerfile"
      to: "/health/live"
      via: "HEALTHCHECK CMD"
      pattern: "health/live"
---

<objective>
Fix the ai-processing-service crash-loop so the container starts reliably and passes health checks.

Purpose: This service is a prerequisite for Phase 6 (AI/ML Pipeline). It currently crash-loops due to 6 identified bugs: wrong health endpoint paths, missing healthcheck functions, hard sys.exit on startup failure, eager heavy ML imports, eager Milvus import, and MILVUS_ENABLED defaulting to True.

Output: A stable ai-processing-service that starts, responds to health probes, and gracefully handles missing Milvus/ML dependencies.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-service-stabilization/03-RESEARCH.md

<!-- Key source files to fix -->
@services/ai-processing-service/src/main.py
@services/ai-processing-service/src/config.py
@services/ai-processing-service/src/core/database.py
@services/ai-processing-service/src/core/redis.py
@services/ai-processing-service/src/models/clip_embedder.py
@services/ai-processing-service/src/models/real_esrgan.py
@services/ai-processing-service/src/services/face_detection_service.py
@services/ai-processing-service/src/services/face_embedding_service.py
@services/ai-processing-service/requirements.txt
@services/ai-processing-service/Dockerfile

<!-- Reference implementation for health check pattern -->
@services/gallery-service/src/main.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test scaffolding for health, lazy imports, and database fallback</name>
  <files>
    services/ai-processing-service/tests/conftest.py,
    services/ai-processing-service/tests/test_health.py,
    services/ai-processing-service/tests/test_lazy_imports.py,
    services/ai-processing-service/tests/test_database.py
  </files>
  <read_first>
    services/ai-processing-service/src/main.py,
    services/ai-processing-service/src/core/database.py,
    services/ai-processing-service/src/core/redis.py,
    services/ai-processing-service/src/config.py,
    services/gallery-service/src/main.py
  </read_first>
  <behavior>
    test_health.py:
    - test_liveness: GET /health/live returns 200 with {"status": "alive"}
    - test_readiness_healthy: GET /health/ready returns 200 when DB and Redis are mocked as healthy
    - test_readiness_unhealthy: GET /health/ready returns 503 when DB or Redis mock returns False
    - test_no_milvus: Service starts with MILVUS_ENABLED=false, /health/live returns 200

    test_lazy_imports.py:
    - test_no_torch_at_import: Importing clip_embedder module does NOT trigger torch import (check sys.modules)
    - test_no_cv2_at_import: Importing real_esrgan module does NOT trigger cv2 import
    - test_no_transformers_at_import: Importing clip_embedder module does NOT trigger transformers import
    - test_no_pymilvus_at_import: Importing core.database does NOT trigger pymilvus import when MILVUS_ENABLED=false

    test_database.py:
    - test_pgvector_fallback: When MILVUS_ENABLED=false, find_similar_by_clip uses pgvector path (not milvus)
    - test_database_healthcheck_success: database_healthcheck returns True when pool is connected
    - test_database_healthcheck_failure: database_healthcheck returns False when pool is None
  </behavior>
  <action>
    Create pytest configuration and test files. These tests will initially FAIL (RED phase) because the fixes have not been applied yet.

    1. Create `tests/conftest.py` with:
       - pytest-asyncio configuration (asyncio_mode = "auto")
       - A `test_client` fixture using FastAPI TestClient with the app from main.py
       - Mock fixtures for database pool and redis client
       - An environment fixture that sets MILVUS_ENABLED=false

    2. Create `tests/test_health.py` with tests per the behavior block above. Use httpx.AsyncClient or FastAPI TestClient. Mock database_healthcheck and redis_healthcheck for readiness tests.

    3. Create `tests/test_lazy_imports.py` that:
       - Clears relevant modules from sys.modules before each test
       - Imports the target module
       - Asserts heavy libraries are NOT in sys.modules after import
       - Uses importlib.import_module for clean imports

    4. Create `tests/test_database.py` with tests for pgvector fallback and healthcheck functions.

    Note: Tests for health endpoints will fail because endpoints are at /health and /ready (not /health/live and /health/ready). Lazy import tests will fail because imports are still eager. This is expected RED state.
  </action>
  <verify>
    <automated>cd services/ai-processing-service && python -m pytest tests/ --collect-only 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py exists with test_client fixture and mock fixtures
    - tests/test_health.py exists with at least 4 test functions (test_liveness, test_readiness_healthy, test_readiness_unhealthy, test_no_milvus)
    - tests/test_lazy_imports.py exists with at least 4 test functions checking sys.modules
    - tests/test_database.py exists with at least 3 test functions (pgvector_fallback, healthcheck_success, healthcheck_failure)
    - All test files are collected by pytest (no syntax errors)
  </acceptance_criteria>
  <done>Test scaffolding created covering AIS-01, AIS-02, AIS-03. Tests collected by pytest. Tests expected to fail until Task 2 and Task 3 apply fixes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix health endpoints, lifespan resilience, healthcheck functions, and Milvus config</name>
  <files>
    services/ai-processing-service/src/main.py,
    services/ai-processing-service/src/core/database.py,
    services/ai-processing-service/src/core/redis.py,
    services/ai-processing-service/src/config.py
  </files>
  <read_first>
    services/ai-processing-service/src/main.py,
    services/ai-processing-service/src/core/database.py,
    services/ai-processing-service/src/core/redis.py,
    services/ai-processing-service/src/config.py,
    .planning/phases/03-ai-service-stabilization/03-RESEARCH.md
  </read_first>
  <behavior>
    - GET /health/live returns 200 {"status": "alive", "service": "ai-processing-service"}
    - GET /health/ready returns 200 when DB+Redis healthy, 503 when either is down
    - Service starts even if DB or Redis connection fails (no sys.exit)
    - MILVUS_ENABLED defaults to False in config.py
    - database_healthcheck() returns True/False based on pool connectivity
    - redis_healthcheck() returns True/False based on Redis ping
  </behavior>
  <action>
    Apply Bug fixes 1, 2, 3, and 6 from the research:

    **Bug 1 - Fix health endpoint paths in main.py:**
    - Change `/health` route to `/health/live`
    - Change `/ready` route to `/health/ready`
    - Liveness: always return 200 `{"status": "alive", "service": "ai-processing-service"}`
    - Readiness: call database_healthcheck() and redis_healthcheck(), return 200 if both pass, 503 otherwise. Do NOT check Milvus in readiness.

    **Bug 2 - Add missing healthcheck functions:**
    - In `core/database.py`: Add `database_healthcheck(timeout=2.0) -> bool` that checks pool connectivity with `SELECT 1`. Return False if pool is None or query fails.
    - In `core/redis.py`: Add `redis_healthcheck(timeout=2.0) -> bool` that calls `client.ping()`. Return False if client is None or ping fails.

    **Bug 3 - Remove sys.exit(1) from lifespan:**
    - Replace `sys.exit(1)` calls in the lifespan function with `logger.error()` and store errors in `app.state.startup_errors` list.
    - Set `app.state.startup_complete = True` after all init attempts.
    - Liveness returns 200 regardless (process is alive). Readiness checks actual connectivity.

    **Bug 6 - Flip MILVUS_ENABLED default:**
    - In `config.py`: Change `MILVUS_ENABLED: bool = Field(default=True)` to `Field(default=False)`.

    Also remove or guard the top-level import of `get_clip_embedder` in main.py (line ~24) -- move it inside the endpoint/lifespan that uses it, or make it lazy. This import triggers the full torch load cascade (Pitfall 5 from research).

    After applying fixes, run the health and database tests:
    `cd services/ai-processing-service && python -m pytest tests/test_health.py tests/test_database.py -x -v`
  </action>
  <verify>
    <automated>cd services/ai-processing-service && python -m pytest tests/test_health.py tests/test_database.py -x -v --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - main.py contains "/health/live" endpoint (grep: /health/live)
    - main.py contains "/health/ready" endpoint (grep: /health/ready)
    - main.py does NOT contain sys.exit (grep -c "sys.exit" returns 0)
    - core/database.py exports database_healthcheck function (grep: "async def database_healthcheck")
    - core/redis.py exports redis_healthcheck function (grep: "async def redis_healthcheck")
    - config.py has MILVUS_ENABLED default=False (grep: "default=False")
    - test_health.py tests pass
    - test_database.py tests pass
  </acceptance_criteria>
  <done>Health endpoints respond at correct paths. Lifespan no longer crashes on infra failure. Milvus is optional by default. Healthcheck functions exist and are tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make ML imports lazy, clean requirements.txt, update Dockerfile HEALTHCHECK</name>
  <files>
    services/ai-processing-service/src/models/clip_embedder.py,
    services/ai-processing-service/src/models/real_esrgan.py,
    services/ai-processing-service/src/services/face_detection_service.py,
    services/ai-processing-service/src/services/face_embedding_service.py,
    services/ai-processing-service/src/core/database.py,
    services/ai-processing-service/requirements.txt,
    services/ai-processing-service/Dockerfile
  </files>
  <read_first>
    services/ai-processing-service/src/models/clip_embedder.py,
    services/ai-processing-service/src/models/real_esrgan.py,
    services/ai-processing-service/src/services/face_detection_service.py,
    services/ai-processing-service/src/services/face_embedding_service.py,
    services/ai-processing-service/src/core/database.py,
    services/ai-processing-service/requirements.txt,
    services/ai-processing-service/Dockerfile,
    .planning/phases/03-ai-service-stabilization/03-RESEARCH.md
  </read_first>
  <behavior>
    - Importing clip_embedder at module level does NOT import torch or transformers
    - Importing real_esrgan at module level does NOT import cv2 or torch
    - Importing face_detection_service at module level does NOT import cv2 or torch
    - Importing face_embedding_service at module level does NOT import cv2 or torch
    - Importing core.database does NOT import pymilvus when MILVUS_ENABLED=false
    - requirements.txt has no duplicate httpx entries
    - requirements.txt has only onnxruntime (not onnxruntime-gpu)
    - Dockerfile HEALTHCHECK uses /health/live path
  </behavior>
  <action>
    Apply Bug fixes 4, 5, and 7 from the research, plus Dockerfile update:

    **Bug 4 - Lazy ML imports in 4 files:**

    For each file (clip_embedder.py, real_esrgan.py, face_detection_service.py, face_embedding_service.py):
    1. Remove top-level `import torch`, `import cv2`, `import numpy as np`, `from transformers import ...`, `from PIL import Image`
    2. Keep only stdlib imports, typing imports, and lightweight project imports at top level
    3. Use `TYPE_CHECKING` guard for type hints: `if TYPE_CHECKING: import torch` etc.
    4. Move heavy imports inside `_ensure_initialized()` or the first method that uses them
    5. Verify NO top-level heavy imports remain (search: `^import torch|^import cv2|^from transformers|^from PIL`)

    Follow the exact pattern from 03-RESEARCH.md "Making clip_embedder.py Lazy" code example.

    **Bug 5 - Lazy milvus import in database.py:**
    - Remove top-level `from services.milvus_service import get_milvus_service` (line ~15)
    - Move this import inside the methods that use it (store_embeddings, find_similar_by_clip)
    - Guard with `if settings.MILVUS_ENABLED:` before importing

    **Bug 7 - Clean requirements.txt:**
    - Remove duplicate `httpx` entry (keep one)
    - Remove `onnxruntime-gpu`, keep only `onnxruntime`

    **Dockerfile HEALTHCHECK update:**
    - Change HEALTHCHECK CMD from `/health` to `/health/live`
    - Set `--start-period=30s` (increased from default to allow Python startup time)

    After applying all fixes, run the full test suite:
    `cd services/ai-processing-service && python -m pytest tests/ -x -v`

    Then verify no heavy imports at module top level:
    `grep -rn "^import torch\|^import cv2\|^from transformers\|^from PIL\|^import numpy" services/ai-processing-service/src/models/ services/ai-processing-service/src/services/`
  </action>
  <verify>
    <automated>cd services/ai-processing-service && python -m pytest tests/test_lazy_imports.py tests/test_health.py tests/test_database.py -x -v --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - clip_embedder.py has no top-level torch/transformers/numpy/PIL imports (grep returns 0 matches)
    - real_esrgan.py has no top-level cv2/torch/numpy/PIL imports (grep returns 0 matches)
    - face_detection_service.py has no top-level cv2/torch/numpy/PIL imports (grep returns 0 matches)
    - face_embedding_service.py has no top-level cv2/torch/numpy/PIL imports (grep returns 0 matches)
    - database.py has no top-level milvus_service import (grep returns 0 matches for "from services.milvus_service")
    - requirements.txt has exactly one httpx entry
    - requirements.txt does NOT contain onnxruntime-gpu
    - Dockerfile HEALTHCHECK references /health/live
    - All tests in test_lazy_imports.py pass
    - Full test suite passes
  </acceptance_criteria>
  <done>All heavy ML imports are lazy. pymilvus import is guarded. requirements.txt is clean. Dockerfile health check path matches application. All tests green.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. All tests pass: `cd services/ai-processing-service && python -m pytest tests/ -v --timeout=30`
2. No heavy imports at module level: `grep -rn "^import torch\|^import cv2\|^from transformers\|^from PIL import\|^import numpy" services/ai-processing-service/src/models/ services/ai-processing-service/src/services/` returns empty
3. No sys.exit in main.py: `grep -c "sys.exit" services/ai-processing-service/src/main.py` returns 0
4. Health endpoints at correct paths: `grep "/health/live\|/health/ready" services/ai-processing-service/src/main.py` shows both
5. MILVUS_ENABLED defaults False: `grep "default=False" services/ai-processing-service/src/config.py` matches MILVUS_ENABLED line
6. Dockerfile uses correct healthcheck: `grep "health/live" services/ai-processing-service/Dockerfile` matches
</verification>

<success_criteria>
- ai-processing-service source code has all 6 bugs fixed
- /health/live and /health/ready endpoints at correct paths
- No sys.exit(1) in lifespan -- graceful degradation
- MILVUS_ENABLED defaults to False with pgvector fallback
- All ML imports (torch, cv2, transformers, numpy, PIL) are lazy in all 4 model/service files
- pymilvus import in database.py is lazy and guarded by MILVUS_ENABLED
- requirements.txt cleaned (no duplicate httpx, no onnxruntime-gpu)
- Dockerfile HEALTHCHECK targets /health/live
- Full test suite passes covering AIS-01, AIS-02, AIS-03
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-service-stabilization/03-01-SUMMARY.md`
</output>
