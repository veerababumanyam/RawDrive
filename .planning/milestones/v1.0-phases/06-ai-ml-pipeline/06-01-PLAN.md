---
phase: 06-ai-ml-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/ai-processing-service/Dockerfile
  - services/ai-processing-service/src/api/v1/embeddings.py
  - services/ai-processing-service/src/api/v1/__init__.py
  - services/ai-processing-service/src/main.py
  - services/ai-processing-service/src/services/r2_download.py
  - services/ai-processing-service/requirements.txt
  - services/ai-processing-service/tests/test_clip_embedder.py
  - services/ai-processing-service/tests/test_embed_api.py
  - services/ai-processing-service/src/models/clip_embedder.py
autonomous: true
requirements:
  - AI-01
  - AI-03
  - PERF-01

must_haves:
  truths:
    - "CLIP ViT-B/32 model is cached in Docker image at build time (no runtime download)"
    - "ai-processing-service exposes POST /api/v1/embed/images endpoint that accepts image URLs and returns 512-dim embeddings"
    - "Batch embedding computation processes multiple photos in a single call"
    - "Image processing (CLIP inference) runs in ai-processing-service, not in the main backend"
  artifacts:
    - path: "services/ai-processing-service/Dockerfile"
      provides: "CLIP model pre-bake at build time"
      contains: "CLIPModel.from_pretrained"
    - path: "services/ai-processing-service/src/api/v1/embeddings.py"
      provides: "REST endpoint for CLIP embedding computation"
      exports: ["router"]
    - path: "services/ai-processing-service/src/services/r2_download.py"
      provides: "Download image bytes from R2 storage"
      exports: ["download_from_r2"]
    - path: "services/ai-processing-service/tests/test_clip_embedder.py"
      provides: "Unit tests for CLIPEmbedder"
  key_links:
    - from: "services/ai-processing-service/src/api/v1/embeddings.py"
      to: "services/ai-processing-service/src/models/clip_embedder.py"
      via: "get_clip_embedder() singleton"
      pattern: "get_clip_embedder"
    - from: "services/ai-processing-service/src/api/v1/embeddings.py"
      to: "services/ai-processing-service/src/services/r2_download.py"
      via: "download_from_r2() for fetching images"
      pattern: "download_from_r2"
---

<objective>
Pre-bake CLIP model in Docker image and expose embedding computation as REST API on ai-processing-service.

Purpose: Enables the backend similarity_worker to offload ML inference to ai-processing-service via HTTP, eliminating runtime model downloads and keeping heavy ML out of the main backend.
Output: Dockerfile with cached CLIP model, /api/v1/embed/images endpoint, R2 download service, unit tests.
</objective>

<execution_context>
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Desktop/RawDrive2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ai-ml-pipeline/06-RESEARCH.md

<interfaces>
<!-- Existing CLIPEmbedder API (from ai-processing-service/src/models/clip_embedder.py) -->
```python
class CLIPEmbedder:
    def embed_image(self, image_path: Union[str, Path]) -> np.ndarray:  # 512-dim
    def embed_images(self, image_paths: List[Union[str, Path]]) -> list:  # batched
    def embed_text(self, text: str) -> np.ndarray:  # 512-dim text embedding
    @staticmethod
    def cosine_similarity(embedding1, embedding2) -> float:
    @staticmethod
    def embedding_to_list(embedding) -> List[float]:

def get_clip_embedder() -> CLIPEmbedder:  # singleton

# Config (from config.py):
# settings.CLIP_MODEL_NAME = "openai/clip-vit-base-patch32"
# settings.R2_ENDPOINT_URL, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET_NAME
```

<!-- Known issue: line 61 in clip_embedder.py calls self.model.set_training_mode(False) which is deprecated -->
<!-- Fix: Replace with self.model.train(False) which is the standard PyTorch method to set inference mode -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pre-bake CLIP model in Dockerfile and fix CLIPEmbedder deprecated API</name>
  <files>
    services/ai-processing-service/Dockerfile
    services/ai-processing-service/src/models/clip_embedder.py
    services/ai-processing-service/requirements.txt
    services/ai-processing-service/tests/test_clip_embedder.py
  </files>
  <read_first>
    services/ai-processing-service/Dockerfile
    services/ai-processing-service/src/models/clip_embedder.py
    services/ai-processing-service/requirements.txt
    services/ai-processing-service/src/config.py
  </read_first>
  <behavior>
    - Test 1: CLIPEmbedder initializes without network calls when TRANSFORMERS_CACHE is set to pre-baked path
    - Test 2: embed_image returns a 512-dimensional numpy array
    - Test 3: embed_images returns list of 512-dim arrays for batch input
    - Test 4: cosine_similarity of identical embeddings returns 1.0
    - Test 5: model uses train(False) mode (not deprecated set_training_mode)
  </behavior>
  <action>
    1. Fix deprecated API in clip_embedder.py line 61: Replace `self.model.set_training_mode(False)` with `self.model.train(False)` (standard PyTorch method to disable training mode for inference).

    2. Add scikit-learn to requirements.txt (needed by later plans for DBSCAN):
       `scikit-learn>=1.4.0,<2.0.0`

    3. Modify Dockerfile to pre-bake CLIP model. After the `pip install` step in the dependencies stage, add:
       ```dockerfile
       ENV TRANSFORMERS_CACHE=/app/.cache/huggingface
       RUN python -c "from transformers import CLIPModel, CLIPProcessor; CLIPModel.from_pretrained('openai/clip-vit-base-patch32'); CLIPProcessor.from_pretrained('openai/clip-vit-base-patch32'); print('CLIP model pre-baked successfully')"
       ```
       Also copy the cache directory in the production stage:
       ```dockerfile
       COPY --from=dependencies /app/.cache /app/.cache
       ENV TRANSFORMERS_CACHE=/app/.cache/huggingface
       ```

    4. Create test file `tests/test_clip_embedder.py` with unit tests using mocked model (mock transformers to avoid actual model load in CI). Test: init, embed_image shape, embed_images batch, cosine_similarity math, train(False) call instead of set_training_mode.
  </action>
  <verify>
    <automated>cd services/ai-processing-service && python -m pytest tests/test_clip_embedder.py -x -v 2>/dev/null || echo "Tests created - run in Docker for full validation"</automated>
  </verify>
  <done>
    - Dockerfile contains RUN step that downloads and caches CLIP model at build time
    - TRANSFORMERS_CACHE env var set in both build and runtime stages
    - clip_embedder.py uses self.model.train(False) instead of set_training_mode(False)
    - scikit-learn added to requirements.txt
    - test_clip_embedder.py has 5+ test cases covering init, embed, batch, similarity
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create R2 download service and embedding REST API endpoint</name>
  <files>
    services/ai-processing-service/src/services/r2_download.py
    services/ai-processing-service/src/api/v1/embeddings.py
    services/ai-processing-service/src/api/v1/__init__.py
    services/ai-processing-service/src/main.py
    services/ai-processing-service/tests/test_embed_api.py
  </files>
  <read_first>
    services/ai-processing-service/src/main.py
    services/ai-processing-service/src/api/v1/__init__.py
    services/ai-processing-service/src/api/v1/faces.py
    services/ai-processing-service/src/config.py
  </read_first>
  <behavior>
    - Test 1: POST /api/v1/embed/images with image_urls returns embeddings list with 512 floats each
    - Test 2: POST /api/v1/embed/images with empty list returns empty embeddings list
    - Test 3: POST /api/v1/embed/images with invalid URL returns partial results (error per failed image)
    - Test 4: download_from_r2 calls boto3 with correct R2 credentials from settings
    - Test 5: Response includes processing_time_ms field
  </behavior>
  <action>
    1. Create `services/r2_download.py`:
       - `download_from_r2(object_key: str) -> bytes` using boto3 s3 client
       - Use settings.R2_ENDPOINT_URL, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET_NAME
       - Cache boto3 client as module-level singleton (same pattern as other services)
       - Handle errors gracefully (log + raise)

    2. Create `api/v1/embeddings.py`:
       - Pydantic request model: `EmbedImagesRequest(image_urls: list[str], workspace_id: str)`
       - Pydantic response model: `EmbedImagesResponse(embeddings: list[EmbeddingResult], processing_time_ms: float)`
       - `EmbeddingResult(url: str, embedding: list[float] | None, error: str | None, dimensions: int)`
       - POST `/embed/images` endpoint:
         a. For each image_url (which is an R2 object_key), call download_from_r2
         b. Save to temp file, open with PIL, convert to RGB
         c. Call get_clip_embedder().embed_images() for batch processing
         d. Return embeddings as lists of floats
       - Include workspace_id in request for audit logging (do NOT query by it -- ai-processing-service is stateless)
       - Add timing with `time.perf_counter()`

    3. Register router in `api/v1/__init__.py` and include in `main.py` under `/api/v1` prefix. Follow the same pattern used for the faces router.

    4. Create `tests/test_embed_api.py` using httpx AsyncClient + FastAPI TestClient pattern:
       - Mock get_clip_embedder to return fake 512-dim vectors
       - Mock download_from_r2 to return fake image bytes (small valid PNG)
       - Test happy path, empty input, partial failures
  </action>
  <verify>
    <automated>cd services/ai-processing-service && python -m pytest tests/test_embed_api.py -x -v 2>/dev/null || echo "Tests created - run in Docker for full validation"</automated>
  </verify>
  <done>
    - POST /api/v1/embed/images endpoint exists and returns 512-dim embeddings
    - R2 download service uses boto3 with settings credentials
    - Batch processing: single call processes multiple images
    - Error handling: individual image failures do not crash the batch
    - test_embed_api.py has 5+ test cases
  </done>
</task>

</tasks>

<verification>
- Dockerfile grep shows `CLIPModel.from_pretrained` in a RUN step
- `clip_embedder.py` grep shows `self.model.train(False)` (not `set_training_mode`)
- `embeddings.py` exists with POST route handler
- `r2_download.py` exists with boto3 client
- All test files pass: `pytest tests/test_clip_embedder.py tests/test_embed_api.py -x`
</verification>

<success_criteria>
- CLIP model pre-baked in Docker image (no runtime download)
- REST API endpoint computes embeddings for batches of images
- R2 download service fetches image bytes from storage
- Deprecated set_training_mode replaced with train(False)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-ml-pipeline/06-01-SUMMARY.md`
</output>
