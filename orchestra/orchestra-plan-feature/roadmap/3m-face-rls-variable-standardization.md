# 3m - Standardize face_clusters RLS on app.current_workspace_id (D-2)

**Status:** PENDING

> Generated by /orchestra-plan-feature from the main feature "FaceID / Find-Me audit remediation (faceid-findme-audit-2026-06-07)".
> Build with: `/orchestra-task-add 3m`

## User story

As a platform engineer, I want face_clusters/aliases RLS to use one consistent session variable, so the defensive dual-variable coverage in migration 180 can be simplified and future tables follow one convention.

## Scope

- **Goal:** Standardize the face RLS session variable on app.current_workspace_id via an append-only migration with a contract test.
- **Summary:** face_clusters/aliases RLS uses app.current_workspace_id while older tables use app.workspace_id; migration 180 defensively covers both. Add an append-only migration standardizing the face RLS policies on app.current_workspace_id (keeping isolation intact), with a contract test, as part of the cross-domain RLS cleanup.
- **Out of scope:** The calendar/CRM RLS cleanup (separate domains)
- **Out of scope:** Changing tenant-isolation semantics

## Dependencies

- None (no upstream sub-feature)
- Module: backend/internal/database/migrations/NNN_face_rls_standardize.up.sql
- Module: backend/internal/database/migrations/NNN_face_rls_standardize.down.sql
- Module: backend/internal/database/migrations/NNN_face_rls_standardize_test.go

## Acceptance criteria

- [ ] An append-only migration (number verified unique vs origin/main) standardizes face_clusters/aliases RLS on app.current_workspace_id with a paired down migration.
- [ ] A contract test asserts cross-tenant isolation still holds (a session for workspace A cannot read workspace B face rows).
- [ ] Migration numbering passes scripts/check-migration-numbers.mjs and the uniqueness contract test.

## Design contract

Reuse the host design system - do not introduce a parallel style.
- AGENTS.md:1

DB-only slice; no UI.

## Structure contract

Place + wire files where their peers live (cited below).
- backend/internal/ai/face_repo.go:161

face_repo is the access layer whose RLS-scoped queries the policy standardization must keep working.

## Verification / test plan

- node scripts/check-migration-numbers.mjs
- cd backend && go test ./internal/database/migrations -count=1

## NFR and compliance

- NFR: Isolation must be preserved exactly; the migration only renames the predicate variable, not the policy intent.
- Compliance: Maintains tenant isolation for special-category data.

## Rollback

Run the paired down migration to restore the dual-variable coverage.
