# 3c - Add the affirmative consent step to the standalone public Find-Me page (FE-2)

**Status:** PENDING

> Generated by /orchestra-plan-feature from the main feature "FaceID / Find-Me audit remediation (faceid-findme-audit-2026-06-07)".
> Build with: `/orchestra-task-add 3c`

## User story

As a wedding guest using the primary public Find-Me page, I want to explicitly consent to biometric face matching before my selfie is used, matching the affordance the modal already shows, so the consent contract (GAL-FR-107) is consistent across surfaces.

## Scope

- **Goal:** Give the standalone Find-Me page the same explicit consent step as the modal and wire it to the backend consent contract.
- **Summary:** The standalone photo-search page only shows descriptive prose; add an affirmative consent affordance (the modal already has one) that must be accepted before capture, and send consent_given:true to the now-consent-aware backend (3b).
- **Out of scope:** Converging the two Find-Me UIs (that is 3i)
- **Out of scope:** Backend consent persistence (that is 3b)

## Dependencies

- Depends on roadmap #3b
- Module: frontend/src/app/g/[slug]/photo-search/page.tsx

## Acceptance criteria

- [ ] The standalone Find-Me page presents an affirmative consent control that must be accepted before the camera/selfie capture step proceeds.
- [ ] On submit the request sends consent_given:true; without acceptance the user cannot reach capture.
- [ ] The consent control uses semantic design tokens (no Tailwind primitive/arbitrary values) and meets the 44px touch-target + focus-ring laws.
- [ ] A frontend test asserts capture is gated on consent acceptance.

## Design contract

Reuse the host design system - do not introduce a parallel style.
- frontend/src/components/gallery/faceid-gate.tsx:130
- design-tokens.json:1

The modal gate already shows a consent notice; reuse the same copy/affordance pattern with semantic tokens.

## Structure contract

Place + wire files where their peers live (cited below).
- frontend/src/app/g/[slug]/photo-search/page.tsx:353

Around :353 the standalone page renders descriptive prose with no affirmative consent control; insert the consent gate before capture.

## Verification / test plan

- pnpm --dir frontend test src/app/g
- pnpm --dir frontend lint
- pnpm --dir frontend exec tsc --noEmit

## NFR and compliance

- NFR: No regression to the existing 8-stage capture flow; consent step is additive and theme-correct across all three themes.
- Compliance: Delivers the user-facing GAL-FR-107 consent affordance the primary biometric surface was missing.

## Rollback

Revert the page change; backend still requires consent (3b) so the surface fails closed rather than matching without consent.
