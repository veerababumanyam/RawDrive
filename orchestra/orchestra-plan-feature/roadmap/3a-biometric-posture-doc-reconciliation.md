# 3a - Reconcile the biometric opt-in/opt-out documentation to the shipped default-on reality (B-P1 docs)

**Status:** PENDING

> Generated by /orchestra-plan-feature from the main feature "FaceID / Find-Me audit remediation (faceid-findme-audit-2026-06-07)".
> Build with: `/orchestra-task-add 3a`

## User story

As an engineer or auditor reading the code and AGENTS.md, I want the comments and docs about biometric face-recognition default state to match what migrations 111/112 actually shipped, so the codebase does not assert a false DPDP/GDPR opt-in contract.

## Scope

- **Goal:** Make the documentation/comments tell the truth about the default-on biometric posture; record that the real consent capture lands in 3b/3c.
- **Summary:** Migrations 112 (workspaces) and 111 (galleries) set face recognition DEFAULT TRUE and backfilled, yet face_service.go, workspace_face_recognition_handler.go and AGENTS.md still say 'DEFAULT FALSE - opt-in'. Update those comments/docs to state the real default-on posture and reference the risk-accepted-pending-consent decision recorded by 3j, with no behavior change.
- **Out of scope:** Changing any migration or runtime default (no behavior change in this slice)
- **Out of scope:** Adding consent capture (that is 3b/3c)
- **Out of scope:** The licensing/posture decision record itself (that is 3j)

## Dependencies

- None (no upstream sub-feature)
- Module: backend/internal/ai/face_service.go
- Module: backend/internal/handler/workspace_face_recognition_handler.go
- Module: AGENTS.md

## Acceptance criteria

- [ ] No source comment or doc in the touched files asserts biometric face recognition is 'DEFAULT FALSE' or 'opt-in' as the shipped contract.
- [ ] The corrected comments/docs explicitly state default-on (per migrations 111/112) and point to the consent capture (3b/3c) and the posture decision record (3j).
- [ ] No migration, default value, or runtime branch is changed; backend tests still pass unchanged.

## Design contract

Reuse the host design system - do not introduce a parallel style.
- AGENTS.md:1

Documentation-only slice; no UI.

## Structure contract

Place + wire files where their peers live (cited below).
- backend/internal/ai/face_service.go:77
- backend/internal/ai/face_service.go:253
- backend/internal/handler/workspace_face_recognition_handler.go:6

These are the three places that still assert the stale 'DEFAULT FALSE / opt-in' contract; AGENTS.md mirrors it.

## Verification / test plan

- cd backend && go build ./... && go test ./internal/ai ./internal/handler -count=1
- grep -rin 'DEFAULT FALSE' backend/internal/ai/face_service.go backend/internal/handler/workspace_face_recognition_handler.go AGENTS.md (expect no stale opt-in assertion)

## NFR and compliance

- NFR: No behavior change; pure documentation/comment correction.
- Compliance: Removes a misleading DPDP/GDPR opt-in claim; the real consent control is delivered by 3b/3c.

## Rollback

Revert the comment/doc edits; no schema or runtime impact.
