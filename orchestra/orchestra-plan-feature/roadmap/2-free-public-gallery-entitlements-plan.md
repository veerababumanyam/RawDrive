# 2 - Free subscription public gallery branding, gallery cap, and strict admin-defined storage quota (plan)

**Status:** PENDING

> Umbrella plan generated by /orchestra-plan-feature. This is the MILESTONE; the sub-features below
> are the trackable units. Build each with `/orchestra-task-add <number>` in dependency order.

## Goal

Make the free subscription visibly platform-branded on shared public galleries, limit free workspaces to three active galleries, and enforce the admin-configured storage quota strictly until the workspace upgrades to a paid subscription.

## Summary

Use the existing subscription_plans catalog and public-gallery branding endpoint as the entitlement authority. Keep the RawDrive logo requirement free-only; paid tiers keep the existing white-label/custom-brand behavior.

## Sub-features

- [ ] #2a Set free-plan catalog defaults to three galleries -> `/orchestra-task-add 2a`
- [ ] #2b Enforce the free gallery limit at gallery authority points (after #2a) -> `/orchestra-task-add 2b`
- [ ] #2c Use admin-defined storage quota in strict upload accounting (after #2a) -> `/orchestra-task-add 2c`
- [ ] #2d Render RawDrive logo badge only on free public galleries (after #2a) -> `/orchestra-task-add 2d`
