# 3d - Route the /ai/faces FaceClusterDetail through the decryption hook (FE-6)

**Status:** PENDING

> Generated by /orchestra-plan-feature from the main feature "FaceID / Find-Me audit remediation (faceid-findme-audit-2026-06-07)".
> Build with: `/orchestra-task-add 3d`

## User story

As a photographer viewing the global /ai/faces surface, I want face cluster thumbnails to render decrypted instead of ciphertext on E2EE galleries, so the owner face surface is not permanently broken.

## Scope

- **Goal:** Make /ai/faces thumbnails decrypt correctly and show the real cluster name, reusing the existing decrypt hook.
- **Summary:** FaceClusterDetail renders thumbnails via getAssetPreviewUrl with no decryption hook, producing ciphertext images on every E2EE gallery, and is passed clusterName='' so the header always says 'Unknown Person'. Route its thumbnails through the existing DecryptedThumb/useDecryptedAssetUrl path (as the gallery-scoped review panel already does) and pass the real cluster name.
- **Out of scope:** Retiring the /ai/faces surface entirely (kept as the simplest fix; convergence is tracked separately)
- **Out of scope:** The GlassIconButton/token cleanup on this surface (that is 3n)

## Dependencies

- None (no upstream sub-feature)
- Module: frontend/src/components/ai/FaceClusterDetail.tsx
- Module: frontend/src/app/(dashboard)/ai/faces/page.tsx

## Acceptance criteria

- [ ] FaceClusterDetail renders thumbnails via DecryptedThumb/useDecryptedAssetUrl (no raw getAssetPreviewUrl ciphertext) so E2EE-gallery faces display decrypted.
- [ ] The cluster header shows the real cluster name when one exists, not a hardcoded 'Unknown Person' for every cluster.
- [ ] A frontend test asserts the decrypt hook/DecryptedThumb is used for thumbnails on this surface.

## Design contract

Reuse the host design system - do not introduce a parallel style.
- frontend/src/components/gallery/decrypted-thumb.tsx:1
- frontend/src/lib/media-encryption/use-decrypted-asset-url.ts:1

Reuse the exact decrypt component/hook the gallery-scoped review panel already uses.

## Structure contract

Place + wire files where their peers live (cited below).
- frontend/src/components/ai/FaceClusterDetail.tsx:76
- frontend/src/components/ai/FaceClusterDetail.tsx:60

:76 is the undecrypted getAssetPreviewUrl thumbnail; :60 is the clusterName||'Unknown Person' header fed an empty name.

## Verification / test plan

- pnpm --dir frontend test src/components/ai
- pnpm --dir frontend exec tsc --noEmit

## NFR and compliance

- NFR: Decryption is in-memory per tile (L2), nothing persisted; no N+1 introduced.
- Compliance: Restores the E2EE-correct rendering law on the global owner surface.

## Rollback

Revert FaceClusterDetail/page changes.
