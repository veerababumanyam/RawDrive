# 3j - Record the buffalo_l licensing constraint + E2EE Find-Me posture decision (B-L1 + B-E1 decision)

**Status:** PENDING

> Generated by /orchestra-plan-feature from the main feature "FaceID / Find-Me audit remediation (faceid-findme-audit-2026-06-07)".
> Build with: `/orchestra-task-add 3j`

## User story

As an owner/engineer, I want the non-commercial buffalo_l licensing exposure and the unresolved E2EE recognition posture written into the spec and AGENTS.md, so the live server's licensing risk and the slice-2b posture (a vs b) are explicit decisions, not silent gaps.

## Scope

- **Goal:** Make the licensing exposure and the E2EE posture choice explicit, written decisions referenced by the code/docs.
- **Summary:** The live face-svc sidecar ships InsightFace buffalo_l pretrained weights that are licensed non-commercial-research-only, undocumented in the audit and the e2ee-face-search spec; and B-E1 never names the chosen E2EE posture (server-side plaintext match vs client-side encrypted-embedding match). Record both as explicit decisions (an ADR/decision section): the licensing options (buy commercial license / AuraFace / MobileFaceNet, each an index-wide regen), the live-server exposure, the §7.4 posture (a)/(b) choice and rationale, and that in-browser ONNX embedding (slice 2b) remains owner-blocked on commercial model licensing + hosting. Documentation only.
- **Out of scope:** Buying a license, hosting a model, or wiring ONNX embedding (owner-blocked external prerequisites)
- **Out of scope:** Any runtime/model change

## Dependencies

- None (no upstream sub-feature)
- Module: docs/features/e2ee-face-search/README.md
- Module: docs/decisions/faceid-licensing-and-e2ee-posture.md
- Module: AGENTS.md

## Acceptance criteria

- [ ] A decision record documents the buffalo_l non-commercial licensing constraint, the live face-svc exposure, and the resolution options (commercial license / model swap with full re-index).
- [ ] The record names the posture choice (server-side plaintext-at-rest match vs client-side encrypted-embedding match) with rationale, and states slice-2b (in-browser ONNX) is owner-blocked on licensing + model hosting.
- [ ] The e2ee-face-search spec and AGENTS.md link to the decision record; no code/runtime change.
- [ ] The record explicitly names the disposition of the ingest-endpoint flag inconsistency (StoreEmbeddings is client_face_index-gated while StoreIndexImage is not) and recommends, per chosen posture, whether the plaintext /face-index-image path is retired or guarded — so the post-decision runtime reconciliation is a recorded, actionable follow-up (tracked, not a dropped §8.5 blocker).
- [ ] The decision record is referenced by 3a's reconciled comments.

## Design contract

Reuse the host design system - do not introduce a parallel style.
- AGENTS.md:1

Documentation/decision slice; no UI.

## Structure contract

Place + wire files where their peers live (cited below).
- backend/internal/featureflag/client_face_index.go:51
- backend/internal/handler/face_embeddings_handler.go:265

:51 is the client_face_index default-off flag the decision discusses; handler :265 is the live plaintext-frame StoreIndexImage path the posture decision concerns.

## Verification / test plan

- test -f docs/decisions/faceid-licensing-and-e2ee-posture.md
- grep -li 'buffalo_l' docs/features/e2ee-face-search/README.md docs/decisions/faceid-licensing-and-e2ee-posture.md

## NFR and compliance

- NFR: No code or runtime impact.
- Compliance: Surfaces a live commercial-licensing exposure and the special-category-at-rest posture decision for owner sign-off.

## Rollback

Remove the decision doc and the spec/AGENTS links.
