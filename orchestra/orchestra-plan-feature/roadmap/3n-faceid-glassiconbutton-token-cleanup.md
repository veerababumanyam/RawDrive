# 3n - Replace raw face-control buttons with GlassIconButton + fix token/empty-state law violations (FE-7)

**Status:** PENDING

> Generated by /orchestra-plan-feature from the main feature "FaceID / Find-Me audit remediation (faceid-findme-audit-2026-06-07)".
> Build with: `/orchestra-task-add 3n`

## User story

As a user, I want the face controls to use the standard GlassIconButton + design tokens and to distinguish a fetch error from an empty state, so the surfaces follow the icon-button law and do not mask errors as 'no faces'.

## Scope

- **Goal:** Bring face controls onto GlassIconButton + semantic tokens and stop masking fetch errors as empty.
- **Summary:** Every face control is a raw <button> (zero GlassIconButton across gate, panel, browser), there is an arbitrary min-h-[44px], and FaceClusterBrowser masks fetch errors as the empty state. Swap icon controls to GlassIconButton, replace the arbitrary value with a token-driven touch target, and distinguish fetch error from empty in FaceClusterBrowser.
- **Out of scope:** UI convergence (3i)
- **Out of scope:** Dialog a11y (3l)

## Dependencies

- None (no upstream sub-feature)
- Module: frontend/src/components/ai/FaceClusterBrowser.tsx
- Module: frontend/src/components/gallery/faceid-gate.tsx
- Module: frontend/src/components/ai/FaceIdentityReviewPanel.tsx

## Acceptance criteria

- [ ] Icon-action controls across the face gate/panel/browser use GlassIconButton with a required label (no raw <button> for icon actions).
- [ ] The arbitrary min-h-[44px] is replaced with a token-driven 44px touch target; no Tailwind primitive/arbitrary values remain in the touched controls.
- [ ] FaceClusterBrowser distinguishes a fetch error from the empty state (error is not silently rendered as 'no faces').
- [ ] Tests/build pass with the GlassIconButton usage and the error-vs-empty distinction.

## Design contract

Reuse the host design system - do not introduce a parallel style.
- frontend/src/components/ui/glass-icon-button.tsx:1
- design-tokens.json:1

Use the canonical GlassIconButton + semantic tokens; required label prop.

## Structure contract

Place + wire files where their peers live (cited below).
- frontend/src/components/ai/FaceClusterBrowser.tsx:101
- frontend/src/components/ai/FaceClusterBrowser.tsx:47

:101 is the arbitrary min-h-[44px]; :47 masks fetch errors as the empty state.

## Verification / test plan

- pnpm --dir frontend test src/components/ai src/components/gallery
- pnpm --dir frontend lint
- pnpm --dir frontend exec tsc --noEmit

## NFR and compliance

- NFR: Renders correctly across all three themes; touch targets >=44px; focus ring uses focusRing tokens.
- Compliance: Design-system law compliance; no data impact.

## Rollback

Revert the component edits.
