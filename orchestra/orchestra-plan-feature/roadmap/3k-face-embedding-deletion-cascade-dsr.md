# 3k - Explicit face-embedding deletion + cascade regression test + DSR coverage (B-X1)

**Status:** PENDING

> Generated by /orchestra-plan-feature from the main feature "FaceID / Find-Me audit remediation (faceid-findme-audit-2026-06-07)".
> Build with: `/orchestra-task-add 3k`

## User story

As a data-protection officer, I want face embeddings provably deleted when an asset is deleted and included in data-subject export, so biometric data deletion/portability is enforced and tested, not just assumed via FK cascade.

## Scope

- **Goal:** Prove and test biometric-embedding deletion on asset delete, and include face data in DSR/export.
- **Summary:** Embedding deletion relies solely on FK cascade; DeleteFacesByAsset is dead code; there is no cascade regression test and no evidence face embeddings are in any DSR/export path. Wire/keep an explicit deletion call (or assert the cascade), add a regression test proving asset hard-delete removes face_clusters rows, and include face data in the DSR/export path.
- **Out of scope:** Changing the synchronous hard-delete model (project law)
- **Out of scope:** Changing the embedding schema

## Dependencies

- None (no upstream sub-feature)
- Module: backend/internal/ai/face_repo.go
- Module: backend/internal/ai/face_service.go
- Module: backend/internal/handler/asset_handler.go

## Acceptance criteria

- [ ] A regression test asserts that hard-deleting an asset removes its face_clusters rows (cascade or explicit DeleteFacesByAsset), using tests/photos assets.
- [ ] DeleteFacesByAsset is either wired into the delete path or removed in favor of an asserted cascade (no dead code left behind).
- [ ] Face embeddings/metadata are included in the DSR/export path with a test asserting their presence.

## Design contract

Reuse the host design system - do not introduce a parallel style.
- AGENTS.md:1

Backend-only slice; no UI.

## Structure contract

Place + wire files where their peers live (cited below).
- backend/internal/ai/face_repo.go:161
- backend/internal/ai/face_service.go:104

face_repo holds the face_clusters access incl. the dead DeleteFacesByAsset; face_service :104 is the detect/store path tying embeddings to assets.

## Verification / test plan

- cd backend && go test ./internal/ai ./internal/handler -run 'Delete|Cascade|DSR|Export' -count=1
- cd backend && go build ./...

## NFR and compliance

- NFR: Deletion stays synchronous + awaited per the asset hard-delete law; no retention window introduced.
- Compliance: DPDP/GDPR erasure + portability for special-category biometric embeddings, now tested.

## Rollback

Revert the deletion/test/DSR edits.
