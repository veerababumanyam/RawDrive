# 3o - Remove or wire the inert face API helpers (B-X2)

**Status:** PENDING

> Generated by /orchestra-plan-feature from the main feature "FaceID / Find-Me audit remediation (faceid-findme-audit-2026-06-07)".
> Build with: `/orchestra-task-add 3o`

## User story

As a maintainer, I want the wired-but-uncalled face API surface either connected or removed, so the codebase does not carry dead/ambiguous endpoints and clients.

## Scope

- **Goal:** Eliminate ambiguity: each inert helper is removed, wired, or explicitly annotated as a retained future-slice stub.
- **Summary:** getClusterContact, getClusterAssets, triggerFaceDetect, and uploadAssetFaceEmbeddings are client/API surface with no live caller (dead or future-slice). Decide per helper: remove if obsolete, or wire if it has a current caller; document any intentionally-retained future-slice helper (uploadAssetFaceEmbeddings is the 2b ingest, retained + annotated per 3j).
- **Out of scope:** Implementing 2b ingest (owner-blocked)
- **Out of scope:** Changing live endpoint behavior

## Dependencies

- None (no upstream sub-feature)
- Module: frontend/src/lib/api/ai.ts
- Module: backend/internal/handler/face_embeddings_handler.go

## Acceptance criteria

- [ ] Each of getClusterContact/getClusterAssets/triggerFaceDetect is either removed (no remaining references) or wired to a real caller.
- [ ] uploadAssetFaceEmbeddings (the 2b client ingest) is explicitly annotated as a retained, owner-blocked future-slice stub referencing the 3j decision, not silently dead.
- [ ] Build + tests pass with no dangling references after removal/wiring.

## Design contract

Reuse the host design system - do not introduce a parallel style.
- frontend/AGENTS.md:1

API-surface cleanup; no new UI.

## Structure contract

Place + wire files where their peers live (cited below).
- frontend/src/lib/api/ai.ts:1
- backend/internal/handler/face_embeddings_handler.go:152

ai.ts holds the inert client helpers incl. uploadAssetFaceEmbeddings; handler :152 is the flag-gated StoreEmbeddings endpoint behind it.

## Verification / test plan

- pnpm --dir frontend exec tsc --noEmit
- pnpm --dir frontend test src/lib/api
- cd backend && go build ./...

## NFR and compliance

- NFR: No change to any live endpoint's behavior.
- Compliance: Reduces dead-code attack/confusion surface.

## Rollback

Restore the removed helpers from git history.
