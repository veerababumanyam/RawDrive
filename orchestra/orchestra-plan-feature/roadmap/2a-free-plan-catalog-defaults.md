# 2a - Set free-plan catalog defaults to three galleries

**Status:** PENDING

> Generated by /orchestra-plan-feature from the main feature "Free subscription public gallery branding, gallery cap, and strict admin-defined storage quota".
> Build with: `/orchestra-task-add 2a`

## User story

As a platform admin, I want the Starter/free catalog to say three galleries and expose that limit through the existing admin/public plan APIs so enforcement and UI copy use the same source.

## Scope

- **Goal:** Change only the free-plan gallery limit and user-facing plan copy; leave paid quotas, paid gallery limits, and pay-per-event storage semantics unchanged.
- **Summary:** Update the static fallback catalog, frontend fallback tokens, and an append-only migration so the DB-backed subscription_plans and subscription_plan_versions record free gallery_limit = 3 while preserving admin-editable storage quota bytes.
- **Out of scope:** Changing the free storage quota amount unless an admin catalog row already defines it
- **Out of scope:** Changing paid plan prices or paid gallery limits
- **Out of scope:** Adding a new billing subsystem outside subscription_plans

## Dependencies

- None (no upstream sub-feature)
- Module: backend/internal/service/plan_catalog.go
- Module: frontend/src/lib/tokens.ts
- Module: backend/internal/database/migrations/188_free_public_gallery_entitlements.up.sql
- Module: backend/internal/database/migrations/188_free_public_gallery_entitlements.down.sql

## Acceptance criteria

- [ ] The static backend free catalog has GalleryLimit 3 and features/copy that no longer claim one event.
- [ ] The frontend fallback pricing plan for free has galleries 3 and matching copy.
- [ ] An append-only migration updates subscription_plans.free gallery_limit to 3 and inserts an approved subscription_plan_versions row only when the current approved/free version does not already match.
- [ ] The migration has a focused contract test and does not reuse an existing migration number.

## Design contract

Reuse the host design system - do not introduce a parallel style.
- design-tokens.json:21
- frontend/AGENTS.md:24

No new visual treatment is needed in this slice; keep catalog text within existing plan surfaces and token-generated styling.

## Structure contract

Place + wire files where their peers live (cited below).
- backend/internal/service/plan_catalog.go:47
- backend/internal/service/plan_catalog.go:56
- frontend/src/lib/tokens.ts:263
- frontend/src/lib/tokens.ts:272
- backend/internal/handler/admin_plans.go:20
- backend/internal/handler/admin_plans.go:50

The catalog already carries quota_bytes and gallery_limit through backend and frontend fallbacks; update those existing rows rather than creating a second entitlement table.

## Verification / test plan

- node scripts/check-migration-numbers.mjs
- cd backend && go test ./internal/database/migrations ./internal/service -count=1 -run 'Test.*(188|PlanDefaultQuotaBytes|PlanCatalog)'
- pnpm --dir frontend test src/lib

## NFR and compliance

- NFR: Migration must be idempotent against manually updated local data and deployed data that already has the desired free limit.
- NFR: Static fallback remains a fallback only; DB-backed catalog remains canonical when present.
- Compliance: No secrets or storage credentials are introduced.

## Rollback

Run the paired 188 down migration to restore the prior free gallery_limit/copy snapshot, then revert the static frontend/backend fallback copy.
