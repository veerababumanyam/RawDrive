{
  "name": "rawdrive-cdn-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: signed-URL-validated, SSE-C-injecting CDN edge for Backblaze B2 (rawdriveclientfiles).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
