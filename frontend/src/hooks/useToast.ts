/**
 * useToast hook re-export
 *
 * Re-exports the useToast hook from the Toast component for convenience.
 */

export { useToast, useToastActions } from '@/components/ui/Toast';
export type { ToastData, ToastVariant } from '@/components/ui/Toast';
