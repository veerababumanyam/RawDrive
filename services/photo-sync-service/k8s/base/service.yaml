apiVersion: v1
kind: Service
metadata:
  name: photo-sync-service
  labels:
    app: photo-sync-service
    tier: backend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: photo-sync-service
