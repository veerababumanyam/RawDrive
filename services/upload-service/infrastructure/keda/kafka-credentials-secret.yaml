apiVersion: v1
kind: Secret
metadata:
  name: kafka-credentials
  labels:
    app: upload-service
    component: uploads
type: Opaque
stringData:
  username: "rawdrive"
  # IMPORTANT: Replace with actual Kafka password in production
  # Use external secret management (Vault, AWS Secrets Manager) for production
  password: "CHANGE_ME_KAFKA_PASSWORD"
