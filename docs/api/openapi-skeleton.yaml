# DEPRECATED — superseded by docs/api/openapi.yaml
#
# This 75-line Swagger 2.0 placeholder was the original ISSUE-004
# scaffold. As of the ISSUE-005 follow-up (brownfield fix wave, 2026-
# 04-11) the canonical spec is openapi.yaml in this same directory,
# written against OpenAPI 3.1 and covering the canary-critical
# surface: auth, TOTP MFA, chunked upload (TUS 1.0.0), single-file
# asset upload, asset CRUD, gallery CRUD, and gallery share links.
#
# This file is kept only to preserve the path `docs/api/openapi-
# skeleton.yaml` for any link or tool that may still reference it.
# The next commit that touches this directory should delete it.
#
# Primary spec:   docs/api/openapi.yaml
# Backing code:   backend/internal/auth/{handler,mfa_handler}.go
#                 backend/internal/handler/{gallery_handler,asset_handler,
#                                          chunked_upload,routes_m2}.go
# Spec format:    OpenAPI 3.1.0 (not Swagger 2.0)
#
# Dual mount note: main.go (backend/cmd/api/main.go:455-495) mounts the
# auth handler at BOTH /auth/* and /api/v1/auth/*. The canonical spec
# documents only /auth/* for brevity — /api/v1/auth/* is the exact
# same surface and accepts the same requests.

openapi: 3.1.0
info:
  title: RawDrive API (deprecated skeleton)
  version: "0.0.0-deprecated"
  description: |
    This file is a deprecation pointer. See `docs/api/openapi.yaml`
    for the real API specification.
paths: {}
