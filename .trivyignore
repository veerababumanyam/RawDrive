# Trivy ignore list. Trivy auto-loads this for `trivy fs .`.
# RULE: every entry MUST carry a justification + a re-review date. Do not add
# CRITICAL/HIGH vulns that have an available fix — upgrade those instead.

# (no active ignores)
#
# RESOLVED 2026-06-03 — CVE-2023-36308 (DoS in github.com/disintegration/imaging)
# was eliminated by REMOVING the dependency, not ignoring it: the thumbnail /
# cover-variant / watermark services now use internal/imageops, built on the
# maintained golang.org/x/image/draw + stdlib. No ignore entry is needed.
